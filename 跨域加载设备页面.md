# 跨域加载设备页面（第三方设备界面 iframe）

设备的界面（空调、喷淋、开关…… 的平板端 / 手机端）由**第三方自行开发、自行部署**，宿主
（webapp-matrix 的设备详情页）用 `<iframe>` 把它嵌进来。跨域是这套做法的前提，也是它全部麻烦的来源。
本文写清楚：为什么这么设计、跨域到底限制了什么、第三方页面要做什么、宿主做了什么、以及踩过的坑。

样例实现在 `service/device/`（`air-conditioner` / `sprinkler` / `switch`，各 `mobile` / `tablet` 两版），
宿主实现在 `src/app/pages/main/device/detail/device.detail.component.ts`。

---

## 一、为什么用 iframe

* **技术栈自由**：第三方用 jQuery、Vue、React 还是纯 HTML 都行，不用跟着宿主升级 Angular。
* **天然隔离**：第三方页面的样式（尤其是 `*` / `body` 级别的重置）不会污染宿主，宿主的
  ng-zorro 全局样式也不会破坏第三方页面。用组件库、Web Component 都做不到这么干净。
* **升级互不牵连**：第三方页面改版不用重新发宿主。

代价就是下一节的跨域限制 —— 选 iframe 就等于选了「只能靠 postMessage 通信」。

---

## 二、跨域限制了哪些事（原理）

同源 = **scheme + host + port 三者全同**。宿主跑在 `https://xxx.openxiot.cn`，第三方页面在
另一个域名（本地调试是 `http://127.0.0.1:8000` 对 `http://localhost:4200`）—— 端口不同即跨域。

| 宿主想做的事 | 跨域下可否 |
|---|---|
| 设置 iframe 的宽高 | ✅ |
| 给 iframe `postMessage` | ✅ |
| 接收 iframe 的 `postMessage` | ✅ |
| 读 / 改 iframe 里的 DOM | ❌ `contentDocument` 是 `null` |
| 量 iframe 的内容高度（`scrollHeight`） | ❌ |
| 给 iframe 注入 CSS | ❌ |
| 读 iframe 的 URL、监听它的点击 | ❌（`location` 只可写、不可读） |

**结论：宿主与第三方页面之间只有 postMessage 一条通道。** 凡是「宿主想自动做点什么」的需求，最后都
落到「第三方页面愿不愿意配合上报」上 —— 比如下面要讲的高度。

（`sandbox` 属性可以在同源的基础上进一步收紧，见第八节第 8 条。）

---

## 三、两条滚动条是怎么来的

* **宿主**：设备详情页在 `main.component.less` 的 `nz-content { overflow: auto }` 里 —— 宿主有自己的滚动条。
* **iframe**：写死 `height="600"`，而第三方页面内容比 600px 高 —— 内部也有自己的滚动条。

于是同屏两条滚动条，且内层那条只滚一小块，很难用。

**唯一正确的解法：把 iframe 的高度设成它内容的高度。** 内容不溢出，内部自然没有滚动条，
整页由宿主的滚动条统管。这不是「把条藏起来」，而是「让内部压根没有可滚的东西」。

几个看似可行、实际不行的做法：

| 做法 | 为什么不行 |
|---|---|
| `scrolling="no"` | 只是把内部的滚动条藏掉，超出的内容**既看不见也滚不到** —— 等于丢内容。本工程**没有**用它，理由见第六节 |
| 宿主自己量 `scrollHeight` 再撑高 | 跨域读不到（第二节），只有同源才行 |
| 把 iframe 高度写成 `10000px` 之类的固定大值 | 内容短时底下留一大片空白，内容长时照样被裁 |
| 让宿主去改第三方页面的 `overflow` | 同上，跨域拿不到它的 DOM |

而这个高度**只有第三方页面自己知道** —— 所以高度上报是整套契约里唯一必不可少的一条。

---

## 四、第三方页面要做的四件事

### 1. 上报内容高度（必须）

把下面这段贴到页面脚本末尾（`</script>` 之前）：

```js
// ===================== 嵌入宿主页面（跨域 iframe）时的高度上报 =====================
// 本页是"内容流式高度"的，宿主会把 iframe 撑到本页内容高度：这样本页不会出现自己的滚动条，
// 由宿主页面的滚动条统管（否则会同时出现内外两条滚动条）。跨域下宿主读不到本页的 DOM，
// 高度只能由本页自己上报。
let reportedHeight = 0;
function reportHeight() {
  const height = document.documentElement.scrollHeight;
  if (height === reportedHeight) return; // 值没变就不重复上报，免得和宿主来回弹
  reportedHeight = height;
  parent.postMessage({ type: "iframe-height", height: height }, "*");
}
if (window.parent !== window) { // 只有被嵌进 iframe 时才上报；直接打开本页预览时不打扰
  new ResizeObserver(reportHeight).observe(document.documentElement);
  window.addEventListener("load", reportHeight);
  reportHeight();
}
```

四个要点：

* 用 `ResizeObserver` **持续**上报，不能只在 `load` 时报一次 —— 折叠展开、异步取数、字体加载完
  都会让页面变高，只报一次的话后半截内容又被裁在 iframe 外。
* 值没变就不要重复发（`reportedHeight` 那行），否则宿主的「改高度 → 触发 resize → 再上报」可能来回弹。
* `window.parent !== window` 判断是为了**直接打开**本页预览时（平板/手机端单独看）不白发消息。
* 发的时候 targetOrigin 用 `"*"` 就够了（高度不是秘密）；**接收方**才需要严格校验来源，宿主那边做了。

### 2. 页面要保持「内容流式高度」（同样必须）

这是第 1 条能生效的前提：

* `html` / `body` 上**不要**写 `height: 100vh`、`height: 100%`，也**不要**自己加 `overflow: hidden`
  或让 `body` 出现滚动条。想撑满首屏请用 `min-height: 100vh`。
* 写了 `100vh` 会怎样：宿主把 iframe 撑到 `scrollHeight`（= 100vh），页面内部永远"刚好装下"，
  而你排在 100vh 之外的内容就在 iframe 外，用户既看不到也滚不到。
* 之所以强调，是因为「整屏应用」式的布局（`height: 100vh` + 内部滚动区）在平板端很常见 ——
  那种页面**不适合**被这样嵌进来，要走第九节的反向方案。

### 3. 提示信息（toast）交给宿主弹（强烈建议）

`position: fixed` 相对的是 **iframe 的视口**，而 iframe 被撑成全高（几千像素）后，它的"底部"
在用户的屏幕外面 —— 所有 fixed 定位的提示都会出现在用户看不见的地方。所以：

```js
function toast(msg) {
  // 被宿主嵌进 iframe 时交给宿主弹提示（宿主用 nz-message 展示）
  if (window.parent !== window) {
    parent.postMessage({ type: "toast", message: msg }, "*");
    return;
  }
  // 下面是直接打开本页预览时的本地提示，保持原样即可
  const t = $("toast");
  t.textContent = msg;
  t.classList.add("show");
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => t.classList.remove("show"), 1800);
}
```

同样的问题也适用于悬浮按钮、底部抽屉、遮罩层 —— **任何 `position: fixed` 的元素**都会被放到
用户视口外。要么交给宿主，要么改成随内容流的提示（`position: absolute` 且贴住触发它的元素）。

### 4. 别在 iframe 里做整页跳转

iframe 里点 `<a href="/other.html">` 默认**在 iframe 内部**跳转：浏览器地址栏不变，用户的观感是
"设备详情页中间那块变成了另一个页面"，且一旦跳走就没有返回路径。要跳就
`target="_blank"`（新标签页），或干脆别做跳转。

---

## 五、消息契约

| type | 载荷 | 宿主行为 |
|---|---|---|
| `iframe-height` | `height: number`（px，正整数） | 把 iframe 撑到这个高度 |
| `toast` | `message: string` | 用 `nz-message` 弹一条提示 |

宿主只接受**这一个 iframe** 发来的消息，其余一律丢弃：

```ts
const frame = this.frameRef()?.nativeElement;
if (!frame || e.source !== frame.contentWindow || e.origin !== this.frameOrigin) return;
```

* `e.source !== iframe.contentWindow`：只认这一个 iframe。**只校验 origin 是不够的** —— 同一个
  第三方域名下如果还嵌了别的窗口（或者宿主自己开了同源的页面），它也能改我们的布局。
* `e.origin`：域名被换掉/被顶替时兜底。

后续如果要加新消息（传参、通知宿主刷新、上报错误……），**沿用这张表往下加**，并同步更新本文件。

---

## 六、宿主侧实现

`src/app/pages/main/device/detail/device.detail.component.ts`：

```ts
/** 内嵌的第三方设备页面地址（自行开发、自行部署，宿主只负责嵌进来） */
const DEVICE_FRAME_SRC = 'http://127.0.0.1:8000/air-conditioner-tablet.html';

/** iframe 的高度：由第三方页面 postMessage 上报（跨域下宿主读不到它的文档，量不了） */
readonly frameHeight = signal(600); // 初始值只是首帧兜底，收到上报后立刻被覆盖

private readonly frameRef = viewChild<ElementRef<HTMLIFrameElement>>('deviceFrame');

@HostListener('window:message', ['$event'])
onFrameMessage(e: MessageEvent) {
  const frame = this.frameRef()?.nativeElement;
  if (!frame || e.source !== frame.contentWindow || e.origin !== this.frameOrigin) return;

  if (e.data?.type === 'iframe-height') {
    const height = Number(e.data.height);
    if (Number.isFinite(height) && height > 0) this.frameHeight.set(height);
  } else if (e.data?.type === 'toast' && typeof e.data.message === 'string') {
    this.msg.info(e.data.message);
  }
}
```

模板：

```html
<iframe
  #deviceFrame
  class="device-frame"
  [src]="frameSrc | safe"
  [style.height.px]="frameHeight()"
  width="100%">
</iframe>
```

* **刻意不加 `scrolling="no"`，也不加它的等价 CSS `overflow: hidden`。** 两者作用一样（都是把内部
  滚动条藏掉），但一旦第三方页面漏了高度上报，iframe 就停在初始高度，下面的内容**既看不见也滚不到** ——
  失败方式是静默丢内容。不设的话，那种页面退化成「自己有一条滚动条」：虽然正是要消灭的东西，
  但看得见、能滚、好排查。按契约写好的页面本来就没有内部滚动条，这个属性对它毫无贡献。
* `frameborder` 同样被 HTML5 列为过时属性，边框改在 `.less` 里用 CSS 关：
  ```less
  .device-frame { border: none; } // 光删 frameborder 不补这句，UA 样式表会补一条 2px inset 边框
  ```
* 顺带一提：`width` / `height` / `allow` / `loading` / `sandbox` 在 HTML5 里仍是合规属性，不会被 IDE 划线。
* `[src]="... | safe"`：`SafePipe`（`common/pipe/safe/SafePipe.ts`）绕开 Angular 对
  `iframe.src` 的 `SafeResourceUrl` 检查。
* 目前 `DEVICE_FRAME_SRC` 是**硬编码常量**：哪台设备用哪个第三方页面还没做映射。要有这张表，
  应当放进注册表（型号 → 地址），详情页按设备类型取 —— 与「设备界面子组件」那套注册表是同一类问题。

---

## 七、新增一个第三方设备页面

1. **起手**：照抄 `service/device/` 下的样例（六个页面，同名不同端：`*-tablet.html` / `*-mobile.html`）。
   页面是「单文件 + 内联样式脚本 + 内联产品实例数据」，方便直接丢给第三方改。
2. **加两段 JS**：第四节 1 的高度上报、第四节 3 的 toast 转发（照抄，别改消息名）。
3. **部署到 https 地址**（见第八节第 1 条，这条最容易在联调通过、上线翻车）。
4. **通知宿主**把「设备型号 → 页面地址」配上（目前还没做这张表，见第六节）。
5. **自测清单**：
   * 直接打开这个页面 → 布局正常、内部的本地 toast 还能弹（没被嵌入时的分支）；
   * 嵌进宿主 → 只有宿主一条滚动条；把宿主窗口拉窄，页面跟着变高变矮都不出内部滚动条；
   * 展开/折叠页内卡片 → iframe 高度跟着变，底部内容不会被裁；
   * 点一个会弹 toast 的按钮 → 提示出现在宿主页面里、看得见；
   * 宿主在设备之间来回切换 → iframe 重建后高度仍然正确。

---

## 八、坑（按踩到的概率排序）

1. **混合内容（http 套 https）**：宿主线上是 https，iframe 用 http 会被浏览器**直接拦掉**，
   连请求都不发、控制台只留一条 Mixed Content 警告。第三方页面的地址必须是 **https**。
   （本地 `http://127.0.0.1:8000` 能跑，是因为 localhost / 127.0.0.1 属于浏览器的安全上下文例外。）
2. **`100vh` 布局**：见第四节 2 —— 页面自己整屏了，撑高就没意义，内容会被裁。
3. **高度只上报一次**：见第四节 1。
4. **`position: fixed` 的元素**：toast、悬浮按钮、抽屉、遮罩层，全部会落到用户视口外。
5. **第三方页面自己调后端**：它在自己的源里发请求，属于跨域，需要后端 CORS；要带 cookie 的话
   还得 `SameSite=None; Secure`，否则浏览器拦掉。
6. **iframe 重建会丢状态**：宿主刷新、切换设备时 iframe 会重建，第三方页面里的所有状态归零。
   有状态就往 URL 上带（`?did=xxx&tab=2`）。
7. **传参**：目前宿主**不给第三方页面传任何参数**。要传设备 did / 产品实例，用 URL query 最省事，
   要传复杂对象再考虑 postMessage —— 定了之后补进第五节的表。
8. **安全**：宿主现在**没有**给 iframe 加 `sandbox`，也就是第三方页面拥有完整能力，包括
   `window.top.location = '...'` 把宿主页面整个跳走。自己人写的页面无所谓；一旦嵌的是**不完全可信**
   的页面，应该加 `sandbox="allow-scripts allow-same-origin allow-forms"` 并按需逐项放开。
   （注意 `allow-scripts` + `allow-same-origin` 同时给，等于没沙箱，需要想清楚。）
9. **全屏 / 摄像头 / 麦克风**：这类能力要 iframe 显式 `allow="fullscreen; camera; microphone"`，
   默认不给。
10. **打印与下载**：iframe 里的 `window.print()` 打印的是 iframe 内容（通常是想要的），
    但 `target="_blank"` 下载在部分浏览器会被拦。

---

## 九、替代方案（什么时候不该用这套）

| 方案 | 做法 | 代价 |
|---|---|---|
| **同源**（推荐给"自己人"写的页面） | 把第三方页面通过反代挂到宿主同一个域名下（nginx / dev-server 代理） | 宿主能直接读 `contentDocument` 量高度，第三方页面**一行代码都不用写**；但要动部署 |
| **反向方案** | iframe 撑满内容区（`height: calc(100vh - 页头)`），并把宿主的 `nz-content` 改成 `overflow: hidden` | 同样只有一个滚动条，但滚的是 **iframe 内部**那条，不是宿主的 —— 想让宿主滚就用不了；适合本来就是「整屏应用」的第三方页面 |
| **开新标签页** | 直接 `window.open(第三方地址)` | 没有任何跨域问题，但用户离开了设备详情页 |

---

## 十、参考实现

| 位置 | 内容 |
|---|---|
| `service/device/air-conditioner-tablet.html` 等 6 个文件 | 第三方页面样例（含高度上报与 toast 转发） |
| `webapp-matrix/src/app/pages/main/device/detail/device.detail.component.ts` | 宿主侧：`DEVICE_FRAME_SRC` / `frameHeight` / `onFrameMessage` |
| `webapp-matrix/src/app/common/pipe/safe/SafePipe.ts` | `iframe.src` 的 `SafeResourceUrl` 管道 |
| `webapp-matrix/src/app/pages/main/main.component.less` | 宿主的滚动容器（`nz-content { overflow: auto }`） |
