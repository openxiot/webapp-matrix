# 自定义数据看板（卡片式）设计方案

> 落点：`webapp-matrix` 的 `/main/dashboard`（已归位，见 §7.6 与修订（十一）） +
> `service-matrix` 新增 `/matrix/v1/dashboard/**`
>
> 目标：像 Grafana 一样，**用 JSON 描述一屏卡片，用户自己增删改，页面加载后按配置渲染**。
>
> **命名约定（重要）**：这个功能的领域概念是**自定义数据看板**，所以
> **前后端一律用 `dashboard`** —— 接口 `/matrix/v1/dashboard/**`、实体 `DashboardLayoutEntity` /
> `DashboardWidget`、集合 `dashboard-layouts`、类名 `DashboardResource` / `DashboardLayoutService` /
> `DashboardCodec` / `DashboardWidgetValidator`，前端镜像后端的 DTO 与 service 也跟着用 `dashboard`
> （`typedef/define/dashboard/`、`typedef/codec/dashboard/`、`service/dashboard.service.ts`）。
> **前端页面这一侧也已经统一到 `dashboard`**（2026-09-17 归位）：目录 `pages/main/dashboard/`、
> 类 `DashboardComponent`、路由 `/main/dashboard`、内部选择器 `main-dashboard` / `dashboard-*`、
> 纯函数文件 `dashboard.{grid,folding,charts,config}.ts`。本方案曾把页面留在 `home` 名下（因为它
> 只是临时落点），**所以本文档 2026-09-17 之前的各「修订（N）」小节里写的是 `home.*`** ——
> 那是当时的文件名，读到时按 `dashboard.*` 对应即可，不必逐条回改（见修订（十一））。
>
> 相关文档：
>
> - `service-matrix/API.md`：Matrix 现有接口与通用约定（本文档的接口一律对齐它）
> - `service-matrix/Modbus-History.md`：时序存储与降采样设计（曲线卡片的直接先例）
> - `webapp-docs/docs/spec/spec.mdx`：Device / Service / Property / Action / Event 的规范定义
> - `webapp-matrix/AGENTS.md`：前端编码规范（本文档的前端部分严格遵守）

---

## 1. 范围与结论摘要

**做什么**：在 `/main/dashboard` 实现一个用户可编排的看板。卡片配置存在后端（按项目/空间），
用户可创建、编辑、删除、排布；页面加载后一次取回布局与全部卡片数据并渲染。

**五种卡片**：单一统计、曲线图、数据分布、设备卡片、服务卡片。

**三条必须先拍板的结论**（详见 §3）：

| 结论 | 说明 |
| --- | --- |
| **曲线图只做「有历史数据的那部分」** | 告警曲线 + Modbus 服务字段曲线。**任意设备属性的历史曲线没有数据源**（Matrix 只存 Modbus 采集的历史），已确认本期不做（§3.1） |
| **取数走「批量渲染」接口，不是每张卡片一个请求** | 沿用 `StatisticsResource` 那套「为什么是一个聚合接口」的理由：N 张卡片打 N 个请求，既慢又会在清单接口的 `limit` 上被静默截断 |
| **服务卡片是「读」不是「调」** | 服务卡片取的是 `ModbusService` **轮询任务**已经采集到的字段值（影子 / 历史），**纯读、无副作用**。xiot-spec 的 `POST /actions` 方法调用**不在范围内** —— 它真的有副作用（可能启停流、写属性），不该挂在会定时刷新的看板上 |

---

## 2. 现状盘点：能复用什么

### 2.1 前端（`webapp-matrix`）

> **复用边界（重要）**：老那一屏固定看板（当时在 `src/app/pages/main/dashboard/**`）曾被当作
> 「待删的兄弟页面」，新页面不依赖它的任何代码 —— 它只作行为参照，**等价逻辑全部自建**。
> **它已于 2026-09-17 删除**（自建的纯函数落在 `dashboard.charts.ts` / `dashboard.folding.ts` /
> `dashboard.grid.ts` / `dashboard.config.ts`），这条边界现在是既成事实而不是约定。
>
> 真正可复用（属共享层，与老那一屏无耦合）：`common/**`、`typedef/**`、`service/**`。

| 可复用项 | 位置 | 说明 |
| --- | --- | --- |
| 页面壳 | `src/app/pages/main/dashboard/dashboard.component.ts` | 曾是一张空壳（顶替了老那一屏的同名类），现在是一屏完整的看板；挂路由 `/main/dashboard`（`main.routes.ts`，带 `authGuard`） |
| 图表指令 | `common/components/echarts/echarts.directive.ts` | `[appEcharts]="option"`，zoneless 兼容、ResizeObserver 自适应、销毁 dispose |
| **Modbus 字段曲线 option** | `pages/main/device/services/service/history/device.service.history.charts.ts:53` 的 `historyFieldOption` | **现成的跨页面复用模块**（定义处注释即写明「供 history 页与将来别的页面复用」，已被 `device.service.history.component.ts:52` 与 `history.services.component.ts:51` 两处 import）。carry-in、降采样（均值线 + min/max 虚线）、采集失败竖线、开关量阶梯线、单位轴、时间刻度粒度**全都处理好了**。**服务字段曲线卡片直接用它，不要重写** |
| 数据 define/codec | `typedef/define/statistics/OverviewStatistics.ts`、`typedef/define/modbus/ModbusHistory.ts`、`typedef/codec/modbus/ModbusHistoryCodec.ts`（含 `isBucket`） | agent 要的数据模型与判桶工具 |
| 服务层 | `service/statistics.service.ts`、`service/modbus.service.ts` | `overview` / Modbus 历史的取数封装 |
| 显示名拼装 | `typedef/define/modbus/Modbus.ts:152-172` 的 `modbusConfigLabel` | 「厂家 型号」的拼法与空 `configId` 口径 |
| 对话框范式 | `common/dialog/*` | `NzModalRef` + `NZ_MODAL_DATA`，`ok()`/`cancel()` 返回结果 |
| 拖拽 | `@angular/cdk/drag-drop`（已在用） | `modbus/editor/modbus.editor.ts:478-485` 用于行排序。**没有** gridster/gridstack 依赖 |
| 项目上下文 | `AccountService.space()` | `spaceId` 的唯一来源，localStorage `'spaceId'` 持久化；页面用 `effect` 监听它变化重载（非 dashboard 的先例很多：`device.component.ts`、`alarm.component.ts`、`project.component.ts` 等） |
| 权限口径 | `project.component.ts:171-187` 的 `isAdmin` computed | 「自己 user 条目 role=admin」或「组织条目 + 自己是组织 admin」 |

**关键依赖**：Angular 22（zoneless、signals）、ng-zorro 22、echarts 6、`@ngx-translate/core` 18、vitest。

### 2.2 后端（`service-matrix`）

| 可复用项 | 位置 | 说明 |
| --- | --- | --- |
| 资源基类 | `common/resource/AbstractResource.java` | `getUserId()` / `getAccountId()` / `currentUser()` |
| 资源写法 | `api/statistics/StatisticsResource.java` | `@Path` + `@RolesAllowed({OxRole.USER})` + `Uni<Response>` + `requireSpaceMember(spaceId)` + `OxResponse.ok/error` |
| 权限判定 | `service/orgcheck/OrgAccessService.java` | `checkSpaceMember` / `checkSpaceAdmin` / `checkMember` / `checkAdmin` |
| 统计聚合 | `api/statistics/StatisticsQueryService.java` | 设备/服务/告警/故障四类聚合 + 整点桶，**已是卡片的现成数据源** |
| 时序查询 | `api/modbus/history/ModbusHistoryQueryService.java` | 含降采样（`maxPoints`）、carry-in、按时间加权均值 |
| 实体范式 | `db/modbus/config/ModbusConfig.java` | `@MongoEntity` + `ReactivePanacheMongoEntity` + `Person creator/updater` + `visibility` + `lifecycle` |
| **异构字段落库** | `db/mongo/ObjectPropertyCodecProvider.java` | **声明成 `Object` / `Map<String,Object>` 的字段可直接落库**，无需新写 codec（见 §6.1） |

**库划分**：`matrix`（spaces、devices）、`modbus`（configs、services、histories、service-shadows、service-failures、service-alarms、polls）、`account`。

---

## 3. 数据源盘点：这是决定范围的一张表

五类卡片里，**四类今天就有数据源，曲线图有一半没有**。

| 卡片 | 能否做 | 数据源 |
| --- | --- | --- |
| 单一统计（设备总量、在线数、服务总量、今日告警、故障） | ✅ | `GET /matrix/v1/statistics/overview/{spaceId}` |
| 数据分布：设备类型 | ✅ | 同上 `devices.byType`（设备 URN 第 4 段） |
| 数据分布：服务类型 | ✅ | 同上 `services.byConfig`（按所配点表 id 分组） |
| 数据分布：告警类型 | ✅ | 同上 `alarms.byText`（按告警文本分组） |
| 数据分布：故障类型 | ✅ | `GET /matrix/v1/modbus/history/failures/{spaceId}` 的 `summary.byType` |
| 曲线：告警曲线 | ✅ | `overview.alarms.hourly`（整点桶，密集零填充） |
| 曲线：**Modbus 服务字段** | ✅ | `GET /matrix/v1/modbus/history/range/{spaceId}` |
| 设备卡片（当前值） | ✅ | `GET /matrix/v1/device/shadows/{spaceId}` 或 `/properties/{spaceId}` |
| 服务卡片（ModbusService 字段当前值） | ✅ | `GET /matrix/v1/modbus/history/current/{spaceId}/{serviceId}` → 影子的 `fields[字段名]` |
| 曲线：**任意设备属性** | ❌ | **无数据源**，已确认本期不做（§3.1） |
| 曲线：**任意服务属性** | ❌ | 同上 —— 但 **Modbus 服务字段有**（见上表「曲线：Modbus 服务字段」） |

### 3.1 「任意设备属性曲线」为什么没有数据源（已确认本期不做）

设备侧属性读写是 Matrix 校验权限后**转发给 `service-manipulation` 执行**的（`API.md` §1），
Matrix 自己不落地。而 Matrix 里唯一的历史时序是 **Modbus 采集**（`api/modbus/history/`），
落 `modbus` 库三张集合、90 天 TTL、只覆盖「轮询点表」这条链路。

普通设备的属性值现在**只有当前影子**（manipulation 侧的 `{pid, value}`），没有任何时刻序列。

三条路，代价差一个数量级：

| 方案 | 做法 | 代价 |
| --- | --- | --- |
| **A. 只做有历史的** | 曲线只支持告警曲线 + Modbus 服务字段曲线 | 零新存储 |
| B. 通用设备属性历史 | 订阅 manipulation 的属性变更通知 → 新集合 → TTL → 降采样查询接口 | ≈ 再做一个 `Modbus-History`（存储 + 保留 + 降采样 + 单测） |
| C. 按需落历史 | 卡片配置里选了哪些 PID，就只给那几个 PID 落库 | 中；但要处理「配置变更 → 订阅变更」的联动与旧数据归属 |

**已确认：走 A，B/C 本期不做。**

UI 后果：**曲线卡片的「数据源」下拉里不出现「设备属性」这一项**。这比「让用户选得到、
但选了永远是空图」诚实得多 —— 后者是这套设计里最该避免的体验。将来真要做，卡片 JSON 不用改
（`source` 是枚举，加一项即可）。

### 3.2 可以直接抄 `Modbus-History.md` 的四个坑

新曲线卡片如果在服务端做二次折算，下面四条是现成的教训，**别重新踩**：

1. **carry-in 点**：变化点存储下，区间查询必须额外带回 `from` 之前最近的一条，否则曲线从 `from` 起就是空白。
2. **服务端降采样**：`maxPoints` 缺省 500、封顶 2000；样本不超上限时返回原始点。
3. **均值按时间加权**，不能按记录条数平均 —— 变化点存储里「多少条」≠「持续多久」。且 `min ≤ avg ≤ max` 必须恒成立。
4. **不静默截断**：一次区间查询超 20000 条原始样本要报错，让用户缩小范围；截断会把曲线尾巴悄悄抹掉。

---

## 4. 核心设计决策

### D1 布局归属：项目级（空间级），不是用户级

用户明确说「后端维护**这个项目**的卡片配置」，所以布局挂在**空间**上，与空间的生命周期、
权限、`accesses` 完全同构 —— 这是最省事也最一致的选择：

- 复用 `OrgAccessService.checkSpaceMember/checkSpaceAdmin`，**不引入新权限模型**
- 布局不随用户换设备而丢，团队共享一屏
- 空间被删时布局自然失去意义，级联清理同 `ModbusHistoryService.purge` 的路子

**已确认（用户）：空间共享一份，不做个人覆盖。** 读写不对称 —— 读是空间成员，写是空间管理员
（§6.2）。「项目级默认 + 个人覆盖」是 Grafana 的完整模型（dashboard + 个人偏好），
会让实体与读取逻辑多一层回退；本期明确不做，将来真要做也只是在这层之上叠加，不影响现在的 schema。

### D2 存储形态：整个布局一个文档，widgets 内嵌

**不拆 widget 表**。理由：

- 用户一次「拖一下位置」，改的是**多张卡的坐标**：被压住的往下让位（§D4），
  分表存要在一个事务里改 N 行，还解决不了并发下的中间态
- 一屏卡片量级是几十，远够不上 Mongo 单文档 16MB 的边
- 读布局永远整体读，没有「只查某一个 widget」的场景

因此：**一次读回整个布局，一次整体保存**，配 `version` 字段做乐观锁。

**widgets 的异构 `config` 用 `Map<String, Object>` 存**。这是本方案最省力的一个点：
`db/mongo/ObjectPropertyCodecProvider.java` 已经把「声明类型是 `Object`」这一格的 codec 兜底装好了
（`MapPropertyCodecProvider` 对值类型为 `Object` 的 Map 本来也有兜底），
所以**不需要为每种 widget 写 codec**，也不需要把 5 种 config 塞进一个大 POJO。

### D3 取数：批量渲染接口，一次请求喂满一屏

前端**不**为每张卡片发一个请求，改为：

```
POST /matrix/v1/dashboard/{spaceId}/render
```

服务端按 widgets 的 `type` + `config` **编排现有的查询服务**，同源的去重：

| widget | 服务端实际做的事 |
| --- | --- |
| 若干 `stat` + 若干 `distribution` + 告警曲线 | **只调一次** `StatisticsQueryService.overview(spaceId, from, to)`，各卡片从同一份结果取自己的字段 |
| 多条 Modbus 曲线 | 按 `(serviceId, functionIndex)` 归并，同字段的合并；每条走 `ModbusHistoryQueryService` |
| 设备卡片 | 按 `did` 归并成一次 `GET /shadows`（manipulation 的 `did` 参数本身可重复） |
| 服务卡片 | 读 Modbus 影子（`/modbus/history/current`），与同服务的字段曲线**同源**，能合并的合并 |

**窗口统一到一个最大窗口**：`overview` 的窗口由调用方算，所以同一屏里所有依赖它的卡片
（`stat` 的今日告警、告警曲线、告警分布）共用一个 `[from, to]` —— 取「本屏所有这类卡片所需窗口的最大值」。
今日告警从 `hourly` 里按本地 00:00 之后求和（桶起点正好等于 00:00 的那一桶算今天），
窗口恒被 24 小时盖住，所以**不必为「今日」多发一次请求**。

**单卡片错误隔离**：一张设备卡片查不到，不能让整屏白掉。响应里每个 widget 各自带成功/失败：

```json
{ "success": true,
  "data": {
    "asOf": 1757919600000,
    "widgets": {
      "w_7f3a": { "success": true,  "data": { "value": 12 } },
      "w_9c21": { "success": false, "message": "No access to this device" }
    }
  } }
```

### D4 布局：24 列 × 无限行网格 + 固定尺寸档位 + 自由摆放

- **24 列、高无限**：屏幕当成「宽 24 格、高无限」的网格。列数与 ng-zorro 栅格同基数，
  也与 Grafana 一致；行是**可拼接的单位** —— 这是修订（十二）加的，见下。
- **固定尺寸档位**（用户说的「几个固定的尺寸」），每档给一个 `w × h`（网格单位）。
  档位名是 **`W{占几列}H{多少像素}`**（修订（十四）换的，理由见下）：

  | 档位 | w（列） | h（行） | 像素高 | 典型用途 |
  | --- | --- | --- | --- | --- |
  | `W6H92` | 6 | 1 | 92 | 统计数字（统计卡专用） |
  | `W12H92` | 12 | 1 | 92 | 统计数字（统计卡专用） |
  | `W6H200` | 6 | 2 | 200 | 单值、小分布图 |
  | `W12H200` | 12 | 2 | 200 | 单曲线、分布图 |
  | `W24H200` | 24 | 2 | 200 | 整宽横幅（一排读数、一行状态） |
  | `W6H308` | 6 | 3 | 308 | 窄而高：饼图、三张统计卡那一竖条 |
  | `W12H308` | 12 | 3 | 308 | 三行的曲线 |
  | `W24H308` | 24 | 3 | 308 | 整宽的三行 |
  | `W12H416` | 12 | 4 | 416 | 双高曲线 |
  | `W24H416` | 24 | 4 | 416 | 整宽大图 |

  **名字说的就是这一档的两件事**：`W6H200` 读作「6 列宽、200 像素高」。
  用**像素**而不是行数命名，是因为行高改过一次（38 → 92，每档的行数都跟着减半）而像素高
  从头到尾没变 —— 名字里写行数的话，那一次改动就得迁移库里每一个文档。

  **有几张卡同宽、只有高度不同**（`W12H200` / `W12H308` / `W12H416`）是故意的：「宽图」和
  「高图」是两种不同的需求，用同一宽度区分高度比引入更多宽度更好记。
  也有**故意不存在的组合**：6 宽没有 416（那样的图太窄太高，不成形状，而竖着叠两张矮卡
  就能拼出同样的高度），24 宽没有 92 那一档（整宽的一行高更像一条分隔线，不是一张卡）。
  6 宽的 308 是修订（十六）补的：它正好是「三张统计卡竖着叠起来」那一条
  （`92 + 16 + 92 + 16 + 92`），窄饼图也放得下 —— 原先「6 宽没有 308」那条理由
  （太窄太高、拿矮卡摞就行）在试过之后被用户否掉了。

  **统计卡只给两个一行高的档位**（`W6H92` / `W12H92`，见 `sizeAllowed`），因为它是唯一
  「没有卡头、按内容自然高 ≈ 90px」的卡片 —— 塞在 200px 的档位里下面会空 110px；
  反过来别的卡片一行都太矮（光卡头就要占掉一行的一多半）。这条界线写成「恰好一行」与
  「一行以上」的**判定**而不是一份手抄的档位名单：加新档位时它不用改，
  更不会出现「加了档位却忘了加到某个类型的名单里」那种静默的漏。

  > **真正的常量只有两个：行高 92、间距 16。** 卡片的**像素宽度不是常量** ——
  > 它取决于容器宽度（视口 − 侧栏 − 内边距），所以这张表**不写像素宽**。
  > 需要估算时按 `列宽 = (容器宽 − 23×16) / 24`、`宽 = w×列宽 + (w−1)×16`、
  > `高 = h×92 + (h−1)×16` 自己算：以内容区 1400px 为例，列宽 ≈ 43px，
  > 于是 `W6H92 ≈ 338×92`、`W12H200 ≈ 692×200`、`W6H308 ≈ 338×308`、
  > `W12H416 ≈ 692×416`、`W24H416 = 1400×416`。
  >
  > 尺寸档位是**常量表**，放一个文件里，觉得不合适改数字即可，不动 schema。

  > **行高为什么是 92 而不是 90**（修订（十二）用户定的口径）：`h × 92 + (h−1) × 16`
  > 让**改造前每一档的像素高度一像素不差** —— 1 行 = 92、2 行 = 200（原 `S` / `M` 就是它）、
  > 4 行 = 416（原 `L` / `XL`）。这是一个**等差数列**（步长 108 = 一个行高 + 一道缝）：
  > `92 + 16 + 92 = 200`、`200 + 16 + 92 = 308`、`308 + 16 + 92 = 416` ——
  > 矮卡竖着叠起来正好等于高一档，底边能对齐。308 这一档（修订（十四）补的）就是这条梯子上
  > 缺的那一级：`3×92 + 2×16`，也等于「一张 200 + 一道缝 + 一张 92」。
  > 按字面的 90 / 20 就不行：间距一动，24 列的列宽会从约 43px 缩到约 39px，
  > **所有卡片的宽度跟着变**。

- **位置存 `x` / `y` 两个平铺的整数**（修订（十二）），渲染用 CSS Grid
  （`grid-column: x+1 / span w`、`grid-row: y+1 / span h`）。不用 `nz-row/nz-col`：
  nz 是**流式**布局，表达不了自由坐标。
  **`w` / `h` 不上行** —— 见 §5.2 那条说明：它由 `size` 档位在前端算，存下来就是第二份会过期的真值。
- **拖动时把挡路的推开，让完就停**（修订（十三）去掉了上吸：位置是用户摆的，不能自己跑；
  横向留白同样保留）。这是 `dashboard.grid.ts` 里几条纯函数的活，配 vitest 单测：
  `flowPlace`（按数组顺序贪婪铺，复刻改造前的流式排布）、`placeAt`（移动 + 推开，**不再上吸**）、
  `compact`（只管上吸，今天只有「删掉一张卡」一处调用）、`findSlot`（加卡片时扫第一个空位）、
  `fitsAt`（落点在不在界内 —— 拖动中的「可以放置 / 不可以放置」判的就是它）。
  - **被拖的那张卡是钉住的**：它落在用户放的那一格，让位只动别人。
  - **落点越界 = 不可以放置**：越出 24 列或拖到板子上方时幽灵卡染红、松手弹回原位（§7.3）。
    压在别人身上**仍算可以放置** —— 被压的往下让，否则纵向叠卡永远做不到。
  - 三条不变量都写成用例：`compact` **幂等**（输出一律按吸完之后的 `(y, x)` 重排 ——
    修订（四）在这里踩过「每保存一次布局就自己重排一次」的坑）；任何一次操作后**不重叠、不越界**；
    `flowPlace` 复刻今天的流式排版（这是存量布局长相不变的保证，见修订（十二）第 7 节）。

> **拖拽已在修订（十二）落地**（§8 的 P3 里那一项因此划掉，只剩缩放与撤销/重做）：这里曾经写
> 「待确认：编辑器要不要真·拖拽」，后来是修订（六）的「上下移按钮」→ 修订（十）的「拖拽排序」→
> 修订（十二）的「二维自由摆放」。做法见 §7.3。

### D5 卡片的「原始值」与「展示值」分层

服务端**只返回原始值**（统计数字、设备影子的 `{pid, value}`、Modbus 影子的 `fields[字段名]`），
展示层的格式化留在前端：

- 枚举属性的 `value-list` → 中文描述
- 单位（`unit`）、精度、布尔的真假文案
- 时间标签（整点桶的坐标轴标签按本地整点，如 `HH:00`；服务字段曲线的时间刻度由
  `historyFieldOption` 的 `axisTime()` 按窗口跨度自己选粒度）

理由是 `AGENTS.md:60-69` 的硬规则：**可翻译的是页面文案，服务端/用户数据原样显示**。
把「值的解释」放在前端，才能保证同一份数据在不同语言下都对。

**Modbus 侧这个问题已经解决**：`ModbusServiceField` 自带 `format` / `unit` / `valueList` / `bitList`，
服务卡片和 Modbus 曲线卡片的展示元数据直接读服务定义就有（§5.4）。

> **只剩设备卡片待确认**：前端能否拿到任意 PID 的 `format` / `unit` / `value-list`？
> 这取决于 `@openxiot/xiot-core-spec-ts` 与 `DeviceDisplayService` 当前的覆盖范围。
> 如果拿不到，退路是服务端在设备卡片数据里一并返回这些展示元数据 —— 那就把「解释」挪回了后端，
> 口径要单独定（这也是 §10 的问题 4 之外，设备卡片唯一还没落定的地方）。

### D6 服务卡片：读轮询结果，不调方法

服务卡片指的是 **`ModbusService` 的 function 调用**：轮询任务（`ModbusPoll`）按 `interval`
周期性地按功能码读一段，解析出的字段值落在 `ModbusServiceShadow.fields`（每个字段一条影子，
带 `recordedAt`）。所以服务卡片就是**读这份已经采集好的值**：

```
ModbusService{functions:[{index, interval, polling, request, response}]}
      ↓ 轮询采集
ModbusServiceFunction → ModbusServiceField{index, field, format, unit, valueList, ...}
      ↓ 取值
ModbusServiceShadow{fields:{字段名→值}, recordedAt, lastError, errorAt}
```

**这是纯读，没有任何副作用** —— 看板可以放心地按工具条上那个间隔（§7.2）定时刷新它。

数据来源二选一：

| 用途 | 接口 | 取什么 |
| --- | --- | --- |
| 当前值（服务卡片） | `GET /modbus/history/current/{spaceId}/{serviceId}` | 影子的 `fields[字段名]`，加 `recordedAt` / `lastError` |
| 变化曲线（曲线卡片） | `GET /modbus/history/range/{spaceId}` | 该字段的历史样本 |

**明确不做：xiot-spec 的 `POST /device/actions` 方法调用。** 那个接口会**真的执行方法**
（示例里有「开启摄像头视频流」「写 SRTP 配置」这类），把它挂在会定时刷新的看板上，
等于给设备装了一个定时操作器。用户已明确服务卡片指的是 ModbusService 的轮询字段，
所以这条路径整个不进 schema ——**没有 `readMode`，没有入参编辑，没有副作用确认**。
这是本方案相对「通用方法调用卡片」省掉的相当大一块复杂度（入参表单引擎、方法白名单、
副作用警告 UI 全都不需要了）。

### D7 服务卡片天然有「陈旧提示」的数据，设备卡片没有

`ModbusServiceShadow` 带 `recordedAt`（最后一次成功落库时间）与 `lastError` / `errorAt`，
所以**服务卡片可以显示「最后已知值 + 它有多旧 / 是否在故障中」**。
`Modbus-History.md` 特意强调过这条：「前端得能显示『最后已知值 + 它从哪一刻起不再更新』，
而不是把陈旧值当最新值画」。服务卡片直接继承这个能力。

而 `GET /device/shadows` 只返回 `{pid, value}`，**没有时间戳** —— 设备卡片拿不到陈旧信息（§5.6）。

### D8 卡片标题是用户数据，绝不翻译

用户在编辑器里填的标题（「东区温度」「3 号线能耗」）是**用户数据**，
按 `AGENTS.md:60-69` 必须原样显示，不能进词典、不能走 `| translate`。

页面**自己的**文案（编辑器标签、「添加卡片」「保存布局」「选择设备」等）才走 i18n，
键即中文原文，追加到 `public/i18n/*.json` —— 注意 **66 个词典键集合与顺序必须完全一致**。

---

## 5. 卡片 JSON Schema

### 5.1 布局文档

这就是 `GET|PUT /layout/{spaceId}` 的报文形状，也是后端 `DashboardLayoutDto` 与前端
`DashboardLayout` 的字段来源 —— 三者同名同形，一个字段都没改过（前后端命名统一到 `dashboard`
是 2026-09-17 的事，见修订（十一）；那之前前端页面叫 `home`，但这份报文里从来没有 `home` 字样）。

```json
{
  "spaceId": "66f2...0001",
  "version": 7,
  "widgets": [ /* 见 5.2 */ ],
  "creator": { "id": "...", "name": "...", "timestamp": 1758096000000 },
  "updater": { "id": "...", "name": "...", "timestamp": 1758124800000 }
}
```

> **`spaceId` 是根空间（项目）**，不是路径上的任意空间：布局「一个项目一份」，
> 服务层用 `space.rootSpaceId()` 归一到根空间再读写（见 §6.5）。
> `creator` / `updater` 用既有的 `Person`（自带 `timestamp`），**不另设 `createTime` / `updateTime`**
> —— 两个时间点与两个人一一对应，多两个字段只会多两种不一致的写法。
> 预置布局（未落库）没有 `creator` / `updater`，这两个键**整个不出现**，不是 `null`。

### 5.2 widget 通用字段

```json
{
  "id": "w_7f3a2b",
  "type": "stat",
  "title": "东区温度",
  "titleKey": "设备总量",
  "size": "W6H92",
  "x": 6,
  "y": 4,
  "config": { }
}
```

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 是 | 客户端生成（`w_` + 随机），文档内唯一，用于 render 结果对号入座 |
| `type` | 是 | `stat` / `line` / `distribution` / `device` / `service` |
| `title` | 否 | **用户填的原文，原样显示，不翻译**（D8） |
| `titleKey` | 否 | **预置标题的 i18n key，前端翻译**；只有预置布局会填。取值顺序 `title` → `titleKey` → 按 `type` 的默认名 |
| `size` | 是 | 档位名 `W{列}H{像素}`（§D4 常量表，**表在前端**），决定占几列几行。改造前的 `S1` / `M1` / `S` / `M` / `L` / `XL` 仍然读得出来（翻译成占格相同的新名，见修订（十四）） |
| `x` | 否 | 起始列，`0..23`。**与 `y` 同生同灭** |
| `y` | 否 | 起始行，`0` 起、上不封顶 |
| `config` | 是 | 按 `type` 不同，见下 |

> **自动刷新不在卡片上**（修订十七）：改造前每张卡各带一个 `refresh`（秒，缺省 60），
> 现在整屏一个间隔、放在工具条上（§7.2），**不落库**、存在浏览器里。老文档里的 `refresh`
> 属于「不认识的键」，读得进、不报错，过一趟前端保存就自然没了 —— 与更早那个嵌套 `layout` 同待遇。

> **位置是两个坐标**（修订十二）：`x`（列）/ `y`（行），屏幕当成「宽 24 格、高无限」的网格，
> 卡片二维自由摆放。占几列几行（`w` / `h`）仍由 `size` 档位在前端算，**一个字节都不上行** ——
> 服务端拿不出第二个人来算它，存下来就是第二份会过期的真值。
>
> 更早的形态换过两轮：修订（四）是 `layout: {x, y, w, h}` 一个嵌套对象（连 `w`/`h` 一起存，
> 服务端因此持有排版知识），修订（十）把它整个删掉改成「顺序即位置」（数组下标就是屏幕位置），
> 修订（十二）又把坐标请回来了 —— 但只回 `x`/`y` 两个平铺的整数，理由见 §D4。
>
> **两个坐标都是可选的**，缺了就是**老文档**（改造前的卡片没有坐标）：前端 `decode` 时按数组
> 顺序 `flowPlace` 整份重铺一遍补齐，铺出来的就是改造前浏览器流式排开的那个样子，
> 所以**存量布局打开后长相完全不变**，不需要迁移脚本（修订（十二）第 7 节）。每次落定后数组按 `(y, x)` 重排一次
> 让「数组顺序」与「坐标」保持一致，但**数组顺序不是位置的真值**，坐标才是。

> **从老文档到 `x`/`y` 的路只有一条**：`decode` 时整份重铺一次，第一次保存就把坐标写进库。
> 反过来说，**没有坐标的卡片在保存前一个坐标键都不发** —— 发半个（只发 `x`）比全不发更糟。

> **为什么必须有 `titleKey`**（`title` 与 `titleKey` 只出现一个就够，两个都给时 `title` 优先）：
> 预置布局是**服务端**造的，而 `title` 按 `AGENTS.md` 属于用户数据、前端**不许翻译**。
> 若把「设备总量」直接写进 `title`，英文界面就会看到中文；若留空让前端按 `type` 给默认名，
> 预置的那 4 张统计卡就会同名（都叫「统计」）。两者都不行，所以给预置布局一个
> **我们自己的文案 key**：是页面文案，就该翻译。

### 5.3 `stat` 单一统计

```json
{ "type": "stat",
  "config": { "metric": "devices.total", "subMetric": "devices.online" } }
```

`metric` 取值（全部来自 `overview`，无需新接口）：

| metric | 含义 | 需要窗口 |
| --- | --- | --- |
| `devices.total` / `devices.online` | 设备总量 / 在线数 | 否 |
| `services.total` | 服务总量 | 否 |
| `alarms.today` | 今日告警（`hourly` 里本地 00:00 之后求和） | 是（恒被 24h 盖住） |
| `alarms.window` | 窗口内告警总数 | 是 |
| `failures.total` | 故障种类数（注意：是「几种」不是「几次」，见 `API.md` §8.1） | 是 |

`subMetric` 可选，用于「设备总量」这张卡上再挂一个次要数字（如右下角「在线 9」）。
它与 `metric` **共用同一个 `window`**，所以「任一方需要窗口就必填」——只校验 `metric` 的话，
`metric: devices.total` + `subMetric: alarms.today` 会漏掉窗口、render 时退回兜底窗口，
前端把 1 小时当成「今日」求和：数字看着像真的，但是错的。

> `failures.total` 的口径要在编辑器里写清楚，否则用户一定会把它理解成「失败次数」。

### 5.4 `line` 曲线图

```json
{ "type": "line",
  "config": {
    "source": "alarmCount",
    "window": { "kind": "last", "hours": 24 },
    "bucket": "hour"
  } }
```

`source` 二选一：

```json
// 1) 告警曲线 —— 复用 overview.alarms.hourly
{ "source": "alarmCount",
  "window": { "kind": "last", "hours": 24 },
  "bucket": "hour" }

// 2) Modbus 服务字段曲线 —— 复用 /modbus/history/range
{ "source": "serviceField",
  "serviceId": "65f0...",
  "functionIndex": 1,
  "field": "温度",
  "window": { "kind": "last", "hours": 24 },
  "maxPoints": 500,
  "showFailureShadow": true }
```

`window` 两种形态：`last`（最近 N 小时/天，**由前端算成 epoch 毫秒**）或
`range`（绝对区间 `{from, to}`）。

**`bucket` 只对 `alarmCount` 生效**（后端整点桶）；`serviceField` 的粒度由原始采样与降采样决定，
不设 `bucket` —— 沿用 `ModbusHistoryWindow` 的既有口径（`downsampled` / 桶的 `first,last,min,max,avg`）。

**`showFailureShadow`**：在曲线上标出采集失败的时刻。数据取 `GET /modbus/history/failures/{spaceId}`
的 `items`（按时间倒序）—— `Modbus-History.md` 里写明这是「历史区间接口的同批产出」。

> 名字保留 `shadow`，但**实际画法是竖虚线（`markLine`）不是阴影区**：
> 失败是「某一刻没采到」，不是一个持续取值的时段。`historyFieldOption` 里的
> `failureMarkLine()` 已经是这个口径（`silent`、无标签、红色虚线），不要改成 `markArea`。

**渲染直接复用 `historyFieldOption`**（§2.1）——它已经实现了本卡片需要的全部形态：

| 本卡片的需求 | `historyFieldOption` 里对应的实现 |
| --- | --- |
| carry-in 点 | 把 `range.carryIn` 补在 `from` 那一刻，曲线不从窗口内第一条采样才开始 |
| 降采样展示 | 均值线 + `downsampled` 时叠 min/max 两条同色细虚线（「这一段是这么走的」） |
| 采集失败 | `series.failures` → `markLine` 竖虚线 |
| 开关量阶梯线 | `series.step` → `step: 'end'`（直连会画出根本不存在的中间值） |
| 单位 | `series.unit` → y 轴 `name` |
| 非数值字段 | 跳过、**留成断点**（不画错，也不报错） |
| 时间刻度粒度 | `axisTime()` 按窗口跨度选时分秒 / 时分 / 月日时分 |

所以 看板侧只需要一个**适配器**：把卡片 `config` + render 回来的数据拼成
`HistoryFieldSeries` / `HistoryChartContext` 两个入参。**没有第二份曲线实现。**

**校验**（与上表最后一行的既有行为对齐，不要更严）：字段是否画得出来由数据决定，
`historyFieldOption` 对非数值一律留断点。所以服务端要拦的是**真正非法**的配置：

- `(serviceId, functionIndex, field)` 三元组必须真实存在（保存时就报错）
- 曲线卡片**只画一个 `field`**（与 §5.7 服务卡片的 `fields` 数组不同）。history 页支持勾多个
  字段画多条线，这里先做单条 —— 多字段曲线留到 P3，schema 上加个 `fields` 即可，不破坏现有配置
- `string` 格式的字段**允许保存但编辑器给出提示**（「该字段为非数值，曲线上会是断点」），
  而不是拒绝 —— `Modbus-History.md` 记录过，命中 `valueList` 的字段在影子与历史里存的
  是 `description` 字符串而非数值，这类字段在 history 页是**可以选的**，看板侧保持同口径，
  免得同一个字段在两个页面一个能选一个不能选
- 窗口 ≤ 31 天；`maxPoints` 缺省 500、封顶 2000

> **元数据是现成的**：`ModbusServiceField` 自带 `format` / `unit` / `valueList` / `bitList` / `scale`，
> 所以编辑器的「选服务 → 选 function → 选字段」三级级联可以直接读服务定义，
> 选中字段时就知道单位是什么、枚举值怎么显示、要不要走阶梯线 —— 不需要额外的元数据接口。
> 这也回答了 §D5 里「前端能不能拿到展示元数据」的疑问（**Modbus 侧能**）。

### 5.5 `distribution` 数据分布

```json
{ "type": "distribution",
  "config": {
    "dimension": "alarmType",
    "window": { "kind": "last", "hours": 24 },
    "chart": "pie",
    "limit": 8
  } }
```

| `dimension` | 含义 | 是否需要窗口 |
| --- | --- | --- |
| `deviceType` | 设备类型（URN 第 4 段） | 否 |
| `serviceType` | 服务类型（按所配点表，前端解成「厂家 型号」并合并同名） | 否 |
| `alarmType` | 告警类型（按告警文本） | **是（用户明确要求）** |
| `failureType` | 故障类型 | 是 |

- 后端返回**全量分组**（已按条数降序、同数按名称升序排好），`limit` 由**前端**截断显示为「前 N + 其他」。
  这样切 limit 不用重新请求，也不会有「后端截断了但卡片看不出来」的老问题。
- `chart`：`pie`（默认，环形）或 `bar`。**选 `bar` 需要在 `echarts.directive.ts:15-24` 补注册 `BarChart`**
  —— 那个 `use([...])` 是按需注册，不加就画不出来。
  在补注册之前（P3），**校验器只接受 `pie`**，提交 `bar` 会被拒：用户配了柱状图却看到饼图，
  比保存时报一句错更难查。`display: gauge` / `boolean` 同理（见 §5.6）。

### 5.6 `device` 设备卡片

```json
{ "type": "device",
  "config": {
    "did": "abc",
    "pid": "abc.1.1",
    "display": "value",
    "showUnit": true
  } }
```

- `display`：`value`（大字） / `gauge`（仪表盘，需新注册 `GaugeChart`） / `boolean`（开关态）
- **`showStale` 本期不做**（用户确认）。原因不是「懒得做」，而是 `GET /shadows` 只返回
  `{pid, value}`，**没有时间戳** —— 没有最后更新时刻，所谓「陈旧提示」无从判断。
  要做得先去 manipulation 侧补这个字段，本期不动。
  **绝不把 render 的 `asOf` 冒充成属性的更新时刻** —— 那会让人以为数据是新的，比不显示更糟。
  服务卡片不受此限（Modbus 影子有 `recordedAt`，见 §D7）。

### 5.7 `service` 服务卡片（ModbusService 字段当前值）

```json
{ "type": "service",
  "config": {
    "serviceId": "65f0...",
    "functionIndex": 1,
    "fields": ["温度"],
    "display": "value",
    "showUnit": true,
    "showStale": true
  } }
```

**卡片的样子（用户确认）**：标题是服务名，正文一行一个字段：

```
[服务名称]
温度: 23.5 ℃
```

- **标题**默认取服务名（`ModbusService.name`），编辑器里可改；改过就显示改后的。
- `fields` 是**数组**而不是单值：一次 function 调用返回该 function 的**全部**字段，
  所以多显示几个字段**不增加任何后端成本**（§D3 按 `(serviceId, functionIndex)` 归并，
  同 function 只查一次）。编辑器默认选一个，用户想多显示就多选，每多一个多一行 `字段: 数值`。
- **约束：同一张卡片的字段必须来自同一个 function**，所以 `functionIndex` 与 `fields` 是同级字段。
  理由是 `recordedAt` / `lastError` 属于**整个 function**，不是每个字段各自的 ——
  混两个 function 的话，卡片上的时间戳对其中一行必然不成立。
- `display`：`value`（默认，就是上面的 `字段: 数值`）。`gauge` 仪表盘延到 P3
  （要在 `echarts.directive.ts:15-24` 注册 `GaugeChart`）。
  **没有 `bitList` 这个模式** —— 见下方「位区字段」。
- `showUnit`：显示 `ModbusServiceField.unit`。值本身**已经是 `scale` 缩放后的**（历史里存的就是
  缩放后的 `double`），前端不要再乘一次 `scale`
- `showStale`：显示「最后已知值 + 它有多旧」（用 `recordedAt`），以及最后一次采集是否失败
  （`lastError` / `errorAt`）

**点位身份是 `(serviceId, functionIndex, field)` 三元组**，卡片照此寻址 —— 不拼 `pointId` 字符串。
这是 `Modbus-History.md` 明确过的口径：「字段名是用户填的（可能是中文，可能带任何分隔符），
拼串做前缀匹配迟早会撞上转义问题」。所以配置里是三个字段，不是一个大 id。

#### 位区字段（`bitList`）在卡片上怎么显示

**结论：不需要任何新排布，因为在这套数据模型里「位」本来就是独立点位。**

- `ModbusServiceField.bitList`（线上 `bit-list`，元素 `{offset, field}`）的语义是
  **「01/02 位区逐位取值」**。定义处的注释说得最清楚（`ModbusService.ts:86-87`）：

  > 位区字段里的具名位：除字段自身那份**整段位掩码**外，把该位单独作为一个 **0/1 取值**输出（key 即 `field`）。

  也就是一次调用同时产出两样东西：**整段位掩码**（父字段本身，一个数值）和
  **每个具名位的 0/1**（键是 `bit.field`，即位的名字）。另见 `MODBUS.md:557`。
- 两者**各自有独立的轮询值与历史**：`device.service.history.component.ts:247-258` 里，
  每个位都被展开成一个独立的 `FieldRef`（`step: true`，`unit: ''`），与父字段并列出现在字段列表里。
  轮询身份三元组里的 `field` 就是 `bit.field`。
- 所以**服务卡片的字段下拉里，位与父字段是同一层的平级选项**：
  `状态字`（整段掩码）、`进水阀`（0/1）、`出水阀`（0/1）…… 用户在级联里选哪个就显示哪个，
  **永远只有一行 `字段: 数值`**，不存在「一组小灯怎么排」的问题。
  编辑器应沿用 history 页那份展开逻辑（父字段 + 逐位），别在卡片侧另造一套。

> 顺带**删掉了原方案里的 `display: "bitList"`**（「把字段展开成一组 0/1 小灯」）：
> 既然位是独立点位，用户想显示三个位就选三个位、排三行，不需要一种把字段偷偷展开的展示模式 ——
> 那种模式还会让人搞不清卡片到底订了几个点位。

**位的值按「开/关」显示，不显示 0/1**：`0` / `1` 对运维没有可读性。
这是 §D5 分层里「前端负责布尔的真假文案」的一例，判断依据与曲线卡片的 `step: true` 同源
（见 §5.4 的阶梯线一行）。

**服务卡片必须能区分三种"没有正常值"的状态** —— 这是 `Modbus-History.md` 特意强调过的一条：

| 状态 | 数据特征 | 卡片应显示 |
| --- | --- | --- |
| **从没采集过** | 影子里没有这个 function / 这个字段 | 空态：「尚未采集」（不是 0，也不是故障） |
| **有值但很旧** | `fields` 有值，`recordedAt` 距今很远 | 值 + 陈旧提示（灰色 / 时间角标） |
| **采集失败** | `lastError` 非空，`errorAt` 是故障首现时刻 | 最后已知值 + 故障标识（`errorAt` 到下次成功的间隔就是故障持续时长） |

原文的说法是：**「没有影子的方法不出现在结果里（那是『从没调用过』，与『有值但很旧』不是一回事）」**。
把这三态混成一个「没有数据」，运维就失去了唯一能看出点表配错的线索。

---

## 6. 后端设计

### 6.1 实体

新库位沿用 `matrix`（与空间同库，空间级数据）：

```java
// src/main/java/cc/openxiot/matrix/db/dashboard/DashboardLayoutEntity.java
@MongoEntity(database = "matrix", collection = "dashboard-layouts")
@RegisterForReflection
@Schema(description = "自定义数据看板布局（一个空间一份）")
public class DashboardLayoutEntity extends ReactivePanacheMongoEntity {

    @Schema(description = "所属空间 ID", required = true)
    public String spaceId;

    @Schema(description = "卡片列表。数组顺序是阅读顺序，位置的真值是每张卡自己的 x / y；服务端不排序、不补位",
            required = true)
    public List<DashboardWidget> widgets = new ArrayList<>();

    @Schema(description = "乐观锁版本号，每次保存 +1", required = true)
    public long version;

    @Schema(description = "创建者/创建时间")
    public Person creator;

    @Schema(description = "最后更新者/更新时间")
    public Person updater;
}
```

```java
// src/main/java/cc/openxiot/matrix/db/dashboard/DashboardWidget.java
@RegisterForReflection
public class DashboardWidget {
    public String id;
    public String type;          // stat | line | distribution | device | service
    public String title;         // 用户数据，原样下发
    public String size;          // W6H200 这类档位名（W{列}H{像素}），占格表在前端
    public Integer x;            // 起始列 0..23；null = 老文档（由前端铺一遍）
    public Integer y;            // 起始行 0 起；与 x 同生同灭
    public Map<String, Object> config = new LinkedHashMap<>();   // 异构，靠现有 codec 兜底
}
```

**`x` / `y` 用 `Integer` 而不是 `int`**：**「没有这个键」要能与 `0` 区分开** ——
老布局没有坐标，`int` 会默认成 0，把「没摆过」读成「摆在左上角」。

老那一版卡片上还有个**嵌套的** `WidgetLayout layout`（`{x, y, w, h}` 一个对象），修订（十）
连同 `WidgetLayout` 类一起删了；修订（十二）请回来的是**平铺的两个整数**，不是那个对象。
**两者别搞混**：库里的老文档仍然带着那个 `layout` 对象，它是**不认识**的键，照旧被跳过
（`DashboardWidget` 挂着 `@JsonIgnoreProperties(ignoreUnknown = true)`，POJO codec 遇到类上
没有的键**跳过**而不是报错），所以老布局读得出来、不需要迁移脚本，第一次保存就把那个键洗掉；
而平铺的 `x` / `y` 现在**是认识的键**。
`ObjectPropertyCodecProviderTest.legacyLayoutKeyIsIgnored` 钉着前者，
`dashboardWidgetConfigRoundTrips` 钉着后者。

`WidgetData` / `WidgetDataService` 不带 `Dashboard` 前缀：它们表达的是「卡片自己的形状与数据」，
已经被 `db/dashboard/` 这个包名限定住，再加前缀只会让 `DashboardWidget.widgetData` 这类读起来更啰嗦。

**`config` 用 `Map<String, Object>` 而不是 `Object`**：`ObjectPropertyCodecProvider` 的注释里
明确写了「`Map<String, Object>` 本来不会被卡住，驱动的 `MapPropertyCodecProvider` 对值类型为 `Object`
的 Map 有兜底」，而且**必须仍能落库**这件事有单测钉着
（`ObjectPropertyCodecProviderTest` 的两个地图用例）。选 Map 少一层不确定性，语义也更对
（配置本来就是键值）。

**索引**：`spaceId` 唯一索引 —— 一个空间一份布局。这也是「整体读、整体写」的物理保证。

沿用本仓库既有的建索引方式（**没有 migration 文件**）：在 `DashboardLayoutRepository.ensureIndexes()`
里幂等创建，由 `DashboardLayoutService` 的 `@Observes StartupEvent` 触发，
并用一个 `dashboard.index.enabled` 配置项控制 —— 与 `space.index.enabled` / `modbus.index.enabled`
同构（见 `application.properties:32,37`，实现见 `SpaceRepository.ensureIndexes` /
`ModbusServiceHistoryRepository.ensureIndexes`）。唯一索引的创建要容忍「已存在」与「历史脏数据」，
照 `ModbusHistoryRetention.decide` 那种「先判定再动作」的纯函数写法，便于单测。

**时间字段**：`createTime` / `updateTime` 用 `LocalDateTime`（与 `SpaceEntity` 同口径，
而非 `Date`）；`creator` / `updater` 用 `Person {id, name, timestamp: Date}`（与 `ModbusConfig` 同）。

### 6.2 接口清单

Base Path：`/matrix/v1/dashboard`（与 `space` / `device` / `statistics` 并列；网关若剥离 `/matrix` 前缀，
对外即 `/v1/dashboard/**`，与前端现有调用一致）。

| 方法 | 路径 | 权限 | 说明 |
| --- | --- | --- | --- |
| `GET` | `/layout/{spaceId}` | 空间成员 | 读布局；文档不存在时返回**内存生成的预置布局**（见 §6.5），`version: 0`，不报 404、不落库 |
| `PUT` | `/layout/{spaceId}` | 空间管理员 | 整体保存；body 带 `version`，不匹配则报错 |
| `GET` | `/layout/{spaceId}/preset` | 空间成员 | 预置布局长什么样（**只读、无副作用**，给「恢复默认」当草稿起点） |
| `POST` | `/render/{spaceId}` | 空间成员 | 批量取数；body 可选传 `widgets`（编辑器预览用未保存的配置），不传则用已存布局 |
| `GET` | `/catalog/{spaceId}` | 空间成员 | 编辑器下拉用的候选项：设备清单（did + 显示名）、Modbus 服务与字段（含 `format`）、告警文本去重清单 |

> **为什么 `PUT` 是整体保存而不是 widget 级 CRUD**：拖拽改的是多个 widget 的坐标（D2），
> widget 级 CRUD 在并发下会拼出谁都没见过的中间布局。`version` 乐观锁足够覆盖「两个人同时编辑」
> 这个真实冲突场景，且报错信息能直接告诉用户「布局已被他人修改，请刷新」。

> **乐观锁必须是原子的，不能「先比再写」**：只在服务层比较 `version` 再 `replaceOne(_id)`
> 是「读—判—写」——两个管理员同时保存时都能读到 `version = 3`、都判定通过，
> 后写的那份把先写的悄悄盖掉，乐观锁形同虚设。所以写入用的是
> `replaceOne({_id, version: 期望值}, 新文档)`，**把版本放进筛选条件**，
> 匹配数为 0 就一个字都不写、直接报冲突（`DashboardLayoutRepository.replaceIfVersion`）。
> 「不存在则新建」那条路径同理：提交的版本必须是 0，而两个管理员同时创建第一份布局时
> 唯一索引会让后一个拿到 11000 —— 这与「版本不一致」是同一件事（别人先写成功了），
> 一并翻译成冲突提示，而不是 500。

**权限已定（用户确认）**：**读 = 空间成员，写 = 空间管理员**，布局**空间共享一份**。
后端的鉴权就是 §6.3 的 `requireSpaceMember` / `requireSpaceAdmin` 两个方法，不引入新概念。

由此带来两个必须写清的约定：

- **布局 DTO 里不放 `editable` 之类的字段**。前端本来就能算：页面已有的 `isAdmin` 惯用法
  （`project.component.ts:171`、`device.component.ts:196` 等 5 处逐字相同的 computed：
  先看自己在空间成员里的 `role`，再看所属组织在根空间 `accesses` 里的 `role`）
  就是用在这里的。新页面直接照抄这个 computed，**管理员才渲染「编辑布局」「加卡片」入口**
  —— 普通成员看到的就是一屏只读看板，与后端 `PUT` 会拒绝的行为一致。
  （那 5 份拷贝本身就是重复，本页会是第 6 份；顺手提到一个共享 helper 是好时机，
  但不作为本方案的前置条件。）
- **`render` 是成员可用的**，包括 body 里带未保存 `widgets` 的那种调用（编辑器预览）。
  这不构成漏洞：`render` 只读，且 §6.4 的归属校验照做 —— 成员本来就看得见本空间的数据，
  预览不了别人的卡片。

### 6.3 资源骨架

照 `StatisticsResource` 的写法：

```java
@Path("/matrix/v1/dashboard")
@Produces(MediaType.APPLICATION_JSON)
@Tag(name = "Dashboard", description = "自定义数据看板布局与卡片数据 API")
@RequestScoped
public class DashboardResource extends AbstractResource {

    @GET @Path("/layout/{spaceId}")
    @RolesAllowed({OxRole.USER})
    public Uni<Response> layout(@PathParam("spaceId") String spaceId) {
        return requireSpaceMember(spaceId)
                .chain(space -> layoutService.load(space))          // 无则返回预置默认
                .map(layout -> OxResponse.ok(DashboardCodec.encode(layout)))
                .onFailure(IllegalArgumentException.class)
                .recoverWithItem(e -> OxResponse.error(e.getMessage()));
    }

    @PUT @Path("/layout/{spaceId}")
    @RolesAllowed({OxRole.USER})
    public Uni<Response> save(@PathParam("spaceId") String spaceId, DashboardLayoutDto body) {
        return requireSpaceAdmin(spaceId)                            // ← 管理员
                .chain(space -> validator.validate(space, body))     // 见 §6.4
                .chain(space -> layoutService.save(space, body, currentUser()))
                .map(layout -> OxResponse.ok(DashboardCodec.encode(layout)))
                .onFailure(IllegalArgumentException.class)
                .recoverWithItem(e -> OxResponse.error(e.getMessage()));
    }

    @GET @Path("/layout/{spaceId}/preset")                           // ← 预置布局（只读）
    @RolesAllowed({OxRole.USER})
    public Uni<Response> preset(@PathParam("spaceId") String spaceId) {
        return requireSpaceMember(spaceId)                           // ← 只是读，成员即可
                .map(space -> OxResponse.ok(DashboardLayoutCodec.encode(layoutService.preset(space))))
                .onFailure(IllegalArgumentException.class)
                .recoverWithItem(e -> OxResponse.error(e.getMessage()));
    }

    @POST @Path("/render/{spaceId}")
    @RolesAllowed({OxRole.USER})
    public Uni<Response> render(@PathParam("spaceId") String spaceId, RenderRequest body) {
        return requireSpaceMember(spaceId)
                .chain(space -> widgetDataService.render(space, body.getWidgets()))
                .map(result -> OxResponse.ok(WidgetDataCodec.encode(result)))
                .onFailure(IllegalArgumentException.class)
                .recoverWithItem(e -> OxResponse.error(e.getMessage()));
    }

    private Uni<SpaceEntity> requireSpaceMember(String spaceId) { /* 同 StatisticsResource:91-100 */ }
    private Uni<SpaceEntity> requireSpaceAdmin(String spaceId)  { /* 换 checkSpaceAdmin */ }
}
```

两个鉴权辅助方法：

- `requireSpaceMember` 与 `StatisticsResource` 里那个逐字一致（`spaceService.getById` +
  `orgAccessService.checkSpaceMember`）。这个私有 helper 在 `DeviceResource` / `SpaceResource` /
  `SpaceAccessResource` / `ModbusAlarmResource` / `ModbusHistoryResource` / `ModbusPollResource` /
  `ModbusServiceResource` 里各有一份（`checkSpaceMember` 共 8 处调用）—— 本方案是第 9 份。
  建议顺手提到 `AbstractResource` 里共用，但这属于既有重复，**不作为本方案的前置条件**。
- `requireSpaceAdmin` 同形，把 `checkSpaceMember` 换成 `checkSpaceAdmin`
  （`OrgAccessService:109` 已有；写接口的先例见 `ModbusServiceResource:207`、`DeviceResource:351`）。
  只被 `PUT` 用到（预置那个端点只是读，走 `requireSpaceMember`）。

> **鉴权在资源层，不在 validator 里**：「你能不能写」是 `requireSpaceAdmin` 的事，
> validator 管的是「卡片配置本身合不合法」。两者分开，validator 才能被 `render` 复用
> （`render` 是成员可用的，见 §6.2）。

### 6.4 校验（`DashboardWidgetValidator` + `DashboardLayoutValidator`）

服务端**必须**重做一遍校验，不能信前端 —— 卡片配置是用户数据，会直接驱动服务端查询。

**拆成两个类**，因为两类错误的失败语义不同：

| 类 | 碰不碰库 | 失败粒度 | 测试方式 |
| --- | --- | --- | --- |
| `DashboardWidgetValidator`（纯函数，静态） | 不碰 | 整份：第一个错就返回 | 纯 JUnit，不需要 Mongo |
| `DashboardLayoutValidator`（`@ApplicationScoped`） | 查库 | **逐卡片**：返回 `widgetId → 错误文案` | 需要 Mongo |

为什么要逐卡片：一张引用了**已被删除的服务**的卡片，在渲染时只该黑掉它自己，
不该让整屏看板都渲染不出来（§6.6 的「一张卡失败只黑一张」）。而保存路径只想要
「第一个错误」，它是 `errorsByWidget` 的一个包装（`LinkedHashMap` 按卡片顺序插入，
所以报的确实是第一张出错的卡）。校验器的报错文案一律带 `widgets[i](widgetId):` 前缀 ——
编辑器一次能提交几十张卡，不指出是哪一张等于没报。

`DashboardWidgetValidator` 管的（纯结构）：

| 规则 | 失败信息 |
| --- | --- |
| `type` / `size` / `metric` / `dimension` / `source` / `display` / `bucket` / `chart` 取值合法 | 枚举外的值直接拒绝（`size` 那句报错文案**从 `WidgetSize.values()` 生成**，不手抄） |
| 坐标给了就必须合法：`x` / `y` **要么都给、要么都不给**（老文档），`0 ≤ x ≤ 23`、`y ≥ 0` | 只给一个的按「都没有」处理是最坏的结果：半张卡有位置、半张没有 |
| `version` 必填且 ≥ 0（`Long` 而不是 `long`，见 `DashboardLayoutDto`） | 缺 `version` 是客户端 bug，不能被静默当成 0 |
| 卡片数量 ≤ 30、`id` 非空且文档内唯一 | 防止一次 render 打爆后端 |
| `service` 的 `fields` 非空、去重、上限 12 | 空数组渲染出来是一张空白卡 |
| `maxPoints` ∈ [1, 2000]；窗口 ≤ 31 天 | 与 `overview` 同口径 |
| `showUnit` / `showStale` / `showFailureShadow` 是布尔（给了就必须真布尔） | 防止前端把字符串 `"true"` 塞进来 |

`DashboardLayoutValidator` 管的（要查库）：

| 规则 | 失败信息 |
| --- | --- |
| `serviceId`（`service` / `line(serviceField)`）**确实属于该空间**：`service.device.space.rootId == 根空间` | 见下方「越权」一段 |
| `did`（`device` 卡片）同样按 `device.rootId` 判 | 同上 |
| `(serviceId, functionIndex, field)` 三元组真实存在 | 字段名写错的卡片要在**保存时**就报错，而不是渲染成空图 |
| `service` 的 `fields` **每个**都存在，且都属于 `functionIndex`；**位区字段的位名（`bit.field`）也算合法字段名** | 与上一条同因。位是独立点位（§5.7），漏了这条会让「选了一个位」的卡片存不进去 |

三条**故意不拦**的：

- **卡片重叠、以及 `x + w ≤ 24` 都不校验** —— 服务端**没有 `w`**（占格表只在前端，见 §5.2），
  做不了。这条不是漏了，是范围：重排是前端的活，前端 `placeAt` 保证落定后不重叠不越界，
  服务端原样收、原样存。**别以为加个 `w` 就能校验了** —— 那正是修订（十）删掉的东西。
- `line(serviceField)` 的字段**不强制**数值型 —— 非数值在曲线上留断点（与 history 页同口径），
  编辑器给提示即可（§5.4）。
- `device` 卡片的 `pid` **不校验**：PID 的存在性只有 manipulation 服务知道，
  要在保存时校验就得同步调一次上游。它错了的表现是卡片显示上游返回的
  `{status, description}`（`API.md` §1 的既有形状），本来就有反馈。

> **越权是这里最需要小心的一类**：`serviceId` / `did` 是用户填的，如果只校验「是空间成员」
> 就按 id 查，那么拿到别人的 id 就能读别人的数据。`Modbus-History.md` 明确写了当前是
> 「判定通过后一律按 id 查，不再校验服务是否属于该空间」—— 那在**用户手填 id** 的场景下不够。
> 本功能的 `catalog` 接口只下发本空间的候选，`render` 侧再校验一次归属，两道一起才安全。
> 注意 `catalog` 那道只是 UX 层的方便，**真正把关的是这里**。

> **保存时校验存在性是有代价的，这个代价是明知故犯**：卡片引用的服务被删除后，这份布局
> 就再也存不进去了，直到管理员把那张卡删掉 —— 一次删除会让旧布局「变脏」。
> 之所以仍然这么做：替代方案是不在保存时报错，用户只能在渲染时看到一张空卡，
> 且没有任何提示告诉他「你选的字段早就没了」。报错带下标与 widgetId，编辑器能定位到那张卡，
> 用户要做的只是删掉它。

### 6.5 默认预置布局

空间从未配置过时，`GET /layout` 返回一份**预置布局**：就是现在 `dashboard` 那一屏
（4 张统计卡 + 3 张分布 + 1 条告警曲线），用新 schema 表达。具体 8 张卡片：

| # | 卡片 | `type` | `size` | `config` |
| --- | --- | --- | --- | --- |
| 1 | 设备总量 | `stat` | `W6H92` | `metric: devices.total` |
| 2 | 服务总量 | `stat` | `W6H92` | `metric: services.total` |
| 3 | 今日告警 | `stat` | `W6H92` | `metric: alarms.today` + 24h 窗口 |
| 4 | 故障种类 | `stat` | `W6H92` | `metric: failures.total` + 24h 窗口 |
| 5 | 设备类型分布 | `distribution` | `W12H308` | `dimension: deviceType`，`chart: pie` |
| 6 | 服务类型分布 | `distribution` | `W12H308` | `dimension: serviceType`，`chart: pie` |
| 7 | 告警类型分布 | `distribution` | `W12H308` | `dimension: alarmType` + 24h 窗口，`chart: pie` |
| 8 | 告警曲线 | `line` | `W12H308` | `source: alarmCount` + 24h 窗口，`bucket: hour` |

**排满了，没有空洞**：4 张 `W6H92`（各 6 列、一行高）正好填满第一行的 24 列，
4 张 `W12H308`（各 12 列、三行高）两两一行填满接下来两行 —— 首屏共 7 行，
`92 + 16 + 308 + 16 + 308 = 740px`。

**非统计卡取三行的 `W12H308` 而不是两行的 `W12H200`**（修订（十五））：曲线与分布图在 200px 里
要挤掉卡头与坐标轴，剩下画图的地方太扁。也不是四行的 `W12H416` —— 一屏只装得下一行卡，
用户第一眼看到的是「这东西没做完」，与上面否掉 `W24H416` 是同一个理由。

**统计卡从 200px 的档位换成 92px 的**（修订（十二），今天叫 `W6H200` → `W6H92`）：
统计卡是唯一「没有卡头、按内容自然高约 90px」的卡片，塞在 200px 的格子里下面空 110px。
**只影响从未保存过的空间** ——
存过布局的空间读的是自己那份文档，一张卡的档位都不会被悄悄改掉。

**顺序仍是阅读顺序，服务端仍不给坐标**（修订（十二）复核过，与修订十的口径一致）：
位置由前端按这个数组顺序 `flowPlace` 铺一遍。**别把修订（四）那张手写坐标表当成要恢复的东西** ——
服务端写不了坐标，因为坐标得配着 `w` 一起才对（写重叠了就摆出两张叠在一起的卡），
而 `w` 只在前端。前端哪天把那一档从 6 列改成 8 列（那样 `W6H92` 这个名字也得跟着改），
手写的坐标表就会**静默地**摆出重叠的卡片；
交给 `flowPlace` 反而永远自洽。**上表改动顺序，用户看到的预置首屏就跟着变**，
所以 `DashboardCodecTest.encode_presetWidgetsAreInVisualOrder` 钉着它（连「一个坐标键都不发」
也一起钉了）。

**与原页面的两处不同，都是被逼的**：

- **去掉了「本月能耗」与「日能耗曲线」**。这两张卡读的是 `dashboard.mock.ts` 的假数据
  （种子随机数），而 §5.3 的 metric 表里**根本没有能耗** —— 一个没有数据源的卡片不该进预置布局：
  用户第一次编辑时既没法把它改成真数据，也不知道该不该删。第 4 张统计卡因此改用
  `failures.total`：它是 §5.3 里唯一还没被用到的 metric，且原页面从来没展示过故障，正好补上。
- **标题走 `titleKey` 而不是 `title`**（§5.2）：预置布局是服务端造的，直接写「设备总量」
  会让英文界面显示中文。第 4 张的 key 是**新键**「故障种类」，要按 `AGENTS.md` 的规矩
  追加到全部 66 份字典末尾（不是「故障总数」：`failures.total` 的口径是「几种」不是「几次」，
  见 §5.3）。

**预置布局只在内存里生成，不落库**（是 `PresetLayouts` 里一个纯函数，不是「首次 GET 时补一条文档」）。
布局是空间共享一份，若走「首次 GET 落库」，两个成员同时打开页面就会并发建两条文档 ——
要么加唯一索引 + 重试，要么面对重复文档，代价都大于收益。落库只发生在 `PUT`：
管理员真正改过布局，文档才诞生。

**「恢复默认」不是删文档**（修订十七）：它 `GET /layout/{spaceId}/preset` 拿一份只读的预置布局
装进草稿，用户点「保存布局」时才 PUT 上去 —— 所以「恢复到从未保存过的状态」这件事，
从改造前的一次 `DELETE` 变成了「用预置那份覆盖写一次」。`DELETE` 端点已删（留着也没人调，
而它一旦被调就是不可撤销的破坏）。`version` 因此在「从未保存过」时恒为 `0`（预置布局的版本号），
第一次保存提交 `version: 0` 即可 —— 这一点没变。

这样 `/main/dashboard` 一上线就有内容、和用户熟悉的 dashboard 长得一样，而不是一进来空白页 ——
「空白页 + 一个加号」对新功能是最劝退的。也顺便验证了 schema 能表达现有的全部卡片。

**布局按根空间存**：路径上的 `{spaceId}` 可能是子空间，而布局是「项目一份」，
所以 `load` / `save` / `reset` 一律先 `space.rootSpaceId()` 归一到根空间（§5.1）。
`DashboardLayoutValidator` 判归属用的是同一个基准 —— 两边必须一致，
否则会出现「存得下但渲染是空的」。这也意味着**子空间里的设备/服务算本项目可用**
（设备带 `rootId`、服务带 `device.space.rootId`，都是接入时一并写下的）。

### 6.6 编排与量级

`WidgetDataService.render` 的关键是**去重**（§D3）：

```java
Uni<WidgetData> render(SpaceEntity space, List<DashboardWidget> widgets) {
    // 1) 收集所有需要 overview 的卡片，算出最大窗口，只调一次
    // 2) serviceField 曲线按 (serviceId, functionIndex) 归并，同字段只查一次；
    //    showFailureShadow 时同批取一次 failures
    // 3) 服务卡片与曲线同源（都是 Modbus 影子/历史），能合并的合并
    // 4) 设备卡片按 did 归并成一次 /shadows
    // 5) 逐个包成 {success, data|message} —— 一张卡失败只黑一张
}
```

**没有「写」路径**：所有卡片都是纯读（§D6），`render` 不会触发任何设备操作。
这是它敢被前端按那个间隔一直轮询的前提 —— 轮询一个纯读接口，最坏的结果只是多几次查询。

**缓存**：`overview` 是对整个子树的聚合，若 60 秒轮询 × N 个用户，压力不可忽略。
两个可选动作（本期建议先不做，留观测位）：

- 把 `to` 对齐到整点，让同一小时内的请求可缓存 —— 代价是当前小时不再「随时刻往上爬」
  （而 `StatisticsResource` 的注释明确把这个爬升当作**特性**：曲线右端与「今日」同源）。
  **倾向不动**，改为加服务端短 TTL 缓存（如 10 秒），两边的语义都不牺牲。
- 前端对同一屏的渲染做**单飞**（in-flight 去重）：窗口切换频繁时避免请求堆积。

---

## 7. 前端设计

### 7.1 文件结构

```
src/app/typedef/define/dashboard/DashboardLayout.ts     # DashboardLayout / DashboardWidget / 各 config 类型 + 尺寸常量表
src/app/typedef/codec/dashboard/DashboardLayoutCodec.ts # decode / encode

src/app/typedef/define/dashboard/DashboardWidgetData.ts     # render 结果：WidgetDataItem + stat / distribution / line 三个读取器
src/app/typedef/codec/dashboard/DashboardWidgetDataCodec.ts # decode（只解不编）

src/app/service/dashboard.service.ts             # layout / save / preset / render / catalog

src/app/pages/main/dashboard/                     # 首页（2026-09-17 由 home/ 归位而来，见修订（十一））
  dashboard.component.ts|html|less               # 页面：取布局、渲染 24 列 grid、编辑态开关、工具条上的自动刷新间隔
  dashboard.routes.ts                            # DASHBOARD_ROUTES（挂在 /main/dashboard）
  dashboard.grid.ts|spec.ts                      # 纯函数：档位 → 占格 / 高度、坐标铺排（flowPlace / placeAt / compact / findSlot）、
                                                 #   布局改动判定（layoutSignature / sameLayout，修订十七）
  dashboard.folding.ts|spec.ts                   # 纯函数：时间窗口、整点标签、今日求和、分布折算
  dashboard.charts.ts|spec.ts                    # 纯函数：告警曲线与分布图的 option（自建）
  dashboard.config.ts|spec.ts                    # 纯函数：卡片配置的防御性读取（读不出合法值就当没配）
  widget/
    host/widget.host.ts|html|less                # 按 type 分发（@switch），统一卡片外壳 + 错误态
    stat/ · line/ · distribution/ · device/ · service/   # 五类卡片（各自的取数与折算在同目录）
    note/widget.note.* · window/widget.window.*  # 卡片内的两小块：提示行 / 时间窗口
  editor/
    widget.editor.ts|html|less                   # 单张卡片的配置表单（按 type 分表单）+「删除」
    widget.picker.ts|html|less                   # 「添加卡片」的类型选择对话框（修订（十）新增）
```

`widget.picker.*` 当初**没有建**（修订（六）那条口径）：添加卡片做成工具条上的一排类型按钮
（`统计数字` / `曲线图` / `数据分布`），少一次点击，而且「这个版本能加哪几种」一眼看完。
**修订（十）把这条改了** —— 卡片长到五种、还要能拖拽排序之后，工具条上那排按钮挤不下，
改回了一个「添加卡片」按钮 + 类型选择对话框，`widget.picker.*` 就是那时建的。
（`DASHBOARD_EDITABLE_TYPES` 那份类型表仍在 `DashboardLayout.ts` 里。）

`dashboard.config.ts` 是写渲染层时**新加的一层**（原方案没有）：`DashboardWidget.config` 是
`Record<string, unknown>`，库里可能存着旧版本写的、或者手改过的配置 —— 少字段、类型不对、
`hours` 是负数都合法地存在。渲染层从这里取值，读不出合法值就当没配。这一层单列一个文件而不是
塞进 `dashboard.folding`：`dashboard.folding` 管的是「服务端的数怎么折」，这里管的是「配置能不能信」，
两者的输入一个来自 render 响应、一个来自库里的历史数据。


**布局与取数结果分两个文件**（原方案只列了 `DashboardLayout`）：布局是**用户配置**（存库、可编辑、
有版本号），取数是**瞬时读数**（算完就丢、下次刷新全换）。放一份类型里会让「哪些字段能保存」
变成要靠记忆的事 —— 而这两者恰好是「编辑后 PUT」与「刷新时 POST」两个方向的载荷，混在一起
早晚会把 `from` / `to` 写进保存请求体。

几个纯函数文件（`dashboard.grid` / `dashboard.folding` / `dashboard.charts` / `dashboard.config`）
**无注入、有 spec** —— vitest + jsdom 是本仓库已验证的测试策略。**它们全部自建**：老那一屏固定看板
（当时在 `pages/main/dashboard/`）只作行为参照，等价逻辑本页自己写（§2.1 复用边界），
而它已于 2026-09-17 删除。

| 本页要的逻辑 | 处理 |
| --- | --- |
| 服务字段曲线 option | **复用** `historyFieldOption`（跨页面共享模块，与老那一屏无关） |
| 告警曲线 / 分布图 option | `dashboard.charts.ts` 自建 |
| 时间窗口、整点标签、今日求和 | `dashboard.folding.ts` 自建。语义与后端接口口径绑定；老那一屏那份实现已随它删除 |
| 分布折算（同名合并、空 `configId` 归口） | `dashboard.folding.ts` 自建；显示名拼装复用 `modbusConfigLabel` |
| 尺寸档位 → 占格 / 高度 | `dashboard.grid.ts` 自建（修订（十）之后只剩这一件事，修订（十二）又扩回一套坐标运算） |
| 坐标铺排：按顺序铺、移动推开、判界、扫空位 | `dashboard.grid.ts` 自建（修订（四）删掉过一轮，修订（十二）以 `(x, y)` + 行单位的形式回来；修订（十三）去掉上吸、加了 `fitsAt`） |
| 「布局改过没有」（「保存布局」能不能点） | `dashboard.grid.ts` 自建（修订十七）：`layoutSignature` / `sameLayout` 做**结构**比较，config 的键序不算差别 |

### 7.2 状态与取数

- `DashboardComponent`：`signal` 持有 `layout` / `widgetData` / `editMode` / `saving`
- **`canEdit` computed 决定编辑入口是否出现**（§6.2：写权限 = 空间管理员）。
  照抄项目里已有的 `isAdmin` 惯用法（`project.component.ts:171`、`device.component.ts:196`、
  `device.detail.component.ts` / `device.children.component.ts` / `device.services.component.ts`
  共 5 处逐字相同）：先看自己在空间成员里 `role === 'admin'`，再看当前组织命中根空间
  `accesses` 里 `type === 'organization'` 的条目且自己是该组织管理员。
  `canEdit` 为假时：不渲染「编辑布局」「添加卡片」按钮，也不发起 `catalog` 请求。
  > 这 5 份拷贝是既有重复，本页会是第 6 份。**本期照抄**（改动范围可控），
  > 但值得单独提一个共享 helper —— 不塞进本方案，免得一个看板页顺带重构 5 个页面。
- **`effect` 监听 `account.space()` 变化重载** —— 这是本项目「项目上下文」的标准接法
  （不走路由参数），先例很多：`device.component.ts`、`alarm.component.ts`、`project.component.ts`、
  `history.component.ts` 等
- 首次加载：`GET /layout` 与 `POST /render` **可以并行**（render 不传 widgets 时服务端读已存布局），
  省一个 RTT
- 自动刷新（修订十七）：**工具条上一个全局间隔**（`关闭 / 30s / 1m / 5m / 15m / 1h`，缺省 `1m`），
  到点整屏重取一次读数（`render` 不带草稿 = 渲染整份已存布局）。全屏**一个** `setInterval`，
  换间隔 / 换项目 / 页面销毁时清掉。间隔**不落库**，按项目存在 `localStorage`
  （`dashboard.refreshSeconds.<spaceId>`），认不出来（没选过、被手改过、档位已下线）就回缺省值
- 保存：`PUT /layout` 带 `version`；冲突（版本不符）时提示「布局已被他人修改」并提供「重新加载」

### 7.3 渲染

- 网格：CSS Grid，`grid-template-columns: repeat(24, 1fr)`，`gap: 16px`，`grid-auto-rows: 92px`（§D4 常量）。
  **每一格都显式定位**：`[style.grid-column]="x + 1 + ' / span ' + w"`、
  `[style.grid-row]="y + 1 + ' / span ' + h"` —— 跨 `h` 行的格子正好 `h×92 + (h−1)×16` px，
  与 `cardHeight(h)` 逐像素相同，所以 `[height]` 照旧能传（ECharts 要一个有界父级）。
  列数、行高、间距三个常量都**由模板绑上去**，`dashboard.component.less` 里不写死
  （那份 less 专门交代过「样式表不抄第二处」）。
  - `.board` 上**不能加 `align-items: start`**：`align-self: start` 会让 `.cell` 只取内容高、
    不占满自己那 `h` 行，编辑态的虚线框会比卡片矮一截。显式行之后它本来也没用了 ——
    每张卡的行跨度就是它的高度，不再有「一行里矮的要跟高的比」这件事。
  - **拖动中 `@for` 必须仍按 `widgets()` 的数组顺序迭代**，位置只走绑定。被拖的那个节点
    正被 CDK 持有着（`sortingDisabled` 让 CDK 一个 DOM 都不搬），Angular 一挪它卡片就会跳。
- 卡片外壳：`nz-card` + `nzSize="small"` + 标题行：标题（用户数据，原样显示）
  + 编辑态下的操作按钮（编辑、删除、拖动把手）
- 图表：`<div [appEcharts]="option()" class="chart">`；**容器必须有非零尺寸**，
  指令里已经处理了 0 尺寸不初始化（`echarts.directive.ts:71-78`），但网格里仍要给最小高度，
  否则 `.chart { height: 100% }` 会塌成 0
- **服务卡片的正文**（§5.7 的版式）：`字段: 数值` 一行一个，字段名原样显示（用户数据，不翻译），
  数值后按 `showUnit` 缀单位；位值显示成 `开`/`关`。用 `nz-statistic` 会把这张卡挤成大字报，
  多字段时也排不下 —— 这里直接几行 `div`，`.value` 给等宽数字字体即可
- **设备卡片的正文**：`属性名: 数值`，与上面同形（同一段样式，不写两套）
- 空/错态：`nz-empty`（无数据）+ `nz-alert`（该卡片取数失败，只影响这一张）
- 响应式：24 列宽的档位（`W24H200` / `W24H308` / `W24H416`）在窄屏上要能降级。**最后没走「`dashboard.grid.ts` 里按视口宽度折算列数」**
  这条路（那要读视口宽度，纯函数就不再纯了），而是在 `dashboard.component.less` 里一条媒体查询把
  `.board` 从 24 列 grid 换成 `flex-direction: column` —— 一列一张，卡片自带的 `grid-column`
  与 `grid-row` 在 flex 下**一起失效**，卡片按 DOM 顺序（= 数组顺序 = `(y, x)` 阅读顺序）竖排。
  纯函数那边只留「档位 → 占格 / 高度 + 坐标铺排」（修订（十二））
- **拖拽**（修订（十二），判界与配色见修订（十三））：`.board` 上留着 `cdkDropList` 但打开
  `cdkDropListSortingDisabled` —— CDK 从此**一个 DOM 都不搬**（`_sortItem` 直接 early-return），
  让位完全交给 `placeAt`。于是拖动中就能看到其它卡片实时重排，呈现的是**松手后的样子**。
  - `.cell` 上挂 `cdkDragStarted` / `cdkDragMoved` / `cdkDragEnded`。`dragMoved` 每像素都发，
    所以**目标格子没变就直接 return**。
  - 落点框是网格里的**一个正常成员**（同一个 `grid-column` / `grid-row` 绑定 +
    `pointer-events: none` + `z-index: 1`）：跨几行就多高，**不需要量任何像素**。
    它就是 `dragTarget` 自己那个格子（按卡片原坐标取的写法是个 bug，修订（十三）修掉）。
  - **拖动中三处反馈同看一个判据**（`dropState`，由 `fitsAt` 算）：
    | 状态 | 幽灵卡（跟着指针那张） | 落点框 | 松手 |
    | --- | --- | --- | --- |
    | 放得下 | 绿底（`#f6ffed` + `#52c41a` 边） | 绿色虚线 | 落在框里 |
    | 放不下 | 红底（`#fff2f0` + `#ff4d4f` 边） | 不画 | 弹回原位，布局不动 |
    「放不下」只有越界一种（`x < 0`、`y < 0`、`x + w > 24`）；**压在别人身上算放得下**，
    被压的往下让。所以 `dragMoved` **不夹边界** —— 夹了就永远看不出越界。
  - 幽灵卡的底色是**组件的信号**绑上去的（`[class.drag-ghost-ok]`）：这个预览视图挂在 `.cell`
    的 `ViewContainerRef` 上（只有 DOM 节点被 CDK 挪到 `<body>`），所以每轮变更检测都会重算。
    标题不能这么做 —— 它走 `[data]`，那是拖动开始那一刻的快照。
  - 占位块（`.cdk-drag-placeholder`）留成**灰虚线框**（里面的深拷内容 `visibility: hidden` 藏掉）：
    它标的是「卡片从哪来」，与绿色落点框配对读。用 `opacity` 系而不是 `display: none` ——
    后者会让网格项退出隐式行尺寸的计算，松手一瞬间屏幕会抖。
  - 拖动中 `placements()` 用 `dragTarget()` 那次 `placeAt` 的结果渲染（**越界时整屏不动**），
    被拖的那张卡落在用户放的那一格，所以卡片落下的位置与落点框严丝合缝。

### 7.4 编辑态交互（本期取舍）

| 能力 | 本期 | 说明 |
| --- | --- | --- |
| 添加卡片（选类型 → 填配置） | ✅ | 对话框，按 type 分表单 |
| 编辑卡片配置 | ✅ | 同上，回填现有配置 |
| 删除卡片 | ✅ | `nz-popconfirm` 确认 |
| 改尺寸（**宽 × 高两个下拉**） | ✅ | 改完自动重排。宽是列数、高是像素，两边都是**纯数字**（不引入要翻译的文案）。宽只有 2–3 个选项、高只有 1–3 个，且**高跟着宽联动**（`widthChoices` / `sizeChoices`）：统计卡只有 92 一档高度，6 宽只有 200 一档。当前档位选不出来时（存量卡片）补进选项，不让下拉显示空白 |
| **拖拽落格**（二维自由摆放） | ✅ | 修订（十二）落地、修订（十三）补上「能不能放」的提示，见 §D4 / §7.3。`cdk/drag-drop` 只省掉「拖动」，落格判定、碰撞推移、实时预览都是自己写的 |
| 拖动中「可以放置 / 不可以放置」的提示 | ✅ | 修订（十三）：越界（不是「压到别人」）时幽灵卡染红、无落点框，松手弹回原位 |
| 缩放（拉边框改尺寸） | ⏳ 二期 | 行单位化之后它有地方落了，但那是另一件事 |
| 上下移 | ❌ | 修订（六）做过、修订（十）删掉（`moveInOrder` 那条路径也一起删了）—— 「顺序即位置」作废之后「上移一格」不再是用户想表达的意思 |
| 撤销 / 重做 | ⏳ | 编辑态本地草稿 + 整体保存，天然可做，但不必一期 |

**编辑态 = 本地草稿，点「保存」才 PUT**。这既贴合整体保存的设计（D2），也让「撤销」将来几乎免费。

> **「保存布局」只在真改过时才点得动**（修订十七）：草稿与已存布局做**结构比较**
> （`sameLayout`，config 的键序不算差别），一样就 `disabled`。不这么做的话，「进编辑态 →
> 什么都没动 → 保存」会白跑一次 PUT，还会在乐观锁下把版本号推上去 —— 别人正编辑着，
> 就会莫名其妙撞上版本冲突。
>
> **「恢复默认」也只改草稿**（修订十七）：它 GET 一份只读的预置布局装进草稿、标脏，用户还得点
> 「保存布局」才落库；在那之前「退出编辑」就等于什么都没发生过。改造前的 `DELETE` 是一点下去
> 库里的布局当场就没了 ——「保存才生效」在那套里根本无从谈起，所以它当时必须套一个确认气泡，
> 现在那个气泡也去掉了：一个能撤销的动作不必先过一道确认。

**服务卡片与 Modbus 曲线卡片的编辑器要走三级级联**：选服务 → 选 function → 选字段。
候选来自 `catalog`（服务定义），选中字段时立刻能拿到 `format` / `unit` / `valueList`，
于是可以当场做两件事：**预填单位和枚举描述**（§D5 的展示元数据），以及
**对非数值字段给一行提示**（「该字段为非数值，曲线上会是断点」）。
> 注意是**提示，不是从下拉里剔除** —— §5.4 已定：这类字段在 history 页可以选，
> 这里保持同口径，否则同一个字段会在两个页面一个能选一个不能选。

字段下拉里，**位区字段要连它的位一起展开成平级选项**（父字段 + `bit.field` 逐位），
**服务卡片可多选（`fields`）、曲线卡片单选（`field`）**。这份展开逻辑 history 页已经有了
（`device.service.history.component.ts:238-262` 的 `fields` computed），照它来，别另造一套。

让用户手填 `serviceId` / `field` 原始字符串是这个表单里最容易出错的地方，级联能直接消掉这类错误。

### 7.5 i18n

- 页面文案（`添加卡片`、`保存布局`、`选择设备`、各 metric/dimension 的中文名）：
  走 `| translate`，键即中文原文，追加到 `public/i18n/*.json`，
  **66 个词典键集合与顺序必须完全一致，新键追加末尾**
- 卡片标题、设备名、告警文本、点表厂家型号、字段名、`unit`：**一律原样显示，绝不翻译**
- 图表 option 构造函数**不接翻译器**：图上的片名、字段名、单位全是服务端/用户数据，原样显示。
  可翻译的只有卡片标题这类页面文案，那是在模板 / 组件层翻，不进 option 构造函数

### 7.6 路由与入口

**本页就是首页（2026-09-17 归位）**：路由 `/main/dashboard`（`main.routes.ts` 的 `dashboard` 一条，
`DASHBOARD_ROUTES`，带 `authGuard`），breadcrumb 与菜单 label 都是「首页」，
组件类名是 `DashboardComponent`。老那一屏固定看板已删除 —— 这里**不再有「两个入口并存」**。

这一节曾经的口径是「落地页不变（用户确认）：`/main` 继续重定向到 `/main/dashboard`，
`/main/home` 只是菜单里的另一个入口，两者并存，所以一行路由代码都不用改 ——
菜单 label『测试首页』也先留着，**等这屏真接替 dashboard 时再谈改名与改落地页**」。
那个时刻到了，具体做了什么见修订（十一）。

三条随之确定的事：

- **落地页**：`app.routes.ts` 与 `main.routes.ts` 里的 `/main` → `/main/dashboard` 重定向不动，
  登录后 `callback.component.ts` 的缺省跳转也仍是 `/main/dashboard` —— 这两处本来就指着这个路径，
  改名之后它们指向的**就是本页**，一行都不用改。
- **`/main/home` 不再存在**：没有留重定向。全项目本来就没有通配路由，
  旧路径会直接匹配失败 —— 与「这个页面不存在了」的语义一致。
- **§6.5 的「预置布局 = 当时那张首页」** 这条口径的历史理由（两个入口并存时点进来该看到同一屏、
  认知负担最小）已经用完，但**预置布局本身照旧**：它就是本页第一次打开时看到的东西。
  当时那张首页的版本细节见 `dashboard.charts.ts` 的模块说明。

---

## 8. 分期实施

### P1 —— 打通闭环（统计 + 分布 + 告警曲线）

- 后端：`DashboardLayoutEntity` / `DashboardLayoutRepository` / `DashboardResource` / `DashboardLayoutService` /
  `DashboardWidgetValidator` / `WidgetDataService`（只接 `overview`）、`GET|PUT /layout`、
  `GET /layout/{spaceId}/preset`、`POST /render`、默认预置布局
- 前端：typedef + codec + `dashboard.service.ts`、网格渲染、`stat` / `distribution` / `line(alarmCount)`
  三种 widget、编辑对话框（增删改 + 尺寸）
- 前端纯函数（**全部自建，不依赖老那一屏**）：`dashboard.grid.ts`（当时是紧凑、冲突、尺寸换算、
  响应式列数；修订（十）之后只剩档位 → 占格 / 高度；修订（十二）扩回坐标铺排）、
  `dashboard.folding.ts`（窗口、整点标签、
  今日求和、分布折算）、`dashboard.charts.ts`（告警曲线与分布图的 option），各配 spec
- **出口标准**：能用新 schema 完整复刻现在 dashboard 那一屏

### P2 —— 设备卡片 + 服务卡片 + Modbus 字段曲线

- 设备卡片（`GET /device/shadows`，纯读）
- 服务卡片（`GET /modbus/history/current`，纯读）+ **三态展示**（未采集 / 陈旧 / 采集失败，§5.7），
  按用户给的版式渲染：标题 = 服务名，正文一行一个 `字段: 数值`
- `catalog` 接口：设备清单、Modbus 服务与字段（**含 `format` / `unit` / `valueList` / `bitList`**）、
  告警文本去重清单。**位区字段要连位一起展开**（父字段 + 逐位），照 history 页的展开逻辑（§5.7）
- 编辑器三级级联（选服务 → 选 function → 选字段，可多选 `fields`），选中字段即知单位与展示形态
- Modbus 字段曲线：接 `/modbus/history/range` + `/modbus/history/failures`，
  **渲染复用 `historyFieldOption`**，看板侧只写「卡片 config → `HistoryFieldSeries` /
  `HistoryChartContext`」的适配器（§5.4）—— 不写第二份曲线实现
- ~~越权归属校验补齐（§6.4）~~：**已在 P1 提前落地**（`DashboardLayoutValidator` 在保存与
  每次 render 时都判归属）。它不该等到有设备卡片才做 —— 那是安全边界，而 P1 的 render
  已经会拿着用户填的 `serviceId` / `did` 去查库了。P2 只是给它接上真正的数据源。

### P3 —— 编辑体验与进阶

- ~~真·拖拽落格~~（**修订（十二）提前做完了**）+ 缩放、撤销/重做
- `bar` / `gauge` 展示（记得在 `echarts.directive.ts:15-24` 补注册 `BarChart` / `GaugeChart`）
- 故障阴影（`showFailureShadow`）、阈值线、点击下钻到设备/服务详情
- 短 TTL 缓存与前端单飞（§6.6）

### 不在本方案内

- **通用设备/服务属性历史**（§3.1 方案 B/C）：需要新增订阅、落库、TTL、降采样查询接口，
  是独立的一项立项工作。已确认本期不做。
- **xiot-spec 方法调用卡片**（`POST /device/actions`）：有副作用，不适合定时刷新的看板（§D6）。

---

## 9. 测试

| 层 | 内容 |
| --- | --- |
| 前端纯函数（vitest） | `dashboard.grid`：修订（十）删掉坐标后只剩 `sizeOf` / `cardHeight`，修订（十二）扩回一套坐标运算 —— `flowPlace`（**复刻改造前的流式排版**，存量布局长相不变的保证）、`placeAt`（**幂等**、不重叠、不越界、**让完不吸**）、`compact`（**幂等**、只上吸不横挪）、`findSlot`、`fitsAt`（界内判定：越右 / 越上 / 负数各一例，**压着别人算放得下**单独一例）、`cellDelta`（像素位移 → 跨几列几行）、档位↔格子换算、**档位名与档位表一致**（`W{w}H{cardHeight(h)}`，修订（十四）换名的全部理由）、可选档位三函数（`sizeAllowed` 按「恰好一行 / 一行以上」判、`widthChoices`、`sizeChoices` 跟着宽度联动）；`dashboard.folding`：窗口计算、整点标签、今日求和、分布折算（同名合并、空 `configId` 归口）；`dashboard.charts`：告警曲线与分布图的 option |
| 曲线适配器 | 「卡片 config + render 数据 → `HistoryFieldSeries` / `HistoryChartContext`」的映射，含 `step`（开关量）、`unit`、`failures`、`carryIn` 四个字段各自的来源。**`historyFieldOption` 本身不重复测** —— 它已有自己的覆盖 |
| 前端 codec | `DashboardLayoutCodec` 的 decode 兜底（字段缺失/类型不对时给默认值，照 `DeviceEntityCodec` 的口径）；坐标一节单独钉住：读成两个非负整数（字符串数字 / 小数 / 负数一律不收）、**只给一个的按「都没有」处理**、`y = 0` 是合法坐标不是「没设」、往返保真；档位一节钉住**六个旧名各翻译到占格相同的新名**（落进兜底档位就是一次静默的改尺寸）与「先查现名再查旧名」的顺序 |
| 后端单测 | `DashboardWidgetValidator` 每条规则各一例（含越权、越界、非法枚举、不存在的 `(serviceId, functionIndex, field)` 三元组）；嵌套 config 的落库/读回（照 `ObjectPropertyCodecProviderTest`） |
| 后端接口测试 | `DashboardResourceTest`：成员可读不可写、管理员可写、`version` 冲突报错、`render` 的单卡片错误隔离 |
| 服务卡片三态 | 未采集 / 陈旧（`recordedAt` 久远）/ 采集失败（`lastError`）三种数据各渲染一次，别让它们长得一样（§5.7） |

---

## 10. 待确认问题

已确认的不再列出：

- 曲线图数据源范围 → §3.1 走 A（只做有历史的）；服务卡片 → §D6 只读 ModbusService 轮询字段
- **布局写权限 = 空间管理员**（读写不对称：读是成员）→ §6.2、§6.3
- **布局 = 空间共享一份**，预置布局不落库、首次保存才产生文档 → §6.5
- **落地页**：`/main` 重定向到 `/main/dashboard`。这条当时是「与 `/main/home` 并存」，
  **2026-09-17 已归位**：本页就是 `/main/dashboard`，`/main/home` 不再存在 → §7.6、修订（十一）
- **设备卡片的 `showStale` 本期不做**（`/shadows` 没有时间戳，不去 manipulation 侧补）→ §5.6
- **服务卡片版式**：标题 = 服务名，正文一行一个 `字段: 数值`；`display` 默认 `value`；
  位值显示 `开`/`关`；`bitList` 不需要特殊排布（位是独立点位）→ §5.7

**全部已确认，没有待确认的了。**

原最后一条是「尺寸档位：S/M/L/XL 四档够吗？行高 38 / 间距 16 这两个常量是否合适？」——
**2026-09-17 由修订（十二）回答**：档位加到六档（统计卡专用 `W6H92` / `W12H92`），行高定 92、
间距仍 16。两条都是用户定的口径，理由见 §D4。

**2026-09-18 由修订（十四）再动了两处**：补上等差数列里缺的那一级 `W12H308` / `W24H308` 与
整宽的 `W24H200`（共九档），档位名换成 `W{列}H{像素}`。同样是用户定的口径，理由见 §D4。

**同日由修订（十五）用上了那一档**：预置布局的 4 张非统计卡从 `W12H200` 改成 `W12H308`
（首屏 524px → 740px）。改的是 `PresetLayouts`（从未保存过的空间），
**不是** `DASHBOARD_DEFAULT_SIZE`（新加一张卡的档位，仍是 `W12H200`）—— 两者的区别见 §6.5。

**同日由修订（十六）再补一档**：`W6H308`（窄而高，正好是三张统计卡叠起来那一条），
共**十档**。修订（十四）否定它的理由（「太窄太高」）只对曲线成立，饼图不看宽高比 ——
用户试过之后否掉了。6 宽的 416 仍然没有。

---

## 11. 变更记录

### 2026-09-17 初稿

按用户的两点澄清定稿：

- **服务卡片 = `ModbusService` 的 function 调用**（轮询任务里某个 function 的某个返回字段的值），
  **纯读、无副作用**。据此**删掉了「方法调用（`invoke`）读法」**及其全部配套：
  `readMode` 字段、`acknowledgeSideEffect` 校验、方法白名单、副作用警告 UI、入参表单引擎。
  这是本稿相对初版简化最多的一处。同时补上了服务卡片的三态（未采集 / 陈旧 / 采集失败）设计。
- **任意设备属性的变化曲线本期不做**（用户明确）—— 曲线卡片的数据源只保留
  `alarmCount` 与 `serviceField` 两种。

写方案过程中确定、值得单独记一笔的两件事：

- **异构 `config` 不需要新写 codec**：`db/mongo/ObjectPropertyCodecProvider` 已经兜住了
  声明为 `Object` 的字段，`Map<String, Object>` 更是本来就有兜底（`ObjectPropertyCodecProviderTest`
  有一条用例钉着）。所以 5 种卡片的 config 不必统一成一个大 POJO。
- **Modbus 字段的展示元数据是现成的**：`ModbusServiceField` 自带 `format` / `unit` / `valueList` /
  `bitList` / `scale`，编辑器的三级级联和「这个字段能不能画曲线」的判断都不需要新接口。

### 2026-09-17 修订：划清前端复用边界

用户指出 `pages/main/dashboard/**` 后续可能要删，**其代码不能作为复用来源**。据此：

- §2.1 增加「复用边界」声明：dashboard 目录整个视为**待删的兄弟页面**，
  `dashboard.charts.ts` / `dashboard.functions.ts` **只作行为参照、不作依赖**；
  真正可复用的一律收窄到共享层（`common/**`、`typedef/**`、`service/**`）。
- §7.1 明确 `home.grid` / `home.folding` / `home.charts` **全部自建**，
  并新增 `home.folding.ts` 承载窗口与折算（原先打算参照 `dashboard.functions.ts`）。
- 文中其余「照 dashboard 的 X 做」一律改写为自带理由的表述，不再指向该目录。

顺带一个**比原方案更好的发现**：`pages/main/device/services/service/history/device.service.history.charts.ts:53`
的 `historyFieldOption` 是一个**已经跨页面共享**的模块（定义处注释即写明「供 history 页与将来别的页面复用」，
已被 `device.service.history.component.ts` 与 `history.services.component.ts` 两处 import），
carry-in / 降采样 / 失败竖线 / 开关量阶梯线 / 单位轴 / 时间刻度粒度全都实现了。
所以 **Modbus 字段曲线卡片直接复用它**，看板侧只写一个适配器 —— 比原方案里「自建 `home.charts.ts` 画曲线」
省掉一整份实现，且不会出现第二套曲线口径。

同时按它的既有行为修正了两处原设计：

- 失败标记是 **`markLine` 竖虚线**（「某一刻没采到」），不是 `markArea` 阴影区；
  `showFailureShadow` 这个字段名保留，但画法以此为准。
- 非数值字段（命中 `valueList` 的 `description` 字符串）**允许保存、曲线上留断点**，
  与 history 页同口径 —— 原方案写的「服务端拒绝」会造成同一字段在一个页面能选、在另一个页面不能选。

### 2026-09-17 修订：后端命名 `home` → `dashboard`

用户指出：这个功能的领域概念是**自定义数据看板**，`HomeComponent` 只是前端临时落点，
后端不该跟着叫 `home`。据此把**后端命名整体改掉**：

| 原 | 现 |
| --- | --- |
| `/matrix/v1/home/**` | `/matrix/v1/dashboard/**` |
| `db/home/` · `api/home/` | `db/dashboard/` · `api/dashboard/` |
| `HomeLayoutEntity` · `HomeWidget` | `DashboardLayoutEntity` · `DashboardWidget` |
| 集合 `home-layouts` | 集合 `dashboard-layouts` |
| `HomeLayoutRepository` · `HomeLayoutService` · `HomeResource` | `DashboardLayoutRepository` · `DashboardLayoutService` · `DashboardResource` |
| `HomeLayoutDto` · `HomeCodec` · `HomeWidgetValidator` | `DashboardLayoutDto` · `DashboardCodec` · `DashboardWidgetValidator` |
| `home.index.enabled` · `@Tag("Home")` | `dashboard.index.enabled` · `@Tag("Dashboard")` |
| `HomeLayoutCodec` · `HomeResourceTest` | `DashboardLayoutCodec` · `DashboardResourceTest` |

前端跟着改的只有**镜像后端契约的那一层**：`typedef/define/dashboard/DashboardLayout.ts`、
`typedef/codec/dashboard/DashboardLayoutCodec.ts`、`service/dashboard.service.ts`
—— 它们本来就是接口的形状，跟着契约走才不会出现「前端 HomeLayout 对后端 DashboardLayout」。

前端**页面自身**的文件名保持 `home.*`（`home.component.*` / `home.grid.ts` / `home.folding.ts` /
`home.charts.ts`）：页面是临时的，将来这屏看板接替 `/main/dashboard` 时整目录会重命名，
现在改反而多一次无谓的改动。约定已写在文首「命名约定」一节。
（**已于 2026-09-17 兑现** —— 就是这里说的那一刻，见修订（十一）。）

`WidgetLayout` / `WidgetData` / `WidgetDataService` 不加 `Dashboard` 前缀：
它们表达的是卡片自身的形状与数据，已被 `db/dashboard/` 包名限定，加前缀只会让
`DashboardWidget.widgetLayout` 这类引用更啰嗦。

### 2026-09-17 修订：权限与归属定稿

用户确认了两项，据此补齐了三处原先含糊的地方：

- **写权限 = 空间管理员，布局 = 空间共享一份**（§10 前两条已移除）。
  §6.2 补上读写不对称的约定：**布局 DTO 不放 `editable` 字段**，前端用页面已有的 `isAdmin`
  惯用法算 `canEdit`（§7.2），普通成员看到的就是只读看板 —— 与后端 `PUT` 的拒绝行为一致。
  §6.3 补上 `requireSpaceAdmin`，并写明**鉴权在资源层、validator 只管配置合法性**，
  这样 validator 才能被成员可用的 `render` 复用。
- **预置布局改为明确「只在内存生成，不落库」**（§6.5）。原稿写的是「不落库，或首次 GET 时落库」——
  既然布局是空间共享一份，「首次 GET 落库」就有两个成员同时打开时并发建两条文档的竞态。
  落库只发生在 `PUT`，`DELETE` 即回到预置，因此 `version` 在「从未保存过」时恒为 `0`。

### 2026-09-17 修订：落地页、设备卡片、服务卡片版式定稿

用户又确认了三项，§10 从 4 条降到 1 条：

- **落地页不变**：`/main` 继续重定向到 `/main/dashboard`，`/main/home` 与之并存（§7.6）。
  **本方案因此一行路由代码都不用改**，菜单 label 也先留着。这反过来解释了 §6.5 为什么
  要「预置布局 = 现在 dashboard 那一屏」：两个入口并存时内容一致，只是其中一屏从此可改。
- **设备卡片的 `showStale` 删掉**（§5.6）。不是懒得做，是 `GET /shadows` 只返回 `{pid, value}`、
  **没有时间戳**，无从判断陈旧；本期也不去 manipulation 侧补。服务卡片有 `recordedAt`，不受影响。
- **服务卡片版式定稿**（§5.7）：标题 = 服务名，正文一行一个 `字段: 数值`。据此把 config 的
  `field` 改成 **`fields` 数组**（一次 function 调用返回全部字段，多显示几个不增加后端成本），
  并加了约束：**同一张卡片的字段必须来自同一个 function** —— `recordedAt` / `lastError`
  属于整个 function，混 function 的话时间戳对其中一行必然不成立。

**回答「`bitList` 字段在卡片上怎么排布」时发现原方案有问题，据此简化了两处**：

`ModbusServiceField.bitList`（`{offset, field}`）的语义是「01/02 位区**逐位取值**」，
一次调用同时产出**整段位掩码**（父字段）与**每个具名位的 0/1**（键为 `bit.field`），
两者**各自有独立的轮询值与历史** —— `device.service.history.component.ts:247-258` 里每个位
都被展开成一个独立的 `FieldRef` 与父字段并列。也就是说**位本来就是独立点位**，
所以：字段下拉里位与父字段是平级选项，选哪个显示哪个，**永远只有一行 `字段: 数值`**，
根本不存在「一组小灯怎么排」的问题。位的值显示成 `开`/`关`，不显示 `0/1`。

据此**删掉了 `display: "bitList"`**（「把字段展开成一组 0/1 小灯」）：既然位可选，
用户要三个位就选三个位排三行；那种「偷偷展开字段」的模式反而会让卡片订了几个点位变得不清不楚。
同时 §6.4 补了一条：**位名（`bit.field`）也算合法字段名**，否则「选了一个位」的卡片存不进去。

**顺带修正 §D4 尺寸表的一处实打实的错误**：原表的像素列是错的 ——
`S/M/L/XL` 四行分别写成 `200×200`、`200×380`、`416×380`、`416×812`，其中 `200` / `416`
其实是**高度**（`h=4 → 200`、`h=8 → 416`），宽度数字（`338` / `692` / `1400`）整个缺失，
`380` / `812` 则对不上任何一档。改法是**表里不写像素**：卡片的像素宽度取决于容器宽度，
本来就不是常量，真正的常量只有行高 38 与间距 16，表里只留 `w` / `h` 网格单位，
需要估算时附上了算式。

### 2026-09-17 修订：开始实现，后端落地时定稿的七处

用户确认「开始实现」，并选定「布局 + 取数一起做（原方案 P1）」「返回预置布局（原方案原设计）」。
写后端的过程中发现原稿有七处要么含糊、要么行不通，一并在方案里定稿：

1. **新增 `titleKey`**（§5.2）。原稿只有 `title`（用户数据，不翻译）+「空则按 `type` 给默认名」，
   而预置布局是**服务端**造的：写成 `title` 会让英文界面显示中文，留空则 4 张统计卡同名。
   两个都不行，所以给预置布局一个**我们自己的文案 key**（页面文案，该翻译）。
2. **预置布局的 8 张卡片与坐标写死成表**（§6.5），并**删掉两张假数据卡片**
   （「本月能耗」「日能耗曲线」读的是 `dashboard.mock.ts`，§5.3 的 metric 表里根本没有能耗）。
   第 4 张统计卡改用 `failures.total`，标题 key 是新键「故障种类」。
   告警曲线定为 `M` 而不是 `L`/`XL`：4 张 `S` + 4 张 `M` 正好排满 12 行无空洞。
3. **布局按根空间（项目）存**（§5.1、§6.5）：路径上的 `spaceId` 可能是子空间，
   读写一律 `space.rootSpaceId()`；归属判定用同一基准，于是**子空间里的设备/服务算本项目可用**。
   两边不一致就会出现「存得下但渲染是空的」。
4. **校验器拆成两个类**（§6.4）：纯函数的 `DashboardWidgetValidator`（结构，不需要 Mongo）
   与查库的 `DashboardLayoutValidator`（归属与三元组存在性）。拆开是因为失败粒度不同 ——
   渲染要**逐卡片**隔离（一张引用已删服务的卡片只该黑它自己），保存只要第一个错误。
5. **乐观锁必须原子**（§6.2）：`replaceOne({_id, version: 期望值})`，把版本放进筛选条件，
   而不是「读出来比一下再写」；「不存在则新建」那条路径上的 11000 也翻译成版本冲突提示。
6. **`device` 卡片的 `pid` 不校验**（§6.4）：PID 的存在性只有 manipulation 服务知道，
   保存时校验就得同步调上游；它错了的表现是卡片显示上游的 `{status, description}`，本就有反馈。
7. **§5.1 删掉 `createTime` / `updateTime`**：实体上是 `creator` / `updater` 两个 `Person`
   （自带 `timestamp`），多两个字段只会多两种不一致的写法。
   预置布局没有这两个键，`creator` / `updater` **整个不出现**，不是 `null`。

顺带记一笔**明知故犯的代价**（§6.4）：保存时校验三元组存在性，会让「服务被删除」使旧布局变脏
—— 那份布局在删掉那张卡之前存不进去。仍然这么做，是因为替代方案（不报错）会让用户
只能看到一张空卡且不知道原因。

### 2026-09-17 修订（二）：写测试时改掉的四处

后端 120 条测试（纯单测 77 + 接口测试 43）写完之后回补的四处。前两处是**真的 bug**，
不写测试不会发现 —— 三处的共同点是「静默算错」，比报错难查得多：

1. **`subMetric` 也要窗口**（§5.3）：原校验只看 `metric`，于是
   `metric: devices.total` + `subMetric: alarms.today` 这种组合能存进去但不需要窗口，
   render 时退回兜底窗口，前端把 1 小时当「今日」求和。改成「任一方需要窗口就必填」。
2. **`chart` 只接受 `pie`**（§5.5）：原校验接受 `bar`（`bar` 排在 P3，
   `echarts.directive.ts` 还没注册 `BarChart`），与 `display` 只接受 `value` 的处理自相矛盾。
   统一成「没实现的取值在保存时就拒」—— 用户配了柱状图却看到饼图，比保存时报错更难查。
3. **`DELETE /layout` 返回删完之后读到的预置布局**，而不是空响应（§6.2）：响应形状因此与
   GET/PUT 完全一致，前端「恢复默认」省一次 GET，也没有「删完再读」之间被人插进一次保存的竞态。
4. **越权归属校验提前到 P1**（§8 P2 的第一条相应划掉）：它就是安全边界，而 P1 的 render
   已经拿着用户填的 `serviceId` / `did` 查库了，不该等到有设备卡片才补。

另有一条**没改代码、只记进测试**的行为：`showFailureShadow` 只在 `serviceField` 分支校验，
`alarmCount` 上带着它一律忽略 —— 阴影画的是 Modbus 采集失败，告警曲线没有这种东西。
这不是漏校验，用一条测试把它钉住，免得后来者「顺手补全」成一个错误提示。

### 2026-09-17 修订（三）：前端 typedef 与 codec 的四条口径

前端 `typedef` / `codec` / `dashboard.service` 落地时定下的四条，都是**错了不报错、只会悄悄不对**的那类：

1. **布局与取数结果分成两个文件**（§7.1 已补）：布局是可编辑的用户配置、要 PUT 回去，
   取数是瞬时读数、只解不编。混在一起早晚会把 `from` / `to` 写进保存请求体。
2. **`encode` 只发该发的**：`w` / `h` 不发（服务端按 `size` 档位覆盖，发了只会让「谁说了算」含糊）、
   `spaceId` / `creator` / `updater` 不发（服务端从路径与 JWT 取）。但 **`version` 必须发** ——
   它是乐观锁，漏了后端按「首次保存」处理，别人的改动被无声覆盖。
3. **空标题不发 `title` 键**：用户把标题清空，本意是「改回默认名」，发一个空串上去会被存成
   「这个人把标题清空了」的卡片。同理，用户起过名字的卡片**不再回写 `titleKey`**（它已是用户的卡，
   不该留一个会随服务端改预置文案而变的旧记号）。
4. **`undefined` 与 `0` 必须分得开**：`refresh: 0` 是「不自动刷新」，`value` 缺失是「这个指标不给
   直接值」（`alarms.today` 只给 `hourly`）。读取器一律不收字符串数字，坏点丢掉、整条数组留下。

另：服务用 `@Service()`（`autoProvided` 缺省 `true`，等价 `providedIn: 'root'`）+
`inject()`，按 AGENTS.md 对**新**单例服务的要求；仓库里现有服务都还是
`@Injectable({providedIn:'root'})` + 构造器注入，但那是旧写法，不跟着抄。
`catalog` 接口**不在 P1**：编辑器的一级级联排在 P2（§8），所以 `dashboard.service` 现在只有
`layout` / `save` / `reset` / `render` 四个方法。

### 2026-09-17 修订（四）：写 `home.grid` 的测试时改掉的两处

`home.grid` / `home.folding` / `home.charts` 三个纯函数文件与 71 条用例落地时，**测试逼出来两个
真 bug**，都在 `home.grid` 里，都属于「不报错、只是慢慢地不对」：

1. **`compactLayout` 只对位置幂等，对顺序不幂等**。它按 (y, x) 序处理，但处理顺序是按**吸之前**
   的位置排的，吸完一遍之后这个顺序往往已经不等于「按吸完的位置排序」。原样返回的话，第二次进来
   就是另一个顺序、另一组位置 —— **同一份布局每保存一次就自己重排一次**，用户会以为卡片在乱动。
   改为**按吸完之后的位置重排输出**，`compact(compact(x)) == compact(x)` 才真正成立（位置与顺序
   都不动）。这条性质现在是该函数最要紧的不变量，用例直接钉住。
2. **折叠后的占格必须当参数传下去**。`foldLayout` 原本直接复用 `compactLayout`，而后者按 `size`
   档位**现算** `w` / `h` —— 折叠算出来的宽度当场被丢掉，一块 `XL` 在 6 列屏上仍按 24 列算，
   `x + w = 9 > 6` 直接越界，卡片被挤出网格。抽出共用的 `pack()`，占格由调用方给：
   紧凑按档位给、折叠按缩放后的值给。用例用「四种档位 × 三种列数」扫了一遍越界与重叠。

另外三处**不是 bug、是把口径写清楚**：

- **折叠会顺手消掉顶部空洞**（紧凑的副作用）。窄屏上一整条空白带比宽屏刺眼得多，而宽屏那份布局
  之所以有那个洞，往往只是因为它左边原本有张卡、折过来之后挪走了。相对顺序仍然保住 ——
  紧凑从不重排，只上吸。
- **`windowLabel` 不给绝对窗口格式化日期**：相对窗口返回「词条 key + 小时数」（由调用方翻译），
  绝对窗口返回两个**毫秒时间戳**，由模板的 `date` 管道格式化。纯函数不该决定日期怎么写，
  更不该认识 i18n。
- **跨天窗口的整点标签带日期**（`MM-DD HH:00`）：一周的告警曲线用裸 `HH:00` 会出现七个
  一模一样的「08:00」。现在 dashboard 那份固定 24 小时的实现没有这个问题，
  但看板的窗口是用户配的（最大 31 天），所以这里必须先做对。

### 2026-09-17 修订（五）：看板页面与三种卡片（P1 前端渲染层）

页面（`home.component.*`）、卡片外壳（`widget/widget.host.*`）与 `stat` / `line` / `distribution`
三张卡片落地。十条口径，前四条是**错了不报错、只会悄悄不对**的那类：

1. **行高与间距由 `gridStyle` 给，不写进 less**。卡片的像素高度 = `h × 38 + (h − 1) × 16`，
   而 `38` 与 `16` 只有一处常量（`DashboardLayout` 的 `GRID_ROW_HEIGHT` / `GRID_GAP`）。
   抄进样式表就是第二处 —— 改了档位表却漏改它，卡片会溢出格子、和下面那张叠在一起。
   列数同理走[style] 绑定。`home.grid` 的用例因此断言的是**整个样式对象**。
2. **没有读数时留白，不画 0**。首屏那几百毫秒、以及两次自动刷新之间，卡片身子是空的 ——
   而「0」是有效读数（项目里今天真有 0 条告警），两者混起来会让一张空卡片信誓旦旦地报一个假数，
   且那正是最容易被截图发出去的一瞬间。同理，「暂无看板数据」只在 `!loading()` 时显示：
   转圈的时候说「暂无数据」是一句还没查完的话。
3. **自动刷新按每张卡的 `refresh` 分组**，`0` 表示不刷新。不采用「取所有卡里最小的那个间隔、
   到点全刷」：一张 10 秒的卡片不该让一屏 60 秒的卡片陪它每 10 秒请求一次。刷新失败**不弹错、
   不清屏**——页面上那份读数仍然可用（只是旧了几十秒），下一个 tick 会再试；把整屏换成错误态，
   反而把「刚才还好好的」也一并弄没了。
4. **读数的键是 `id`，不是下标**。分组刷新只回来一部分卡片，要靠 id 并进手里那份；下标会随
   保存/拖拽变化，用下标对应等于随机换卡。合并时**其余卡片原样保留**。
5. **`home.config.ts` 单列一层**（见 §7.1）：`config` 是 `Record<string, unknown>`，库里有旧版本
   写的、手改过的配置。读不出合法值就当**没配**，不猜兜底 —— 拿 24 小时去填一张没配窗口的卡片，
   会画出一段用户根本没要的时间，而图上没有任何东西表明这是猜的。
6. **`stat` 卡的单位与次要数字标签由 `metric` 决定**，不由卡片标题决定：用户可以把标题改成
   「东区设备」，metric 还是 `devices.total`，后缀仍是「台」。为此在 `DashboardLayout.ts` 加了
   `DASHBOARD_METRICS`（metric → 显示名 + 单位，两者都同时是词典键）与两个取值函数。
   **单位只有 `台` / `个`**：告警与故障不带单位（中文本来就省略量词，`次` / `种` 直译成
   `5 times` 反而别扭）。这是显示决策，不是缺漏。
7. **P2 的卡片让服务端说自己那句话**。`device` / `service`、以及 `line` 的非 `alarmCount` 分支，
   render 时回 `success: false` 加一句英文（后端分得清「这个组合还没做」与「配置错了」）。
   前端**原样显示、不翻译、也不另编一句**：翻译会把那两句抹成一句，另编则是把服务端的话盖掉。
8. **绝对窗口的日期交给模板的 `date` 管道**（`home.folding` 只给两个时间戳），且**用本地时区**、
   不带 `GMT+00:00` —— 曲线的横轴标签（`hourLabels`）也是本地时刻，两者必须同一个钟，
   否则窗口写着 06:00 而轴上标着 14:00。
9. **列数测量挂在网格自己身上**，不是外层容器：外层那 20px 的内边距会让阈值整体偏掉。
   用 `ResizeObserver` 而不是 `window.resize`（侧栏收起、窗口分屏都改容器宽度而不改窗口宽度）。
   初值给 `GRID_COLUMNS`：第一次测量在首帧之后，先按 6 列排一次再跳到 24 列会让首屏闪一下重排。
10. **`totalOf` 留到编辑器用**：P1 的饼图不带圆心合计（版式要跟现有 dashboard 那一屏一致，
    而那一屏没有圆心字）。它会在编辑器的分布表单里显示「共 N 片」，那里才知道片数有没有被截。

**i18n**：新增 16 个键（`故障种类`、`其他`、`最近 {{hours}} 小时`、`统计数字`、`数据分布`、
`暂无看板数据`、`取数失败`、`重新加载`、`编辑布局`、`添加卡片`、`保存布局`、`恢复默认`、
`退出编辑`、`告警次数` 等；`曲线图` 原本就在词典里）。追加在 66 个词典**末尾的同一位置**，
键集合与顺序仍完全一致。

其中 `zh` / `zh_TW` / `zh_HK` / `en` / `en_AU` / `en_GB` 是**认真写的译文**（繁体两套按各自
词典的既有用词：`zh_TW` 用「資料 / 新增 / 分布」，`zh_HK` 用「數據 / 添加 / 分佈」），
**其余 60 个语种一律填英文**。这是有意为之、且需要后续处理：我写不出 60 种可信的译文，
而**留空会把中文原文直接显示给非中文用户**（严格更差 —— 中文字符他们一个也读不了），
英文至少可读。仓库里显然有自己的翻译流水线（`ne` / `kmr` / `ar` 都有真正的译文），
这些新键应当重跑一遍那条流水线。

### 2026-09-17 修订（六）：编辑态、配置对话框与保存（P1 收尾）

落地的文件：

```
home.component.ts|html|less            # 工具条、编辑态、磁贴、保存 / 恢复默认、canEdit
editor/widget.editor.ts|html|less      # 卡片配置表单（按 type 分表单）
home.grid.ts|spec.ts                   # 新增 moveInOrder（上移 / 下移）
DashboardLayout.ts                     # 新增 DASHBOARD_WIDGET_TITLES / EDITABLE_TYPES / DEFAULT_SIZE /
                                       #   DIMENSIONS / dashboardDimensionLabel
widget/widget.host.ts                  # 私有 DEFAULT_TITLES 搬去 schema
home.folding.ts|spec.ts                # 删掉 totalOf
66 × public/i18n/*.json                # 新增 27 个键
```

十三条口径：

1. **编辑态渲染磁贴，不是真卡片**（与 §7.4 表格里「卡片外壳 + 编辑态按钮」的写法不同）。
   三条理由：卡片头上已经有标题与窗口，操作按钮叠上去要么压住内容、要么被 ECharts 的 canvas
   吃掉点击；八张统计卡铺满一屏，光看标题认不出哪张是哪张，磁贴把「指标 / 维度 / 窗口」
   摊开写出来；编辑期间也不必重绘一屏 ECharts。**格子尺寸一模一样**，所以摆出来的仍然是
   所见即所得的那份布局。
   **代价说清楚**：编辑时看不到真图，「这张曲线该给 L 还是 M」只能先存了再退出编辑看。
   实时预览与真·拖拽一起放二期（§7.4 已列为 ⏳）。
2. **编辑态是一份本地草稿，点「保存布局」才写库**（§7.4）。进编辑态时草稿 =
   `compactLayout(服务端那份)`，而**渲染也走同一条紧凑**，所以进编辑态卡片不会跳一下；
   保存前再紧凑一次 —— 存下去的正是屏幕上那一刻看到的那份。
3. **`version` 用读回来的原值，保存后用服务端返回的那份替换本地那份**。服务端保存成功会 +1，
   不替换的话紧接着再存一次就撞冲突（`layout has been modified by someone else, please reload`）。
   保存失败**不动草稿** —— 用户改的那一屏还在，放不放弃由他点「退出编辑」决定
   （那也是唯一的救援路径：这一版没有「重新加载布局并保留草稿」）。
4. **`canEdit` 是第 6 份 `isAdmin`**（§7.2 已预见）。为假时不渲染编辑入口，也不发写请求。
5. **「添加卡片」是一排类型按钮，不是一层选择对话框**（见 §7.1）。类型表
   `DASHBOARD_EDITABLE_TYPES` 只含 P1 真能渲染的三种：给一个必然回「暂不支持」的按钮不叫功能。
6. **新卡片的 id 是随机串，不是时间戳**。同一个毫秒里连点两次「添加卡片」完全可能
   （对话框一关就能再点），而重复 id 会让两张卡的读数互相覆盖（render 的结果按 id 对应卡片）
   —— 是那种要盯很久才看得出来的错。撞了就重摇。
7. **`config` 里值为 `undefined` 的键是 `delete`，不是写 `null`**：窗口选「无」要真的把键去掉，
   请求体里多一个 `"window": null` 时，服务端对 `null` 与「没有这个键」的处理不一定同义。
8. **取消「添加卡片」会把刚加的那张撤掉**，否则「点一下曲线图、又按取消」会凭空多出一张卡。
   改配置的那种取消什么都不用做 —— 编辑器改的是它自己那份副本（`widget.editor` 里 `copy()`），
   草稿里那张从头到尾没被动过。
9. **「恢复默认」留在编辑态**，草稿一并换成预置布局：这是一次「换一份起点」而不是「改完了」。
   它是破坏性的，故套了确认气泡；「退出编辑」也套（未保存的修改会丢）。
10. **自动刷新的分组只看服务端那份布局，不看草稿**。编辑期间屏幕上是磁贴、本来就没有读数要更新，
    而按草稿起定时器有个更糟的后果：「退出编辑」不会重起定时器，那些定时器会一直刷着一份
    已经被丢掉的草稿。
11. **保存 / 恢复默认之后只重取读数，不重取布局**（`reloadData`）。布局刚由服务端返回，再 GET
    一次是白跑一趟，还会让整屏闪一下加载态。读数取不到就**置空**（卡片留白）而不是换成错误态：
    布局已经存好了，因为读数没取到就把整屏变成一张告警，反而把「刚保存成功」这件事盖掉。
12. **`totalOf` 删掉了** —— 与修订（五）第 10 条相反。这一版没有实时预览，而分布表单里的
    「显示片数」是个纯配置项，手上没有数据可合计。留一个没有调用方的导出比将来重加一次更贵。
13. **`DASHBOARD_WIDGET_TITLES` 从 `widget.host.ts` 搬进 schema**：卡片外壳与看板页（磁贴、
    添加卡片按钮）都要它，各留一份的话加了新类型总有一处忘记补。

**i18n**：新增 27 个键（`上移`、`下移`、`删除卡片`、`编辑卡片`、`卡片标题`、`留空则用默认名称`、
`尺寸`、`刷新间隔`、`不自动刷新`、`指标`、`次要指标`、`维度`、`时间窗口`、`相对时间`、`绝对区间`、
`无`、`小时`、`显示片数`、`最大点数`、`服务类型`、`告警类型`、`故障类型`、`不限制`、
`确认恢复默认布局？`、`未保存的修改将丢失，确认退出编辑？`、`该卡片类型暂不支持编辑`、
`超过这个数量时，其余合并为「其他」`）。`设备类型` 没有新增 —— 词典里本来就有，
值也正好是「设备类型」/`Device Type`，直接复用。66 份的键集合与顺序仍然完全一致（510 个键）。
`zh` / `zh_TW` / `zh_HK` / `en` / `en_AU` / `en_GB` 是认真写的译文（繁体按各自词典的既有用词：
`刷新` 两套都是「重新整理」、`保存` 是「儲存」、`恢复默认` 是「恢復預設」），
其余 60 个语种仍一律填英文 —— 与修订（五）同一个待办，需要重跑仓库的翻译流水线。

**深色模式**：`src/styles.less` 里那份 `body.dark-theme` 清单补了一截，覆盖这一页新出现的表面 ——
`.widget-window` / `.widget-note` / `.stat-sub` / `.tile-window` / `.tile-meta` / `.fixed-value` / `.hint`
（次要文字，`rgba(0,0,0,.45)` → `rgba(255,255,255,.45)`）、`.stat-sub-value`、`.tile`（编辑态磁贴铺在
页面底色上、不在 `nz-card` 里，不补就是深色底上的一块块白板）、`.cell-editing` 的虚线框。
不改的话是**深底上的深字**：字还在、位置也对，就是看不见，这类问题在浅色模式下自测不出来。
图上（ECharts）的文字颜色**没有一起改**：全仓库的 option 都没有写 `textStyle`，这是既有情况，
要动就得连其它页面一起动，不在这一版的范围里。

### 2026-09-17 修订（七）：卡片类型与 `nz-row` / `nz-col` 版式

按「卡片类型各自封装、页面改用 `nz-row`/`nz-col`、widget 各类型拆子目录」这三点重做了渲染层。
**这一版推翻了 §D4 与 §7.3 的两处写法**：「不用 `nz-row/nz-col`：nz 是**流式**布局，表达不了
自由坐标」与「渲染用 CSS Grid、按视口宽度折算列数」都不再成立 —— 换来的东西见第 5、6 条，
代价见第 6、9 条。

落地的文件：

```
widget/host/widget.host.ts|html|less            # 只剩「按 type 分发 + 算卡名 + 兜不认识类型」
widget/stat/stat.widget.ts|html|less            # 无卡头的 nz-card + nz-statistic
widget/line/line.widget.ts|html|less            # 带标题的 small nz-card
widget/distribution/distribution.widget.ts|html|less
widget/device/device.widget.ts|html|less        # 只建壳，见第 10 条
widget/service/service.widget.ts|html|less      # 只建壳
widget/window/widget.window.ts|html|less        # 卡头右端那段窗口，五种卡片共用
widget/note/widget.note.ts|html|less            # 「取数失败 / 还没取到 / 开发中」那句话
home.component.ts|html|less                     # 一屏改成 nz-row / nz-col
home.grid.ts|spec.ts                            # 新增 boardRows / cardHeight；删掉 columnsFor / foldLayout
src/styles.less                                 # 深色模式补 nz-statistic 的字色
```

原 `widget/widget.host.*`、`widget/stat.widget.*`、`widget/line.widget.*`、
`widget/distribution.widget.*` 四个扁平文件已删除。

十一条口径：

1. **卡片外壳的归属从宿主挪到各类型组件**。五张卡各自的壳本来就不同（统计卡**没有卡头**、
   其余四种是带标题的 `nz-card nzSize="small"`），共用一个壳就得在宿主里按类型写分支 ——
   那等于把「一张卡长什么样」摊在分发层。现在宿主只有三件事：按 `type` 分发、把卡名算好传下去、
   兜住前端不认识的类型。
   **共用的那部分外壳样式仍留在宿主的 less 里**（撑满格子、身子 `flex: 1` 且自收溢出），
   用 `::ng-deep`：`<nz-card>` 已经搬走了，但那几条规则是五张卡一模一样的，
   而宿主正好是它们共同的祖先。
2. **统计卡是唯一无卡头的卡，卡名搬进 `nz-statistic`**。`nz-card` 不给 `nzTitle`，卡名与窗口挂进
   `nz-statistic` 的 `nzTitle`（它收 `TemplateRef`）—— 两边都给会把卡名印两遍（卡头一行、
   数字上方一行）。`nzSize` 仍是 `small`：那管的是身子的留白，五张卡留白一致才像同一套东西。
   窗口在四种带标题的卡上落 `nzExtra`（卡头右端）、在统计卡上落标题行右侧 —— 位置因卡而异，
   所以抽成了 `widget/window/`。
3. **失败态与「还没取到」由各类型卡片自己画**（都复用 `widget/note/`）。放到宿主里集中画看着
   更省事，但统计卡一失败，卡名就会从 `nz-statistic` 里跳回卡头上 —— 同一张卡在两种状态下的
   版式不该变。**代价**：`@if (item()?.success)` 这个判断在五处各写了一遍。
4. **`item` 在统计卡上是可选的，在其余四种上是必填的**。统计卡的身子即使还没取到数也要画
   （卡名与窗口就在身子里），那时数字是 `-`；其余四种整个身子就是数据本身，没取到就留白
   （`note` 对 `undefined` 整段不渲染，所以「还没取到」与「取数失败」不会混成一句话）。
5. **一屏按 `y` 分行渲染**（`home.grid` 新增的 `boardRows`）：同 `y` 的卡片并排在一行里、
   行与行首尾相接，`nz-row` / `nz-col` 负责画；行内每张卡带自己的 `nzSpan`、左边空几列、
   以及按档位算出的像素高度。
6. **代价说清楚：`y` 从「绝对纵坐标」变成了「行号」**。「一张 8 行高的卡旁边摞两张 4 行高的卡」
   这种摆法排不出来 —— 后一张会被顶到下一行去，那一行里因此留一块空白。这是选 `nz-row/nz-col`
   时已经确认过的取舍，换来的是窄屏不必自己折算列数。
   **行高由这一行里最高的那张卡决定，但矮的卡不拉伸**（`nzAlign="top"` + 高度按自己的档位给）：
   把一张 `S` 档的卡拉到 400px 高，只会得到一个中间孤零零的数字。
7. **空档用一个空的 `nz-col` 垫出来**，不是 `nzOffset`：`nzOffset`（`margin-inline-start`）
   **不带响应式**，而空列和卡片一样有 `nzSpan`，窄屏折行时会跟着一起折。
   空档取 `x` 而不是「从左往右一个挨一个排」—— 紧凑只上吸、不左右挪（见修订四），
   所以「删掉左边那张、右边那张原地不动」是既定行为，渲染按顺序挨着排会让它自己滑到左边去。
   高度绑在**格子里的 `.cell`** 上、不绑 `nz-col`：`nz-col` 的宿主已经有一个 `[style]` 绑定
   （gutter 给的 `padding-inline`），再叠一个 `[style.height.px]`，两个绑定会互相覆盖。
8. **上移 / 下移看的是全屏下标，不是 `$first` / `$last`**。`moveInOrder` 换的是整屏 (y, x) 序上的
   相邻两张，所以一行里的第一张仍可以与上一行的最后一张换位置 —— 按 `$first` 禁用会禁掉一个
   本来能用的按钮。`rows` 里因此带了一个跨行连着数的 `index`。
9. **折行交给栅格断点，列数测量整个删掉**（`ResizeObserver`、`columns` 信号、`columnsFor`、
   `foldLayout` 一并删除）。两处代价：
   - 阈值看的是**视口**宽度，不是容器宽度。1400px 的屏幕上展开侧栏、容器只剩 600px 时，
     卡片仍按宽屏排 —— 原来那个 `ResizeObserver` 正是为了这件事才存在的。
   - 窄屏一列一张时，卡片**保持自己的档位高度**（`h × 38 + (h − 1) × 16`），不像原来折到 6 列
     那样会跟着变方，手机上会看到一屏又宽又扁的卡片。
10. **`device` / `service` 只建壳，不编数据形状**。后端现在对这两个类型回 `success: false`
    （「这个组合还没做」），`DASHBOARD_EDITABLE_TYPES` 里没有它们（编辑器里选不到），
    预置布局里也没有。所以成功分支是「开发中」、失败分支显示服务端那句话 ——
    **不猜字段名**：猜出来的形状与服务端一对不上整张卡就是空的，而且看不出是猜错了。
    P2 接取数时要改的只有这两个组件的身子。
11. **深色模式补了 `nz-statistic` 的字色**。它的字色写在组件自己的样式里
    （`.ant-statistic` / `-title` / `-content` 各给一次 `rgba(0, 0, 0, …)`），继承不到 `body`
    那一笔。这一笔**顺带修好了 `/main/dashboard` 那一屏**：那里的四个统计数字是同一个组件，
    深色模式下一直是深底深字（既有问题，这次一起修了）。

**i18n**：没有新增键（`开发中` 与 `取数失败` 都在词典里），66 份的键集合与顺序仍完全一致
（510 个键）。

**测试**：`home.grid.spec.ts` 删掉 `columnsFor` / `foldLayout` 两个 `describe`，新增
`boardRows`（9 条）与 `cardHeight`（2 条），其中「删掉左边那张，右边那张**不滑到左边**」
直接钉住第 7 条那个口径。`npx ng build` 与 `npx ng test --watch=false` 都绿
（367 条 / 16 个文件）。

**没验到的部分**：这一版只过了编译与纯函数用例，**没有在浏览器里看过**。版式、栅格的
空列与折行、`nz-statistic` 标题行的宽度分配都是纯 CSS 行为，用例覆盖不到，需要人工过一遍
（尤其窄屏一列一张时的高度、以及编辑态磁贴与真卡片是否严格同格）。

### 2026-09-17 修订（八）：高度归卡片自己 + 自有类名清零

**先作废修订（七）里的两条**：

1. 修订（七）第 7 条说「高度绑在**格子里的 `.cell`** 上」——**作废**：高度改由卡片自己给（见下）。
2. 修订（七）第 1 条说「共用的那部分外壳样式仍留在宿主的 less 里」——**收窄**：宿主里只剩
   `.ant-card-body` 一条，其余都回到各自的 less。

**改了什么**

1. **删掉两个自有类名：`widget-card` 与 `widget-title`**。
   - `widget-card` 原来是给宿主的 `::ng-deep` 当抓手的（`<nz-card>` 在各类型组件里、样式在宿主里）。
     但五张卡的根元素都是 `nz-card`，**选元素就够**。现在每张卡在自己的 less 里写 `nz-card { … }`
     —— 元素就在本组件的模板里，连 `::ng-deep` 都用不上。
   - `widget-title` 是**纯重复**：那三条截断规则（`overflow` / `white-space` / `text-overflow`）
     antd 自己写在 `.ant-card-head-title` 上（结构是 `.ant-card-head > .ant-card-head-wrapper >
     .ant-card-head-title`，已核对 `ng-zorro-antd.min.css`），我们那条一个字都没多给。
     模板里现在是一个裸的 `<span [title]="title()">`，`[title]` 留着悬停看全。
2. **宿主里只剩一条 `.ant-card-body`**，而它走不了：那是 nz-card **内部模板**造出来的元素，
   不带任何组件的 `_ngcontent`，只能由某个祖先用 `::ng-deep` 穿透。宿主正好是五张卡共同的祖先，
   写在那里是一份，不是往五个 less 里各抄一份 —— **这是 widget 树里唯一一处跨文件的样式**。
3. **统计卡的高度由内容定**（`min-height: 140px`），其余四张撑满格子。下限那一条是给
   「一排统计卡一样高」用的：有的卡有底部标注、有的没有，光靠内容高度会差出一行。
4. **高度一路给到卡片自己，`.cell` 不再有高度**（口径已确认：整行跟着缩到 140）。
   - 编辑态：`[style.height.px]="p.height"` 绑在**磁贴**上（原来绑在 `.cell` 上）；
   - 看数据：作为 `height` 输入给 `home-widget` → 宿主 → 四张撑满格子的卡，由它们在模板上
     绑给 `nz-card`；`device` / `service` 现在用不上（身子只有一句提示），但**先收着** ——
     P2 的列表（`overflow: auto`）要的就是这个确定高度的盒子；
   - **统计卡不收这个输入**：收了就得再写一处「忽略它」。
   - **为什么不能只给 `.cell` 高度**：格子（S 档 200px）比统计卡（140px）高，`.cell` 带高度的话
     那 60px 就空在卡片下面，一整行看起来像没对齐。
   - **为什么 `height: 100%` 也不成立**：`.cell` 变成 auto 之后百分比解析成 auto，图与列表的
     容器会塌成 0 —— 所以传递的必须是**像素值**，从档位来。
   - **代价**（当时确认过）：编辑态的磁贴按格子高度画，退出编辑后统计卡那一行往上收 60px、
     下面各行跟着动。换来的是统计卡高度固定、板上不留空带。
5. **`nz-card` 上绑 `[style.height.px]` 是安全的**：已核对 `NzCardComponent` 的 host ——
   只有 `class.ant-card-*` 一串 `class` 绑定，没有 `style` 绑定，两个绑定不会互相覆盖。
   （这条是修订（七）第 7 条踩过的坑：`nz-col` 的宿主有 `[style]`，所以才把高度让给了 `.cell`；
   `nz-card` 没有这个问题，高度现在可以直接落在卡片上。）

**两条原则的落点**

- 「能不用 class 就不滥用」：两个自有类名删掉，五张卡的外壳规则回到元素选择器；
- 「HTML 里的 class 定义放对应的 less」：`home.component.html`、`editor`、`note`、`window`
  与五张卡里出现的每个 class，现在都在同目录同名的 `.less` 里定义（逐个查过）。
  唯一的例外是上面第 2 条那条 `.ant-card-body`，它点的不是我们起的类名。

**i18n**：没有新增键，510 个键 × 66 份、键集合与顺序都不变。

**测试**：`npx ng build` 与 `npx ng test --watch=false` 都绿（367 条 / 16 个文件）。

**没验到的部分**：仍然**没有在浏览器里看过**。这一版改的全是高度与选择器，属于纯 CSS 行为，
用例覆盖不到 —— 尤其两件事需要人工确认：140px 这个下限够不够（卡片内容实测约 100～120px，
带底部标注的那张接近 130），以及编辑态磁贴 200px 与统计卡 140px 之间那 60px 的跳变
在实际操作里是否别扭。

### 2026-09-17 修订（九）：统计卡不再有高度下限 + 取数失败改到标题行 + 次要数字整体删除

**先作废修订（八）里的两条**：

1. （八）第 3 条「统计卡的高度由内容定（`min-height: 140px`）」——**作废**：一个高度值都不给了。
   于是（八）第 4 条那笔代价（「整行跟着缩到 140」、编辑态磁贴与统计卡之间 60px 的跳变）
   也随之改写：跳变还在，但不再是一个固定数。
2. （八）第 4 条里「为什么 `height: 100%` 也不成立」那半条**仍然成立**（`.cell` 是 auto，
   百分比还是解析成 auto），保留。

本条同时覆盖 §5.3（`subMetric` 那一段）、§7 的编辑器与 §9 里与次要数字有关的表述。

**改了什么**

1. **统计卡一个高度值都不给**：卡片就是 `<nz-card>` 包一个 `<nz-statistic>` 的缺省高度。
   行高仍跟着卡片走（`.cell` 自己不高，`nz-row` 那一行由最高的卡决定）。
   - 代价换了形状：一排统计卡不再保证一样高 —— 五张卡里只有统计卡这样，其余四张仍撑满格子。
     `min-height` 那条路走不通的原因写进了 `stat.widget.less`：卡片里只有一个数字，
     下限填多少都是猜，而且它把「差一点」变成了一条谁也不知道该跟谁对齐的硬编码。
   - 编辑态的磁贴**仍按格子高度（S 档 200px）画**，所以退出编辑后统计卡那一行还会往上收，
     收多少现在由内容定，不是固定的 60px。这一条是（八）第 4 条那笔代价的延续。
2. **取数失败那句话从卡片身子挪到标题行右端的一个图标上**（`stat.widget.html`）。
   - 图标**只在失败时出现**：`errorText()` 返回空串就整段不渲染。三态口径与
     `widget.note` 逐字一致 ——「还没取到」不是失败（首屏那几百毫秒、两次自动刷新之间），
     成功时也没有可说的。**没有做成常驻图标**：常驻的话人会以为每张卡都有话要说。
   - tooltip 就是**服务端那句话**（`WidgetDataItem.message`），按 AGENTS 原样显示、不翻译；
     服务端一定会给，真没有时退回词典里那句「取数失败」（既有键，与 `widget.note` 共用）。
   - 图标类型取 `exclamation-circle`：它现在只说错误这一件事。
   - **用 `nz-icon` 元件，不是 `<span nz-icon>`**：less 里按元件选（`.stat-head nz-icon`），
     挂属性的写法一条都选不中。`NgZorro` 的 selector 是 `nz-icon,[nz-icon]`，两种写法都合法，
     但只有元件写法与选择器对得上。
   - `.stat-head nz-icon { flex: none; align-self: center }`：图标是个没有文字的 `inline-block`，
     基线就是自己的下边缘 —— 跟着标题行的 `baseline` 走会整体浮上去半个字。
3. **`subMetric`（次要数字）整体删除**，跨两端一次清干净：
   - 前端：`stat.widget.ts` 的 `subMetric` / `subLabel` / `subValue` / `subUnit` 四个 computed、
     `.stat-sub` / `.stat-sub-label` / `.stat-sub-value` 三条样式、编辑器里那个下拉框与
     `setSubMetric`、`home.component.ts` 磁贴摘要里的 `/ 次要` 分支、
     `StatConfig.subMetric`、`StatData.subValue`（含 `statData()` 里那一行读取）；
   - 后端：`DashboardWidgetValidator.validateStat` 的四条 sub 规则（连同「主副共用一个 window」
     的那段注释，规则本身由 `needsWindow(metric)` 保留）、`WidgetDataService.statData` 的
     `subValue` 与 `needsLocalDay(subMetric)`、`PresetLayouts.stat()` 的那个参数；
   - 词典：66 份各删「次要指标」一行，510 → **509 键**。66 份的键集合与顺序仍完全一致
     （用脚本逐字节核对过：除删掉那一行外，每份文件与原文相同）。
   - **老数据不需要迁移**：校验器对 `config` 没有白名单，存着 `subMetric` 的旧布局照样保存、
     照样取数，只是不再有那个数字。两端各留了一条「残留键被忽略」的用例钉住这件事
     （`stat_leftoverSubMetricIsIgnored`、`render_statCard_leftoverSubMetricIsIgnored`）。
   - **预置布局因此少了一个数字**：原来「设备总量」那张挂 `devices.online`，现在没有卡片显示
     「在线」了。要不要把第一行换成「设备总量 / 在线 / 今日告警 / 故障种类」（仍是 4 张 × 6 列，
     正好 24 列）是产品取舍，**没有顺手改**。
4. 顺带清掉三处死东西：`stat.widget.less` 里的 `.stat-main`（模板里那个 wrapper 早就不在了）、
   `stat.widget.ts` 对 `WidgetNoteComponent` 的引用（错误提示已经不在身子里）、以及
   `styles.less` 深色模式那份覆盖清单里的 `.stat-sub` 与 `.stat-sub-value` 两条
   （那两张卡片里的淡字色，删了次要数字就没有元素用它了）。

**测试**：前端 `npx ng build` 与 `npx ng test --watch=false` 全绿（367 条 / 16 个文件，条数不变
—— 删的是断言不是用例）。后端 `DashboardWidgetValidatorTest` 60 条、`WidgetDataServiceTest` 28 条、
`DashboardCodecTest` 14 条、`DashboardResourceTest` 15 条全绿。

**没验到的部分**：仍然**没有在浏览器里看过**。这一版里需要人眼确认的两件事：
图标会不会把 `S` 档（6 列）那张卡的卡名挤得太短（名字已经能省略号+`title` 让位，但没量过
挤到几个字），以及 tooltip 里服务端那几句 `unsupportedDetail` 的长度（都挺长，会换行，
一行放得下多少没看过）。

### 2026-09-17 修订（十）：顺序进线格式 + 编辑页改造（添加卡片对话框 / 拖拽排序）

四条要求来自对编辑页的反馈：加一个「添加卡片」按钮（弹对话框选类型）、卡片可拖拽且其他卡片
让位重排、编辑态卡片尺寸与非编辑态**一致**、整张卡可点（`nzHoverable` 浮起）→ 打开配置框、
配置框里除「取消 / 确认」外加一个「删除」。

**先作废三处旧设计：**

1. **卡片不再有坐标** —— 修订（四）那套 `layout.x/y` + `compactLayout`（紧凑算法、`findSlot`
   找空位、`inOrder` 按 `(y, x)` 排序）**整个删除**，两端一起。
2. **编辑态不再画「磁贴」** —— 修订（六）第 3 条的 `.tile`（只写卡名 / 摘要 / 四个按钮）
   **作废**：编辑态现在画的就是真卡片本身（见下）。
3. **渲染不再用 `nz-row` / `nz-col`** —— 修订（七）的版式**作废**，换扁平 CSS Grid（见下）。

#### 1. 顺序即位置（线格式变更）

`DashboardLayout.widgets` 的**数组顺序就是屏幕顺序**，从左上往右下流式铺开。
删掉的东西：前端 `WidgetLayout` 类与 `DashboardWidget.layout`、`home.grid` 里的
`compactLayout` / `pack` / `lowestFreeY` / `overlaps` / `findSlot` / `clampX` / `clampWidth` /
`inOrder` / `moveInOrder` / `boardRows`；后端 `WidgetLayout` 类、`WidgetSize.applyTo` 与它的
`w`/`h`、`DashboardWidgetValidator.validateLayout`（连同「坐标不许负 / `x + w ≤ 24`」三条规则）、
`DashboardLayoutCodec.encodeLayout`、`DashboardLayoutService.normalized` 里那次 `applyTo`。

**不额外存 `order` 下标**：数组本来有序，再存一个下标就是两个真值。

**服务端不再持有任何排版知识**：`WidgetSize` 从「每档占几列几行的权威」缩成一个纯档位名枚举，
占格表（`WIDGET_SIZES`）只在前端；`normalized()` 因此只剩「缺省 `refresh` 补 60」一件事。
加一档时前端加一行表、后端加一个枚举常量，数值不必同步。

**老文档不需要迁移** —— 这一条本轮**已在本机实测**，不再只是推理。
`ObjectPropertyCodecProviderTest.legacyLayoutKeyIsIgnored` 手工造一份带 `layout` 的老文档
（走的是与真落库同一个注册表、同一条 `BsonDocumentReader` 路径），断言：读得出来、
`layout` 被跳过、再编回去时那个键消失。机制是 POJO codec 遇到类上没有的键**跳过**
（`DashboardWidget` 本来就挂着 `@JsonIgnoreProperties(ignoreUnknown = true)`），
而**老文档的数组顺序本来就是视觉顺序**（预置布局按视觉顺序写，保存路径走 `compactLayout`，
返回的正是 `(y, x)` 序）。也就是说第一次保存就把 `layout` 从文档里洗掉了。
唯一仍未覆盖的是真 Mongo 本身 —— 而它在这件事上只是把文档原样吐回来。

> 写这条用例时踩到一个坑，记下来：**嵌套 POJO 的 `id` 字段在 BSON 里的键是 `_id`，不是 `id`**
> （POJO codec 的 id 属性约定）。手工造文档时写成 `"id"` 不报错，只会静默得到一张没有 id 的卡片。
> 线格式上仍然是 `id`，因为 `DashboardLayoutCodec` 是手写编码、显式 `put("id", ...)`。

#### 2. 渲染换扁平 CSS Grid（拖拽的前提）

`.board` 从 `.board → nz-row → nz-col` 两层嵌套换成**扁平**的
`display: grid; grid-template-columns: repeat(24, 1fr); gap: 16px; align-items: start`，
每格 `[style.grid-column]="'span ' + p.span"`。

- 为什么必须扁平：拖拽的「让位」靠 CDK 把占位块在**兄弟节点之间**搬来搬去，
  跨行搬动会让 `nz-col` 跑出自己的 `nz-row`。
- 间距逐像素等价：`nz-row [nzGutter]="16"` 是「行 −8px 外边距 + 列左右各 8px 内边距」，
  视觉上就是 16px 的缝、两端与容器对齐，`gap: 16px` 一模一样。
- 行高仍是「一行里最高的那张卡决定」，矮的卡不拉伸（`align-items: start`），
  与 `nz-row nzAlign="top"` 同结果 —— 统计卡照旧自然高。
- 窄屏（`max-width: 767px`）改成 `.board { display: flex; flex-direction: column }`：
  flex 布局下 `grid-column` **整个失效**，一列一张。比原来的 `flex: 0 0 100%` 省事，
  也避开了「`grid-template-columns: 1fr` + 内联 `span 6` 会被浏览器补出隐含列」那个坑，
  不必用 `!important` 去盖模板上的内联样式。
- `NzGridModule` 的导入随之删掉（这一页不再有 `nz-row` / `nz-col`）。

#### 3. 编辑态画真卡片（尺寸一致是构造上成立的）

磁贴（`.tile` 与它的标题 / 摘要 / 窗口三段）整个删掉，编辑态与非编辑态渲染**同一个
`home-widget`**、传**同一个 `height`**。连带作废的代码（磁贴是它们唯一的消费者）：
`present()` 里的 `title` / `summary` / `timeWindow`、`widgetSummary` / `lineSummary` /
`catalogService` / `catalogDevice`，以及 `home.component` constructor 里那个只为磁贴摘要取
「设备显示名 + 产品规格」的 effect（连带 `DeviceDisplayService` / `DeviceSpecService` 两个注入）。

当初做磁贴是为了避开「canvas 吃点击、八张统计卡认不出谁是谁」，代价正是本轮要修的
「编辑态统计卡那一行会往上收一截」。现在这个代价由别的手段付：整卡可点（见下）+
编辑态描一圈虚线 + `cursor: move`。

#### 4. 添加卡片对话框 / 拖拽 / 整卡可点 / 对话框里删除

- **「添加卡片」**：工具条那 5 个类型按钮换成一个 `nzType="primary"` 的按钮，点开新组件
  `editor/widget.picker`（`nz-modal` 里 5 个 `nzHoverable` 的格子，一格一种类型）。
  选完关选择框、开配置框；新卡**落在末尾**（顺序模型的自然结果，要放哪儿拖一下）。
  词条 `添加卡片` 词典里本来就有 → **本轮零新增词条**，66 份仍是 518 键。
  类型选择框里**每种类型只有名字、没有说明文字**：多一句话就得为它新增 66 份词典的词条，
  而「统计数字 / 曲线图 / 数据分布 / 设备 / 服务」五个名字本身已经说清了是什么。
- **拖拽**：`.board` 上挂 `cdkDropList cdkDropListOrientation="mixed"`，每格
  `cdkDrag [cdkDragDisabled]="!editing()"`、`[cdkDropListDisabled]="!editing()"`，
  drop 时用 CDK 的 `moveItemInArray` 改草稿顺序。三处读源码确认过的细节：
  - **`mixed` 不能省**：默认的 `SingleAxisSortStrategy` 按单轴算所有兄弟的偏移量，
    折行的板子上会算错；`MixedSortStrategy` 走的是「把占位块插到光标下那张卡的前 / 后」，
    由浏览器重排 —— 这正是「其他卡片让位」。
  - **5px 阈值**（`DragRef.dragStartThreshold`）：不移动就是一次普通点击 →
    「整卡可点」与「整卡可拖」**不冲突，不需要额外的拖拽把手**。
  - **CDK 的默认预览是深拷贝那张卡**，canvas 拷不出来（会飘着一张白身子的卡）→
    给一个只画卡名的幽灵模板。注意 selector 是 `ng-template[cdkDragPreview]`
    （不是 `*cdkDragPreview`），而模板的上下文是 **CDK 自己给的对象**（不是 `@for` 作用域），
    所以卡名要 `[data]="{ title: p.title }"` 传进去、用 `let-title="title"` 取出来。
  - CDK 的 prebuilt CSS 这份工程没有引（`styles.less` 里没有 cdk 的 import）→
    `.cdk-drag-placeholder` / `.cdk-drag-animating` 两条自己写。
- **整卡可点 + 浮起**：`.cell` 上 `(click)` → 开配置框（只在编辑态生效）；
  浮起用 antd 自己的 `nzHoverable`（不抄它的阴影值），从 `home-widget` 往下转给五张卡片
  各自的 `<nz-card>`（与既有 `height` 输入同一条路由）。**只有编辑态传 true**：
  看数据时浮起却点不动，比不浮起更糟。
- **对话框里删除**：`widget.editor` 加 `removed` 输出，footer 变成
  `删除（nzDanger，靠左） | 取消 确认（靠右）`。删除**只动草稿**，不套确认气泡 ——
  「退出编辑」原样撤销。
- **上移 / 下移不做**（用户定的）：footer 只留三个按钮。代价是**键盘用户改不了顺序**，
  代码里也不再留 `moveInOrder` 那条备用路径。同理，老布局里手摆的空档与「架在别人下面的卡」
  在顺序模型下不存在了 —— 进入编辑态看到的就是按顺序的流式排布。

#### 测试

前端 `npx ng build` 通过、`npx ng test --watch=false` **446 条 / 23 个文件全绿**
（`home.grid.spec.ts` 重写成只测 `sizeOf` 与 `cardHeight` 两个函数；`home.folding.spec.ts`
那一族是取数折算、与排版无关，未动）。后端**整套 747 条全绿**，其中看板相关 175 条
（`DashboardCodecTest` 15 / `DashboardWidgetValidatorTest` 57 / `DashboardResourceTest` 17 /
`DashboardLayoutValidatorTest` 18 / `DashboardCatalogCodecTest` 5 / `WidgetDataServiceTest` 54 /
`ObjectPropertyCodecProviderTest` 9）。词典 66 份 × 518 键，键集合与顺序逐份一致。

#### 没验到的部分（要在你自己的环境里过一遍）

仍然**没有在浏览器里看过**。六件事：加卡片流程、拖拽让位、点卡改尺寸后立刻重排、
对话框删除后「退出编辑」能撤回、保存后尺寸与编辑态一致、以及**拖一张带 ECharts 的卡**
看有没有闪 / 错位（CDK 会把真卡片临时搬到 `<body>` 上，`ResizeObserver` 会 resize 两次）。
另外老文档那条虽然编解码层已实测，**真库还是建议拿一份改造前保存过的布局点一次**
（预期：正常读出来、顺序不变）。

### 2026-09-17 修订（十一）：首页归位 —— 删掉老那一屏，把「测试首页」改名为「首页」

**触发**：用户提出「1. 把当前的首页删除掉。2. 把『测试首页』改成『首页』，组件名称由
`HomeComponent` 改成 `DashboardComponent`，路径也由 `/main/home` 改成 `/main/dashboard`」。

这正是 §7.6 里挂起的那件事（原文：「菜单 label『测试首页』也先留着，**等这屏真接替 dashboard 时
再谈改名与改落地页**」），也是「命名约定」一节里承诺过的「将来这屏看板接替 `/main/dashboard` 时
整目录会重命名」。**这一刻到了。**

#### 删掉了什么

`src/app/pages/main/dashboard/**` 整个目录（8 个文件）—— 老那一屏固定看板：
`dashboard.component.{ts,html,less}`、`dashboard.charts.ts`、`dashboard.functions.{ts,spec.ts}`、
`dashboard.mock.ts`、`dashboard.routes.ts`。它是一屏写死的假能耗数据（`EnergyStats` /
`mockEnergyStats`），从 §2.1 起就被标明「视为待删的兄弟页面，新页面不依赖它的任何代码」。

核实过：**它在 `src/` 里没有任何别的消费者**（只有 `main.routes.ts` 一行相对导入）。

顺带清掉了它在全局样式里留下的孤儿：`styles.less` 深色模式那段的 `.tile` / `.tile-window` /
`.tile-meta` —— 磁贴在修订（十）删掉了，这三条覆盖规则当时漏了，而它们已经没有消费者。

#### 改名的范围（用户定：一路做到底）

| 旧 | 新 |
| --- | --- |
| `pages/main/home/` | `pages/main/dashboard/` |
| `home.component.{ts,html,less}` | `dashboard.component.{ts,html,less}` |
| `home.{grid,folding,charts,config}.{ts,spec.ts}` | `dashboard.{grid,folding,charts,config}.{ts,spec.ts}` |
| `home.routes.ts` · `HOME_ROUTES` | `dashboard.routes.ts` · `DASHBOARD_ROUTES` |
| 类 `HomeComponent` | 类 `DashboardComponent` |
| 选择器 `main-home` | `main-dashboard` |
| 选择器 `home-widget` · `home-widget-{note,window,picker,editor}` | `dashboard-widget` · `dashboard-widget-{note,window,picker,editor}` |
| 选择器 `home-{stat,line,distribution,device,service}` | `dashboard-{stat,line,distribution,device,service}` |
| 容器类 `.home` | `.dashboard` |
| 路由 `/main/home`，breadcrumb 与菜单 label「测试首页」 | 路由 `/main/dashboard`（**本来就在，只是换了个提供者**），label 与 breadcrumb「首页」 |

`main.routes.ts` 里 `home` 那一条删掉了；`dashboard` 那一条**字面上一个字都没动** ——
它的路径、breadcrumb「首页」、`DASHBOARD_ROUTES` 导入在改名后正好全是想要的形状，本轮改的只是
那个文件现在由谁提供。同理 `app.routes.ts` / `main.routes.ts` 的 `/main` → `/main/dashboard`
重定向、以及 `callback.component.ts` 登录后的缺省跳转，都**一行没改**就自动指向了本页。

**选择器不撞车**：`main-dashboard` 原来挂在即将被删的老组件上，删完正好空出来给本页用。

#### 三条核实过的事（不是推测）

1. **词典零改动**：66 份词典里 `测试首页` 出现 **0** 次、`首页` 出现 **66** 次。所以删掉那个菜单项
   既不留下孤儿键、也不用新增键，**518 键仍是 518**。顺带说明：那个 label 一直是没进词典的原文
   （`breadcrumb-translate.directive.ts` 与菜单都走 `translate.instant`，查不到就回退成键本身），
   切语言时它不会变 —— 换成「首页」之后这个隐藏了很久的小毛病就没了。
2. **`/main/home` 在整个工作区没有别的引用**：22 个仓库里翻过一遍，除了 `main.component.html`、
   `main.routes.ts` 与本页 JSDoc 之外一处都没有。所以**不留重定向**：全项目本来就没有通配路由
   （`app.routes.ts` / `main.routes.ts` 都没有 `**`），旧路径会直接匹配失败 ——
   与「这个页面不存在了」的语义一致。
3. **手机端有两个仓库在注释里指着老那一屏**（本轮**没动**，属于别的仓库）：
   `android-matrix/.../ui/home/{HomeDashboard.kt,HomeScreen.kt}` 与
   `iphone-matrix/Sources/Matrix/Views/Home/{HomeDashboard.swift,HomeView.swift}` 里写着
   「布局对齐 web 的 `pages/main/dashboard`」/「web 侧是 `pages/main/dashboard/dashboard.functions.ts`」。
   现在那个目录与文件都不在了 —— 那两屏的**参照实现**是「改造前 web 首页那一屏」这一件事没变，
   只是指向的路径得换个说法（或者直接说「老那一屏，已于 2026-09-17 删除」）。

#### 本文档怎么读

上面各「修订（N）」小节里写的 `home.*` / `HomeComponent` / `/main/home` **是当时的文件名，
保持原样不改** —— 它们记的是那一轮改了什么，回改成 `dashboard.*` 等于篡改历史。
从本节起，这一页的一切都叫 `dashboard.*`。

描述「现状/规格」的章节（§5.1、§7.1、§7.2、§7.3、§7.6、§8、§9、§10）已经**就地更新**成本轮之后
的名字；其中两处不只是改名，而是当年那条口径被本轮作废了：

- **§7.6** 从「落地页不变（用户确认）…两者并存…一行路由代码都不用改」改成「本页就是首页」；
- **§7.3 的响应式**那条：当年建议「在 `home.grid.ts` 里按视口宽度折算列数」，
  实际走的是修订（十）的媒体查询（`.board` 换 `flex-direction: column`），纯函数那边只留
  「档位 → 占格 / 高度」。文档原来那条建议已经改了。
- §7.1 还多改了一处：那里写着「`widget.picker.*` **没有建**」（修订（六）的口径），
  但修订（十）把它建出来了 —— 一并改成现状。

#### 测试

前端 `npx ng build` 通过（模板类型检查是本轮最强的一道网：漏改的 selector、import 路径、
`DASHBOARD_ROUTES` 都会在编译期就报出来）；`npx ng test --watch=false` **426 条 / 22 个文件全绿**
—— 比修订（十）那次少了 20 条 / 1 个文件，**正是随老那一屏删掉的 `dashboard.functions.spec.ts`**。

`src/` 里再 grep `home-` / `main-home` / `/main/home` / `HomeComponent` / `HOME_ROUTES` / `测试首页`
**零命中**（剩下的 `HomeOutline` 图标、`SpaceUtils` 的 `site: 'home'`、`project.component.ts` 的
空间类型 `'home'` 都是不相干的东西；`home3d` 是另一屏，本轮一个字没动）。

#### 没验到的部分（要在你自己的环境里过一遍）

浏览器**没看过**。五件事：登录后左栏只剩一条「首页」、点进去是自定义看板（不是老的能耗那屏）、
敲 `/main` 仍然重定向到 `/main/dashboard`、切语言后「首页」跟着变、以及编辑 / 拖拽 / 保存照旧可用
（本轮只改名，不该有任何行为变化）。

### 2026-09-17 修订（十二）：二维自由摆放 —— 坐标回来，带着行单位

**触发**是用户对「顺序即位置」的一次否定，原话：

> 左边一个大卡片，高度是 200，右边其实可以放 4 个小卡片，横着放 2 个，放 2 行。
> 现在用 nz-col 和 nz-row 做不到。
>
> 卡片拖动的时候，自由度最大，只要空间能放卡片的，就可以放下。……就是拖动布局的时候，
> 卡片不仅仅横向布局，也可以纵向布局占领空间（人眼看上去可以占领的空间，那卡片就可以拖过去占领）。

流式排布里第 2 行从「第 1 行最高的那张卡」下面才开始，右边那块**明明空着**的地方落不下去 ——
这不是 `nz-row` 特有的毛病，换成扁平 CSS Grid 之后同一个限制换了个形式留着。所以问题不在布局容器，
在**「位置由顺序决定」这件事本身**。

顺带要两个**统计卡专用**的矮档位：宽 6 格 / 宽 12 格、高 90px（最后定成 92，见下）。

#### 1. 用户定的两条口径

| 问题 | 定的口径 |
| --- | --- |
| 行高与间距 | **行高 92 / 间距 16**（不是字面的 90 / 20） |
| 拖到别人身上怎么让位 | **推开 + 上吸**：被压住的往下让，松手后整屏上吸（**只往上吸、不左右挪**） |

**为什么是 92 而不是 90**：间距一动，24 列的列宽会从约 43px 缩到约 39px，**所有卡片的宽度跟着变**。
而 `h × 92 + (h−1) × 16` 让改造前每一档的像素高度**一像素不差** —— 1 行 = 92、2 行 = 200
（= 原 `S` / `M`）、4 行 = 416（= 原 `L` / `XL`）—— 同时 `92 + 16 + 92 = 200`，
**两张一行高的卡竖着叠起来正好等于一张两行高的卡**，底边能对齐，正是用户要的「高度可以拼接」。
换句话说：90 是用户按眼睛报的数，92 是让这个数**在既有布局上零代价落地**的那个数。

#### 2. 档位表：`S` / `M` / `L` / `XL` → 六档

```ts
export const GRID_ROW_HEIGHT = 92;   // 38 → 92。GRID_GAP 仍是 16
export type WidgetSize = 'S1' | 'M1' | 'S' | 'M' | 'L' | 'XL';
```

| 档位 | w | h | 像素高 | |
| --- | --- | --- | --- | --- |
| `S1` | 6 | 1 | 92 | 统计专用 |
| `M1` | 12 | 1 | 92 | 统计专用 |
| `S` | 6 | 2 | 200 | 与原 `S` 逐像素相同 |
| `M` | 12 | 2 | 200 | 与原 `M` 相同 |
| `L` | 12 | 4 | 416 | 与原 `L` 相同 |
| `XL` | 24 | 4 | 416 | 与原 `XL` 相同 |

**档位名里那个数字就是「占几行」**（`S1` = 「`S` 的一行版」）—— 因为现在行是**可以拼接的单位**，
「占几行」比「第几档」更能说明它为什么长这样。

**按类型限档位**（`DASHBOARD_SIZE_CHOICES`）：统计卡只给 `S1` / `M1`。理由是它**唯一**
「没有卡头、按内容自然高」—— 实测 `nz-statistic` 标题 22 + 间距 4 + 数字 37.7 = 63.7px，
加 `nz-card nzSize="small"` 的 body padding 24 + 边框 2 = **≈ 90px**。也就是说 92px 的行单位让它
**一个 CSS 字都不用改**就正好是一行；而它今天被塞在 200px 的格子里，下面空着 110px，这一改顺带修掉。

**加一档要改五处**（上一轮是四处，这轮找到第五处）：前端 `WidgetSize` 类型、`WIDGET_SIZES` 表、
`DashboardLayoutCodec.ts` 的 `SIZES`、后端 `WidgetSize` 枚举、以及**后端那句手抄的报错文案**
`"size must be one of S/M/L/XL"`（在 `DashboardWidgetValidator` 里）。最后这处已改成从
`WidgetSize.values()` 生成 —— **它果然在 `S1` 落地当天就失效了**（两条测试红了），
不是推测，是实测。

#### 3. 线格式：坐标回来了，但只回两个平铺的整数

`DashboardWidget` 加 `public Integer x; public Integer y;`（**可选**，为了让老文档读得进）。

**为什么不是复活修订（四）那个 `layout: {x, y, w, h}` 对象**：那一版里 `w`/`h` 是**档位知识的
第二份副本**，服务端因此得靠 `WidgetSize.applyTo` 按档位覆盖一次、再在别处校验 `x + w ≤ 24`，
而修订（十）删它的理由正是「服务端硬留一份会过期的真值」。这一轮 `w`/`h` **仍然只在前端的
`WIDGET_SIZES` 里**，一个字节都不上行。`x`/`y` 不一样：它是**用户摆的**，不是从档位推出来的，
服务端拿不出第二个人来算它 —— 所以只加这两个。

**用 `Integer` 而不是 `int`**：「没有这个键」必须能与 `0` 区分开，否则老布局会被读成
「所有卡片都摆在左上角」。

**老文档不需要迁移**（这条上一轮已实测，本轮口径不变）：库里的老文档带着嵌套的 `layout` 对象，
它是**不认识**的键，POJO codec 跳过；而平铺的 `x`/`y` 现在**是认识的键**。两条规则互不相干，
用例也分着钉（`legacyLayoutKeyIsIgnored` / `dashboardWidgetConfigRoundTrips`）。

**老文档的坐标由前端补**：`decode` 时若任一坐标缺失，按数组顺序跑一次 `flowPlace` 整份重铺。
`flowPlace` 复刻的就是改造前浏览器流式排开的那个样子（写成用例钉着，含一个手算的例子
`[M,S,S,S]` ⇒ 第四张 `S` 落到 `(0,2)`），所以**存量布局打开后长相完全不变**，
第一次保存坐标才落库。三处 `layout.set` 都走 `adopt()`，理由同此。

#### 4. 排版：从「流式」改成「每一格都显式定位」

`.board` 加 `grid-auto-rows: 92px`，每格改成
`[style.grid-column]="x + 1 + ' / span ' + w"`、`[style.grid-row]="y + 1 + ' / span ' + h"`。

跨 `h` 行的格子正好 `h×92 + (h−1)×16` px，与 `cardHeight(h)` **逐像素相同**，
所以 `[height]` 原样保留（ECharts 仍要一个有界父级）。

**删掉了 `.board` 上的 `align-items: start`**：它原来是「一行里矮的卡片不拉伸到最高那张的高度」，
显式行之后这个需求消失了（每张卡的行跨度就是它的高度）。留着反而有害 —— `align-self: start`
会让 `.cell` 只取内容高、不占满自己那 `h` 行，编辑态的虚线框会比卡片矮一截。

**统计卡仍然不传 `[height]`**（`widget.host.html` 的 `@case ('stat')` 是全页唯一的特例）：
`S1` 下不传也只差 2px（90 vs 92），看不见；而 `height: 100%` 会让**存量**那张 200px 格子的
统计卡把数字顶到 110px 空白的中段 —— 那正是这张卡最不该长的样子。

#### 5. 拖拽：CDK 只管「拖动」，让位是自己的算法

`.board` 上留着 `cdkDropList` 但打开 `[cdkDropListSortingDisabled]="true"` ——
读 CDK 源码确认过：`DropListRef._sortItem` 在 `sortingDisabled` 时**直接 early-return**，
CDK 从此一个 DOM 都不搬；但**占位块照旧建立**，而且它是 `parent.replaceChild(placeholder, element)`，
**替换掉 DOM 里那个 `.cell`**。这决定了两件事：

- **`@for` 必须仍按 `widgets()` 的数组顺序迭代**，位置只走绑定。被拖的那个节点正被 CDK 持有着，
  Angular 一挪它卡片就会跳。写成「按重排后的顺序迭代」是错的。
- **占位块要隐掉**（`opacity: 0`）：它是克隆出来的静态节点，停在**原位**，而其它卡片已经实时
  重排了 —— 留着它就是一块过期的灰。用 `opacity` 而不是 `display: none`，因为后者会让网格项
  退出隐式行尺寸的计算，松手一瞬间屏幕会抖。

让位全交给 `placeAt`，于是**拖动中看到的就是松手后的样子**。落点框是网格里的**一个正常成员**
（同样的 `grid-column` / `grid-row` 绑定 + `pointer-events: none` + `z-index: 1`）——
跨几行就多高，**不需要量任何像素**。

两处细节是踩出来的：

- **被拖的那张卡是钉住的**：`placeAt` 只把**别人**推开、然后上吸别人。若连被拖的也一起吸，
  **往下拖就什么也做不成**（一松手就被吸回去），「纵向占领空间」直接落空，落点框也会撒谎。
- **`dragMoved` 每像素都发**：目标格子没变就直接 return，别白算一遍整屏。

#### 6. 后端

改动面比预想小 —— **REST DTO 一个字都没动**（`DashboardLayoutDto.widgets` 直接复用实体）。
落地的是：实体两个字段 + 枚举两个常量 + 校验里加一条坐标规则 + codec 多发两个键 +
`PresetLayouts` 统计卡换 `S1`。`normalized()` **方法体一个字没改**（仍然只补缺省 `refresh`），
只改了它的注释：**位置与顺序都原样保留，服务端不排序、不补位**。

**校验只做「能给的那部分」**：给了坐标就必须两个都给、`0 ≤ x ≤ 23`、`y ≥ 0`。
**重叠与 `x + w ≤ 24` 故意不校验** —— 服务端没有 `w`，做不了。这一条写进了代码注释里，
免得下一个读的人以为漏了（也免得有人为了「补上这个校验」而把 `w` 请回来）。

**预置布局一个坐标都不给**，只把统计卡从 `S` 换成 `S1`。给不了的理由是硬理由：坐标得配着 `w`
一起才对（写重叠了就摆出两张叠在一起的卡），而 `w` 只在前端 —— 前端哪天把 `S1` 从 6 列改成 8 列，
手写的坐标表就会**静默地**摆出重叠的卡片。交给 `flowPlace` 反而永远自洽。
首屏因此从 12 行 632px 变成 5 行 `92 + 16 + 200 + 16 + 200 = 524px`。
**只影响从未保存过的空间**。

#### 7. 存量布局会变成什么样

- **位置不动**：`flowPlace` 复刻的就是改造前的流式排布，打开是同一个样子。
- **统计卡仍按存的档位渲染**：库里存着 `S`（200px）的统计卡不会自己变成 `S1`。
  渲染侧**一律不管，按存的档位渲染**，不悄悄改用户的布局；只有打开编辑器时那个下拉只列
  `S1` / `M1`，用户保存即落地成合法档位。**静默改尺寸比留一个旧档位更坏**。
- 用户想让它变紧凑，拖一下或改一次尺寸保存即可。

#### 8. 没做

- **不做横向压缩**：上吸只往上，不左右挪（用户明确要的「只往上吸」）。横向留白是用户的自由。
- **不做拖拽改高度**（拉边框那种）：行单位化之后它有地方落了，但那是另一件事，仍在 P3。
- **不动窄屏折行策略**：仍是一列一张、按 `(y, x)` 阅读顺序，卡片保持自己的档位高度。
- **不动取数配对**：读数一直按 `widget.id` 配，与坐标无关，这条链路一个字没改。

#### 测试

前端 `npx ng build` 通过；`npx ng test --watch=false` **469 条 / 22 个文件全绿**
（修订（十一）那次是 426 条，多出来的 43 条主要是 `dashboard.grid.spec.ts` 的坐标运算与
`DashboardLayoutCodec.spec.ts` 的坐标读写）。

后端 `./mvnw -o test` **752 条全绿**。中途红过两条 —— 就是上面说的那句手抄的档位名单，
**它按预期在 `S1` 落地当天失效了**。

#### 没验到的部分（要在你自己的环境里过一遍）

浏览器**没看过**，七件事：

1. 拖一张卡到大卡右边那块空处 → **落得下去**，且两张矮卡竖着叠起来与大卡**底边对齐**；
2. 拖一张卡压到别人身上 → 被压的往下让；松手后整屏上吸，留不出竖向空洞；
3. 拖动中落点框跟着走、跨几行就多高；松手后卡片与框**严丝合缝**；
4. 编辑一张统计卡 → 尺寸下拉**只有两个**；别的卡片 → **有四个**；
5. 统计卡在 92px 里**放得下**（标题 + 数字不裁切）；
6. 打开一份**旧布局**（没有坐标的）→ 长相与改动前**一模一样**；
7. 窄屏（<768px）→ 仍是一列一张，顺序是 `(y, x)` 阅读顺序。

**这一轮动了后端字段，前后端必须一起上**：老后端不认 `x`/`y`（Mongo 侧未知键是丢掉的，
不报错但存不下来），表现是「拖完保存、刷新回到原位」这种既不报错又不对的形态。

---

### 2026-09-18 修订（十三）：拖动中的「可以放置 / 不可以放置」

修订（十二）把二维自由摆放做出来了，但拖起来还是**不知道能放到哪**，两条口径都要改：

1. 拖动时**卡片原来的位置有个虚线框**（占位块），而**落点反而没有框** —— 用户的原话是
   「这样拖动的时候，就知道可以卡片可以放到哪里」；
2. 让位策略是**「让位 + 上吸」**，改成**只让位、不上吸**；
3. 拖动中要能看出**当前位置放不放得下**（幽灵卡换底色）。

#### 1. 那个「原来的位置」的虚线框，其实是个 bug

用户说「卡片原来的位置用了一个虚线框，这个没错」—— **它没错，但它不是设计出来的**：
占位块当时是 `opacity: 0` 隐掉的，页面上那个框来自 `dropOutline`。

而 `dropOutline` 是这么写的：

```ts
const item = placementsOf(this.widgets()).find((i) => i.id === target.id);
return item ? { x: item.x, y: item.y, w: item.w, h: item.h } : null;
```

`placementsOf(this.widgets())` 是**卡片自己的坐标**（`target.x` / `target.y` 从头到尾没被用过）。
于是那个框**永远画在卡片出发的那一格、跟着指针一动不动**。用户看到的「原来的位置的虚线框」
就是它；他以为的「落点框」从来没出现过。

修掉之后 `dropOutline` 直接就是 `dragTarget` 自己，而「从哪来」这个信息得由占位块自己给 ——
所以这一轮**顺手把占位块从「隐掉」改成「灰虚线框」**（里面的深拷内容 `visibility: hidden`，
因为那份克隆里的 ECharts canvas 是画不出图的）。于是拖动中是两个框配对读：

| 框 | 颜色 | 意思 |
| --- | --- | --- |
| 占位块（`.cdk-drag-placeholder`） | 灰虚线 | 它从哪来 |
| 落点框（`.drop-outline`） | **绿**虚线 | 它要落到哪 |

#### 2. 去掉上吸

`placeAt` 的最后一行原来是 `return compact(settled, id)`。删掉它就完了：

```ts
// 让完就停：**不调 compact**。上吸会让用户刚摆好的位置自己往上跑
return settled.sort(compareYX);
```

去掉的理由是**用户摆的位置会自己跑**：他特意把卡拖到下面腾出来的空位上（比如想在那儿留出
纵向的空间），一松手整屏吸上去，白摆一次。「上吸」在流式排布时代是对的（那时没有位置，
只有顺序），在自由摆放时代是**擅自改版式**。

`compact` 本身留着（连同 `pinned` 参数一起删掉参数 —— 拖拽那条路整个不吸了，
`pinned` 就没有调用方了），今天只有**一处**调用：**删掉一张卡**之后收掉它留下的洞。
只有那一次那个洞不是任何人摆出来的，收掉才说得过去。

同一口径也适用于**编辑器里改档位**：`commitEditor` 走的是同一个 `placeAt`，所以卡片改小
之后腾出来的地方也留着（不上吸）。用户自己把卡改小，剩下的地方该由他决定放什么。

#### 3. 放不下 = 越界，不是「压到别人」

判据只有一条 `fitsAt(x, y, w)`：`x ≥ 0 && y ≥ 0 && x + w ≤ 24`。**压在别人身上算放得下**
（被压的往下让，这正是让位存在的意义）—— 要是压住了就算放不下，用户永远叠不出纵向的版式。

| 状态 | 幽灵卡 | 落点框 | 松手 |
| --- | --- | --- | --- |
| 放得下 | 绿底 `#f6ffed` / `#52c41a` 边 | 绿色虚线 | 落在框里 |
| 放不下 | 红底 `#fff2f0` / `#ff4d4f` 边 | 不画 | **弹回原位，草稿一个字节都不动** |
| 还没跨格 | 白底（原样） | 不画 | —— |

**两个状态都要有底色**：只给「放不下」上红的话，用户看到一张白卡时并不知道那是「可以放」
还是「还没判」。

为此 `dragMoved` **不再夹边界**（原来是 `clamp(origin.x + dc, 0, 24 - w)`）：
夹了就永远看不出越界，用户会以为松手能落在那儿。越界照样记进 `dragTarget`，
由 `dragState` 判红，`dragEnded` 直接丢弃这一拖 —— CDK 自己会把幽灵卡弹回原位。

`dragTarget` 因此多带 `w` / `h`（判界要知道这张卡多宽），`dropState` / `canDrop` 两个 computed
都由它算出来，整屏预览（`placements()`）在越界时**一个字节都不重算**。

#### 4. 幽灵卡的底色怎么做到「跟着指针实时变」

幽灵卡是 `cdkDragPreview` 模板，它的 `[data]` 是**拖动开始那一刻的快照**（`CdkDragPreview`
没有 `ngOnChanges`，`data` 之后不再推进上下文），所以标题能这么传、状态不能。

能用的办法是**直接绑组件信号**：这个预览视图由 CDK 用 `createEmbeddedView` 创建在
**`.cell` 的 `ViewContainerRef`** 上（只有它的 DOM 节点被挪到 `<body>`），而它声明在组件的模板里，
所以「组件的成员」在这个视图里读得到（`@for` 的局部变量 `p` 读不到，这就是两个绑定走法不同的原因），
而且**后续每一轮变更检测都会重算它**，底色于是跟着指针走。

#### 5. 改了什么

| 文件 | 改动 |
| --- | --- |
| `dashboard.grid.ts` | `placeAt` 去掉 `compact` 调用（返回 `settled.sort(compareYX)`）；新增 `fitsAt(x, y, w)`；`compact` 去掉 `pinned` 参数；文件头「让位的口径」重写 |
| `dashboard.component.ts` | `dragTarget` 带 `w`/`h`；新增 `dropState`（`idle`/`ok`/`blocked`）与 `canDrop`；`dropOutline` 改用 `dragTarget`（bug 修复）；`dragMoved` 不夹边界；`dragEnded` 越界时丢弃这一拖；`placements()` 越界时不预览 |
| `dashboard.component.html` | 幽灵卡加 `[class.drag-ghost-ok]` / `[class.drag-ghost-blocked]`；落点框与 board 的注释跟着改 |
| `dashboard.component.less` | `.drop-outline` 蓝 → 绿；`.drag-ghost-ok` / `.drag-ghost-blocked` 两个新类；`.cdk-drag-placeholder` 从 `opacity: 0` 改成灰虚线框 |
| `dashboard.grid.spec.ts` | 新增 `fitsAt` 四条；`placeAt` 新增「让完就停」一条；`compact` 删掉 `pinned` 那条 |

前端 **472 条全绿**（469 → 472：`fitsAt` +4、`placeAt` 的越界三条合并成一条 -1、
`compact` 的 `pinned` 那条删掉 -1、新增「让完就停」+1），`ng build` 干净。
**后端一个字节都没动**，这轮纯前端。

#### 没验到的部分（要在你自己的环境里过一遍）

浏览器**没看过**，四件事：

1. 拖动中**幽灵卡会变色**：往大卡右边的空处拖 → 绿底；往右边界外拖 → 红底；
2. 松手时如果是红的 → **弹回原位**，版式一动不动；
3. 拖动中是**两个框**：灰色的停在原处（从哪来）、绿色的跟着指针（要落到哪），松手后卡片与绿框
   严丝合缝；
4. 拖一张卡压到别人身上 → 被压的往下让，**松手后不再上吸**（下面留的空就留在那儿）。

第 1 条最值得盯：它压在「预览视图会被重新变更检测」这个判断上（见上面第 4 节）。
万一底色不跟着变，是 CDK 那边的视图没被重算 —— 那就得改成在 `dragMoved` 里直接改 DOM。

---

### 2026-09-18 修订（十四）：档位补到九档 + 换成 `W{列}H{像素}` 的名字 + 编辑器的两个下拉

用户提了三件事（原话）：

> 卡片的高度，目前是：1、92，统计卡片专用 2、200，非统计卡片用，2 个统计卡片高度 +
> 1 个卡片间隔高度 = 200 3、416 …… 现在，感觉需要增加一个高度，308，正好是：
> 3 个统计卡片高度 + 16 间隔，或者是：一个非统计卡片高度 + 1 个间隔高度 + 1 个统计卡片高度。
> 你觉得呢？如果加上这个高度，那感觉界面上要调整下：创建或编辑卡片时，尺寸就应该用 2 个下拉框了。
> 另外，后台服务的 `DashboardWidget` 里的 `size`，用 S, M, L, XL 来标记可能不太好读，
> 可以用：`W6H200`, `W12H416` 可能更好。

三件事他都定了口径：**① 补 `W24H200`；② 两个下拉框的文字走纯数字；③ 名字用
`W6H200` / `W12H308` 这个格式（不带下划线）**。

#### 1. 308 不是「再加一档」，是等差数列里缺的那一级

`cardHeight(h) = h×92 + (h−1)×16 = 108h − 16`，步长 108（一个行高 + 一道缝）：

| h | 1 | 2 | **3** | 4 |
| --- | --- | --- | --- | --- |
| 像素高 | 92 | 200 | **308** | 416 |

所以 308 不是新发明的尺寸，而是这条梯子上**本来就该有的一级** ——
`3×92 + 2×16 = 308`，也正是用户说的「一张 200 + 一道缝 + 一张 92」。
补上它之后，「矮卡竖着叠 = 高一档」这条性质在**每一级之间**都成立。

**它的主顾是大卡片，不是统计卡**：三张统计卡叠起来本来就 308，统计卡的档位不需要它。
要它的是「比 200 高一档、又不到 416」的曲线图。

补齐之后共**九档**（§D4 有完整表）：`W6H92`、`W12H92`、`W6H200`、`W12H200`、`W24H200`、
`W12H308`、`W24H308`、`W12H416`、`W24H416`。加了两档宽度上的取舍：

- **308 只给 12 宽与 24 宽**，没有 6 宽的 308 —— 6 列约 338px 宽配 308px 高是个方块偏窄的
  形状，画曲线不成样子；而「竖着叠两张矮卡」本来就能拼出同样的高度，6 宽不需要它；
- **`W24H200`（整宽的矮横幅）是用户点名补的**：24 宽此前只有 416 那一档，
  想要一条「一排读数 / 一行状态」的整宽矮卡没有档位可选；
- 反过来，**24 宽没有 92 那一档**：整宽的一行高更像一条分隔线，不是一张卡。

#### 2. 名字换成 `W{列}H{像素}`，里面那个数字是像素不是行数

原来叫 `S1` / `M1` / `S` / `M` / `L` / `XL`，两个毛病：

1. **字母早就不成立了**：`S1` 是 6 宽 1 行、`S` 是 6 宽 2 行、`M1` 是 12 宽 1 行、
   `L` 与 `XL` 同高不同宽 —— 字母既不单调对应宽度、也不对应高度，
   「S 比 M 小」这句话从引入矮档位那天起就是错的。加到九档之后更没法用字母排。
2. **`S1` 里那个数字是行数**，而**行高改过一次**（38 → 92，每一档的行数都跟着减半）：
   名字里写行数的话，那一次改动就得迁移库里每一个文档。**像素高从头到尾没变**
   （200 还是 200、416 还是 416），所以名字里放像素：那一次改动一个字节都不用迁。

所以名字就是 `W6H200` = 「6 列宽、200 像素高」，**名字说的就是这一档的两件事**。
`dashboard.grid.spec.ts` 有一条用例把这句话钉死：

```ts
for (const size of Object.keys(WIDGET_SIZES) as WidgetSize[]) {
  expect(size).toBe(`W${WIDGET_SIZES[size].w}H${cardHeight(WIDGET_SIZES[size].h)}`);
}
```

改档位表却忘了改名字（或反过来）时它当场红 —— 一个名叫 `W6H200` 却占 8 列的档位，
比一个难读的名字坏得多。

**没加下划线**（用户问过 `W6_H200` 是不是更好读）：`H` 本身就是分隔符，
「W + 数字 + H + 数字」没有任何一种读法会切错，下划线不解决歧义，只是让每个值多一个字符，
而它出现在库里、线格式里、枚举里、测试里。工程里已有的 `S1` / `M1` 也不带下划线。

#### 3. 旧名怎么办：两张翻译表，都必须有

库里存着的布局写的是旧名。**认不出来就会落到兜底档位 —— 那是一次静默的改尺寸**
（用户没动过的卡片自己变了大小，界面上查不出原因），正是前面几轮反复写过的那条最坏形态。
所以两张表都要有，谁也替不了谁：

| 表 | 位置 | 管什么 |
| --- | --- | --- |
| `LEGACY_SIZES` | 前端 `DashboardLayoutCodec` | **存量的文档读出来是什么**（读的时候翻译成新名，用户下次保存库里就落成新名了） |
| `LEGACY_NAMES` | 后端 `WidgetSize.of` | **旧客户端写进来的是什么**（开着旧标签页 / 浏览器缓存着上一版脚本的用户保存时发的仍是旧名） |

后一张是这一轮**新加的**：不认的话整个 `PUT` 被拒（400，文案说尺寸不认识），
用户那一屏的改动白做，而他唯一的线索是一句看不懂的报错。认下来则是一次无声的、
几何上等价的翻译 —— `S` 与 `W6H200` 都是 6 列 2 行，翻译过去看不出任何变化。

映射（六条，两侧内容一致）：

| 旧名 | 新名 | 占格 |
| --- | --- | --- |
| `S1` | `W6H92` | 6 列 1 行 |
| `M1` | `W12H92` | 12 列 1 行 |
| `S` | `W6H200` | 6 列 2 行 |
| `M` | `W12H200` | 12 列 2 行 |
| `L` | `W12H416` | 12 列 4 行 |
| `XL` | `W24H416` | 24 列 4 行 |

**不需要迁移脚本**：读的时候翻译，用户下一次保存就落成新名了。

#### 4. 编辑器：尺寸从「一个下拉」变成「宽 × 高两个」

九档塞进一个下拉，是让人在一张九行的菜单里找那两个数。拆成两个之后宽度只有 2–3 个选项、
高度只有 1–3 个，用户先定形状再定大小。

**两个下拉的显示文字都是纯数字**（宽是列数、高是像素），中间那个 `×` 是写死的字面量 ——
**这一轮一个新词条都没加，66 份词典一份都不用动**。（为此专门查过：词典里没有
`宽度` / `高度` 这类键，新加就意味着 66 个文件各加一行、且顺序必须一致。）

**高度选项跟着宽度联动**，而不是「列出全部组合再禁用几个」：

```ts
widthChoices(type)          // 统计卡 [6, 12]；其余 [6, 12, 24]
sizeChoices(type, width)    // 6 宽 → [W6H200]；12 宽 → [W12H200, W12H308, W12H416]；24 宽 → [W24H200, W24H308, W24H416]
```

不列禁用项是因为**用户不该先撞一次墙才知道 6 宽没有 308**。

`sizeAllowed(type, size)` 的界线写成「**恰好一行**」与「**一行以上**」的判定
（`type === 'stat' ? rows === 1 : rows > 1`），不是一份手抄的档位名单：加新档位时它不用改，
更不会出现「加了档位却忘了加到某个类型的名单里」那种静默的漏。

**换宽度时高度取最接近的那一档**（`setWidth`）：档位不是笛卡尔积（6 宽没有 308），
拿行数去配会在 6 宽上撞空；取最近的则任何一次换宽度都落在合法档位上、高度又不跳变 ——
从 12×308 换到 6 宽得到 6×200（而不是掉到 6×92 或干脆没得选）。

**存量卡片（旧规则下存下的、按现在的规则选不出来的档位）**：渲染侧一律按存的档位渲染、
不悄悄改用户的布局；编辑器的两个下拉把当前值**补进选项**，免得显示成空白
（空白会让人以为卡片没有尺寸，用户一保存还会莫名其妙地被改小）。

#### 5. 加一档要改的地方（从五处到六处）

上一轮数出来五处，这一轮变成六处 —— 档位名这件事多了一个「名字必须是真的」的约束：

1. 前端 `WidgetSize` 类型（`DashboardLayout.ts`）
2. 前端 `WIDGET_SIZES` 表（**唯一真值**）
3. 前端 codec 的 `LEGACY_SIZES`（只在改名时新增一行，加档位不用动）
4. 后端 `WidgetSize` 枚举
5. 后端校验文案 —— **从枚举生成**（`WidgetSize.values()`），加档位自动更新，不用手改
6. **档位名本身**（名字里带着 w 与像素高，改表就得改名字）—— 由上面那条 spec 钉住

#### 6. 改了什么

| 文件 | 改动 |
| --- | --- |
| `DashboardLayout.ts` | 九档的新名字与新表；`DASHBOARD_SIZE_CHOICES` 删掉，换成 `sizeAllowed` / `widthChoices` / `sizeChoices` 三个纯函数；`DASHBOARD_DEFAULT_SIZE` 跟着换名 |
| `DashboardLayoutCodec.ts` | `SIZES` 换成 `LEGACY_SIZES` + `readSize`（新名 → 旧名 → 兜底 `W6H200`） |
| `dashboard.grid.ts` | `sizeOf` 兜底档位换名；注释里的旧名说明改成「旧名在这里认不出来是对的，codec 负责翻译」 |
| `widget.editor.ts` | `sizes` computed 换成 `width` / `widths` / `heights` 三个 computed + `setWidth` |
| `widget.editor.html` | 尺寸那一格改成两个 `nz-select` 中间夹一个 `×` |
| `widget.editor.less` | `.size`（flex 三等分）/ `.times` 两条新类 |
| `WidgetSize.java` | 九个枚举常量 + `LEGACY_NAMES` 旧名表；类注释重写（名字的含义、为什么用像素、为什么有故意不存在的组合） |
| `PresetLayouts.java` | `S1` → `W6H92`、`M` → `W12H200`；类注释跟着换名 |
| `DashboardWidget.java` | `size` 的 `@Schema` 改成新名字格式 + 说明旧名仍收 |
| `DashboardWidgetValidator.java` | 只改了那句注释（文案本来就是从枚举生成的） |
| 前端两个 spec + 后端五个测试类 | 换名；新增：名字与表一致、`sizeAllowed`/`widthChoices`/`sizeChoices`、六个旧名的映射、后端「旧名仍收」 |

前端 **479 条全绿**（472 → 479），`ng build` 干净；后端 **753 条全绿**（752 → 753），
`BUILD SUCCESS`。

#### 没验到的部分（要在你自己的环境里过一遍）

浏览器**没看过**，三件事：

1. 编辑一张**统计卡** → 尺寸两个下拉：宽只有 `6` / `12`，高只有 `92`；
2. 编辑一张**曲线卡** → 宽 `6` / `12` / `24`；选 6 时高度**只剩 `200`**（其余两档消失），
   选 12 时高度有 `200` / `308` / `416`；
3. 把宽度从 12 换成 6（当前高度 308）→ 高度自动落到 `200`，卡片不跳变、也不报错。

**另外**：库里如果有一份**改名前存的布局**，打开它时卡片尺寸应当**一个都不变**
（旧名被翻译成占格相同的新名）—— 这条只有真库才验得了。

### 2026-09-18 修订（十五）：预置布局的非统计卡改成三行 308

接修订（十四）。那一轮把 308 做成了「可以选」，但**预置布局里一张卡都没用它** ——
九个档位里有三个（`W12H308` / `W24H308` / `W24H200`）在用户改之前永远不会出现。
用户提出：**预置（默认）的非统计卡，高度设成 308**。

改的是一处**常量**，但要说清它改的是哪一份「默认」—— 这个名字底下藏着两份东西：

| 名字 | 是什么 | 这一轮 |
| --- | --- | --- |
| `PresetLayouts.build()` | **从未保存过的空间**读到的整屏布局（4 张统计 + 4 张非统计） | ✅ 改成 `W12H308` |
| `DASHBOARD_DEFAULT_SIZE` | 用户**新加一张卡**时给的档位（`line` / `distribution` 是 `W12H200`） | ❌ 不动 |

前者改完，新用户/存量用户**删掉布局再打开**看到的第一屏就是三行的曲线与分布；
后者改完，用户自己加的那张卡一落地就是 308 高（少一步手动调）。本轮只做前者。

#### 1. 改了什么

| 文件 | 改动 |
| --- | --- |
| `api/dashboard/PresetLayouts.java` | `distribution` / `line` 两个 helper 的 `WidgetSize.W12H200` → `W12H308`；类注释里的首屏算式重写 |
| `DashboardCodecTest.java` | 第 73 行注释与 `encode_presetWidgetsAreInVisualOrder` 里那条断言跟着换 |
| 本文件 §6.5 | 预置表 4 行、首屏算式、以及「为什么不是 200 / 不是 416」 |

**统计卡那 4 张一个字没动**（仍 `W6H92`）：三行高是给「有卡头、有坐标轴」的卡用的，
统计卡没有卡头、按内容自然高约 90px，给它 308 只会空出一大片。

#### 2. 首屏高度

```
改前：92 + 16 + 200 + 16 + 200 = 524px   （5 行）
改后：92 + 16 + 308 + 16 + 308 = 740px   （7 行）
```

「排满、没有空洞」这条性质**没变**（4 张窄卡一行、4 张宽卡两行），变的只是那两行更高。

#### 3. 为什么不是 416

`W12H416` 也在可选集合里，但预置首屏会变成 `92 + 16 + 416 + 16 + 416 = 956px` ——
**一屏只装得下一行卡**，第二行要滚。预置布局是「这东西做完了」的第一印象，
而 740px 在常见 1080p 视口里（扣掉顶栏与边距约 800~900px）正好露出第二行的卡头。

#### 4. 影响范围

**只影响从未保存过的空间**。存过布局的空间读的是自己那份文档，一张卡的档位都不会被悄悄改掉 ——
要看新预置，得主动「恢复默认」（编辑态里那个按钮，§6.4）。这一点与修订（十二）统计卡换档位同口径。

#### 5. 没验到的部分

后端 **753 条全绿**（`BUILD SUCCESS`）。浏览器里**没看过**：

1. 一个**从未保存过布局**的空间打开 `/main/dashboard` → 首屏是「4 张统计卡一行 + 2 张分布一行 +
   2 张（分布 + 曲线）一行」，每张非统计卡 **308px 高**，没有空洞；
2. 展开编辑 → 「恢复默认」→ 确认换成的是**新**预置（三行高），不是旧的 200；
3. 已经存过布局的空间打开 → **与改动前完全一样**（不受影响）。

### 2026-09-18 修订（十六）：补第 10 档 `W6H308`

接修订（十四）（十五）。用户提出：**「6 宽也补一个 308」**。

修订（十四）里 6 宽是**故意留空**的，理由写在 §D4：「那样的图太窄太高、不成形状，
而竖着叠两张矮卡就能拼出同样的高度」。**这条理由被否掉了**，两个原因：

1. 它只对**曲线**成立。饼图不看宽高比，`338×308` 放一张饼很合适 —— 而 6 宽原先
   只有 200 一档，「窄而高」这个形状根本选不出来。
2. 它是**等差数列里的一级**，按同一个阶梯补比按形状补更自洽：`W6H308 ≈ 338×308` 正好是
   「三张统计卡竖着叠起来」那一条（`92 + 16 + 92 + 16 + 92`），底边与 `W12H308` 对齐。

于是档位从**九档变成十档**。**6 宽的 416 仍然没有**（那条理由在这里还成立：四张统计卡叠
够 416 高，而 338×416 是一根细条，曲线与饼图都不好看）—— 所以现在的缺口只剩
「6 宽 × 416」与「24 宽 × 92」两个，§D4 的表已按此改。

#### 改了什么

| 文件 | 改动 |
| --- | --- |
| `DashboardLayout.ts` | 类型加 `'W6H308'`、表加 `W6H308: { w: 6, h: 3 }`；类注释里「故意不存在的组合」那段改成只否 416，并写清 308 是后补的 |
| `widget.editor.ts` | 只改了注释里的档位数（九档 → 十档，6 宽「只有 200 一档」→「两档」） |
| `WidgetSize.java` | 加枚举常量 `W6H308`；类注释的表从九档改十档 |
| `DashboardWidgetValidatorTest.java` | 手抄的 `SIZE_NAMES` 加 `W6H308` |
| `dashboard.grid.spec.ts` | `sizeChoices('line', 6)` 的期望 `['W6H200']` → `['W6H200', 'W6H308']`（其余断言都是遍历 `WIDGET_SIZES` 或由它推出来的，不用改） |

**「加一档要改几处」这条经验因此又验证了一次**：类型、表、枚举是**必须**改的三处；
`SIZE_NAMES` 那条手抄的名单会**故意报红**（它是「名单真的列全了」的凭据，不是从枚举生成的）；
而 `sizeAllowed` / `widthChoices` / `sizeChoices` / 那两个下拉**一处都不用改** ——
它们全从表里推。§D4 那条「按判定写、不按名单写」在这里省下的正是这一批改动。

#### 没验到的部分

前端 **479 条全绿**、`ng build` 干净；后端 **753 条全绿**。浏览器里没看过：

1. 编辑一张**曲线卡** → 宽度选 `6`，高度下拉里**有 `200` 与 `308` 两项**（此前只有 `200`）；
2. 选 `6 × 308` 保存 → 卡片是**窄而高**的一竖条，与它右边一张 `W12H308` **底边对齐**；
3. 6 宽的高度下拉里**没有 416**。

### 2026-09-18 修订（十七）：保存按「改过」启用 + 自动刷新间隔收成工具条上一个全局项

#### 触发

> 1. Dashboard 的布局页面：只有真正修改了布局，"保存" 按钮才有效；"恢复默认"，也需要保存才生效。
> 2. Dashboard 页面：刷新按钮后面跟一个自动刷新时间的下拉框：比如 Off, 30s, 1m, 5m, 15m, 1h。
>    所以后端 DashboardWidget 里的 自动刷新时间间隔 refresh 可以去掉了，前端的这个相关字段和界面
>    也可以跟着改。

两件事其实是一件事的两面：**这个页面上「什么算用户的一个决定」被重新划了一次线**。
改布局要真的改过、恢复默认要点了保存才算数、刷新频率是看的人当下的偏好而不是布局的一部分 ——
三条都在把「草稿/库」与「这一屏/这一份布局」分开。

#### 一、全局自动刷新间隔

| | 改造前 | 现在 |
| --- | --- | --- |
| 存在哪 | 每张卡的 `refresh` 字段，跟布局一起落库 | **工具条上一个下拉**，按项目存在 `localStorage`（`dashboard.refreshSeconds.<spaceId>`） |
| 缺省 | 60 秒 | `1m`（**与改造前逐字一致**，所以升级后刷新节奏不变） |
| 选项 | 表单里一个 `nz-input-number`，0–86400 随便填 | `关闭 / 30s / 1m / 5m / 15m / 1h` |
| 定时器 | **按 `refresh` 的值分组**，一屏最多五个 `setInterval` | **一个** `setInterval`，到点整屏重取 |
| 权限 | 跟布局走（空间管理员才能改别人的刷新周期） | 跟浏览器走，**每个人各调各的** |
| 校验 | 服务端 `refresh ∈ [0, 86400]` | 前端认不出就回缺省值（`REFRESH_INTERVALS.includes`） |

**为什么这个存储位置是对的**：`refresh` 挂在卡片上时，它同时被当成两样东西使 ——
「这份布局长什么样」和「我此刻想让它多久刷一次」。前者是**空间的**（一份布局全空间共享，
改它要有管理员权限，还有乐观锁要过），后者是**看的人的**（我盯大屏时想 30 秒一次，
在自己电脑上开着不管时想 15 分钟一次）。一件要过乐观锁的事，被用来表达一个每秒都可能变的偏好，
是那个字段真正的毛病 —— 它让「切一下间隔」变成一次 PUT，还得先撞赢版本号。

**为什么不是「取所有卡里最小的那个间隔，到点全刷」以外的东西**：改造前那套分组是有道理的
（一个 10 秒的卡不该让整屏 60 秒的卡陪它每 10 秒请求一次），代码里也专门写了注释解释。
它换来的是：一屏最多五个定时器、每次布局变化都要全部重起、每张卡还得各自记「多久没刷」。
而它省下的请求，在 `render` 本来就能**一次取回整屏读数**之后就不成立了 —— 分组刷新省的是
「不重取整屏」，可 `render` 不传 `widgets` 时取的正是已存布局的整屏，服务端对每张卡是按需查的。
换句话说：**这个优化在取数接口是批量的时候，优化掉的是一个不存在的开销**。

**选项为什么是这六档**：`0`（关闭）+ 30 秒 / 1 分 / 5 分 / 15 分 / 1 小时。
往下一档 30 秒是「盯着看」的时候够用的最快节奏（再快就该用 WebSocket 了），
往上一档 1 小时是「挂着当背景板」的下限。`0` 复用词典里已有的「关闭」，
其余显示成 `30s` / `1m` / `5m` / `15m` / `1h` —— **纯数字与单位，不进那 66 份词典**
（与尺寸下拉的裸档位名 `W6H308` 同一个口径）。

#### 二、「保存布局」只在真改过时点得动

草稿与已存布局做**结构比较**（`dashboard.grid` 的 `layoutSignature` / `sameLayout`），
一样就 `disabled`。不这么做的话，「进编辑态 → 什么都没动 → 保存」会白跑一次 PUT，
还会在乐观锁下把版本号推上去 —— 别人正编辑着，就会莫名其妙撞上版本冲突。

三条钉住的口径：

1. **比结构，不比引用**。进编辑态是 `draft.set([...widgets])`：数组是新的、卡片对象还是旧的。
   只比数组引用会一进编辑态就说「改过了」；只比对象引用会漏掉「加了一张卡」。
   所以逐项比 `id / type / size / x / y / title / titleKey / config`。
2. **`config` 的键序不算差别**。`JSON.stringify` 是照插入顺序输出的，而编辑一趟回来键序可能变 ——
   那会被读成「改过」，于是按钮永远亮着。比较前先把键排序（这条有专门的用例）。
3. **`title` / `titleKey` 算差别**。改标题是用户真的改了这个布局，虽然它不影响摆放
   （所以那两条「拖回原处就不算改过」的直觉在这里不适用）。

#### 三、「恢复默认」只改草稿

它 `GET /layout/{spaceId}/preset` 拿一份**只读**的预置布局装进草稿，用户点「保存布局」才落库；
在那之前「退出编辑」就等于什么都没发生过。

- **`DELETE /layout/{spaceId}` 删掉了**，换成上面那个 GET。改造前那个端点一调，库里的布局
  当场就没了 —— 「保存才生效」在那套里根本无从谈起，所以它当时必须套一个确认气泡。
  现在那个气泡也去掉了：一个能撤销的动作不必先过一道确认。
- **预置布局仍然不给坐标**（理由见 §6.5），所以它照样要过 `ensurePlacements` 铺一遍。
- **不换 `layout`**：改造前它把 `layout` 一起换成预置那份，现在只换草稿 —— 手里那份「已存的
  是什么」得一直是真的，否则 `dirty` 的基准就错了，「保存」会带着错的 `version` 上去。
- 「恢复到从未保存过的状态」这件事因此从**一次 DELETE** 变成**用预置那份覆盖写一次**。
  `version` 在「从未保存过」时恒为 `0` 这条没变。

#### 改了什么

| 文件 | 改动 |
| --- | --- |
| `DashboardLayout.ts` | `DashboardWidget.refresh` 删除；新增 `REFRESH_INTERVALS`（六个值）与 `DEFAULT_REFRESH_SECONDS = 60` |
| `DashboardLayoutCodec.ts` | 读写 `refresh` 的两处删除（老文档里那个键因此变成「不认识的键」，同 `layout` 待遇） |
| `widget.editor.ts` / `.html` | 「刷新间隔」那个 `nz-form-item` 删除；「与类型无关的四项」→「两项」 |
| `dashboard.grid.ts` | 新增 `layoutSignature` / `sameLayout`（纯函数，带 spec） |
| `dashboard.component.ts` | `timers[]` + `restartTimers()` + `refresh(widgets)` + `merge()` → 一个 `timer` + `restartTimer()` + `refresh()`；新增 `refreshSeconds` / `refreshOptions` / `dirty` / `setRefreshInterval()`；`reset()` 改成拉预置进草稿；`addWidget` 不再写 `refresh` |
| `dashboard.component.html` | 「刷新」后面加间隔 `nz-select`；「保存布局」加 `[disabled]="!dirty()"`；「恢复默认」去掉确认气泡 |
| `dashboard.component.less` | `.toolbar .refresh { width: 88px }`（定宽，否则换档时整排按钮会挪） |
| `dashboard.service.ts` | `reset()`（DELETE）→ `preset()`（GET） |
| `DashboardWidget.java` | `refresh` 字段与它的 `@Schema` 删除 |
| `DashboardWidgetValidator.java` | `refresh ∈ [0, 86400]` 那条校验删除 |
| `DashboardLayoutCodec.java` | 编 `refresh` 的那段删除 |
| `DashboardLayoutService.java` | `DEFAULT_REFRESH_SECONDS` 删除、`normalized()` 只剩空判、`reset()` → `preset()`（返回预置，不写库） |
| `DashboardLayoutRepository.java` | `deleteBySpaceId` 删除（`reset()` 是它唯一的调用方） |
| `DashboardResource.java` | `@DELETE /layout/{spaceId}` → `@GET /layout/{spaceId}/preset`（`requireSpaceMember`，只读） |
| 三个后端测试 | 两条 DELETE 用例换成 `preset_member_returnsPreset` + `preset_isReadOnly_notDelete`（DELETE 现在是 405）；`refresh` 的读/写/区间用例删除；`SIZE_NAMES` 那句手抄名单不动 |

#### 没验到的部分

前端 **485 条全绿**（新增 7 条 `sameLayout` 用例、去掉 3 条 `refresh` 用例）、`ng build` 干净；
后端 **171 条 dashboard 用例全绿**（全套 753 条在改动前跑过）。浏览器里没看过：

1. 进编辑态 → **「保存布局」是灰的**；拖一张卡再拖回原处 → **还是灰的**（这条最值得试，
   它同时验了 `applyPlacements` 造的是新对象、以及结构比较认得出「拖回去了」）；
2. 改一下标题、或加一张卡 → **亮起来**；
3. 点「恢复默认」→ **没有确认气泡**，卡片当场换成预置那份，此时「保存布局」是亮的；
   点「退出编辑」→ 库里那份**没变**（重新进编辑态还是原来那份）；
4. 「刷新」后面那个下拉：选 `关闭` → 不再自动刷；选 `30s` → 半分钟一次；
   刷新整个浏览器 → **还是 30s**（按项目记着）；换个项目 → 回到那个项目自己记的那档，
   没记过就是 `1m`；
5. 老文档里的 `refresh` 不会让页面报错（它是被跳过的键），保存一次之后就自然没了。
