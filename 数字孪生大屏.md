# 园区数字孪生（浏览器 3D 大屏，WebSocket 实时 IoT）

>
> 场景：园区（室外建筑、道路、绿化、设备点位：摄像头、电表、水泵、路灯等）
> 数据：WebSocket 实时推送设备状态、告警、指标
> 未知点：**模型来源**，我先把模型来源讲清楚，再敲定整套技术方案、选型取舍。

## 一、园区场景模型来源（4 种方式，从低成本到高精度）

园区属于**室外大场景**，模型方案直接决定后续开发工作量、性能、效果。

### 方式 1：倾斜摄影模型（实景三维，最真实）

原理：无人机航拍，自动生成带纹理的园区实景三维模型，输出 OSGB 格式，再转成 3DTiles 瓦片。
✅优点：真实还原园区现状，建筑、树木、道路都自带，不用人工建模；适合对外汇报大屏。
❌缺点：模型体积巨大、三角面超多；**不能直接丢给 Three.js**，必须用 Cesium 加载 3DTiles；模型是 “实心面片”，很难单独选中某一栋楼、某一个路灯做交互；修改建筑外观困难。
适用：大范围园区，需要实景效果；不适合精细设备点位交互。

### 方式 2：人工简模（Blender / 3ds Max 建模，推荐！适配 Three.js）

原理：设计师按 CAD 总平图，**低面数简模**，只保留建筑轮廓、道路、绿化、设备底座；纹理简化，输出 GLB/GLTF。

>
> 重点：**数字孪生园区不需要精细室内，只需要外部简模**，控制面数！一栋楼几千面足够，不要做高模。
> ✅优点：

1. 模型是分离对象：每栋楼、路灯、水泵都是独立模型，可以点击拾取、变色高亮、绑定 IoT 数据；
2. GLB/GLTF 天生适配 Three.js；
3. 文件体积可控，浏览器流畅；
4. 可以自由加动画、飞线、点位标签。
   ❌缺点：需要建模人员，有建模成本；是 “仿真模型” 不是实景照片。
   适用：绝大多数物联网园区孪生项目，**你的场景首选模型方案**。

### 方式 3：CAD 总平面图 + 程序化生成（低成本，无美术）

原理：读取园区 CAD 图纸，前端代码自动拉建筑轮廓、生成体块、道路，不需要建模师。
✅优点：速度最快、成本最低；建筑都是独立几何体，交互方便；体积极小。
❌缺点：效果偏 “线框白模”，视觉质感差，适合内部运维监控，不适合对外展示大屏。

### 方式 4：现成商业平台模型库（ThingJS / 山海鲸等低代码）

原理：平台自带园区组件，拖拽建筑、道路、设备，不用自己建模。
✅优点：零建模，快速搭场景。
❌缺点：锁定平台，定制能力受限，按项目 / 点位收费。

>
> ✅总结模型选型建议：
>
>
> - 如果是**运维监控大屏，要点击楼宇、设备看 IoT 数据**：选【人工低面简模 GLB】+ Three.js 方案
> - 如果是**对外展示，追求实景照片效果，不需要精细单体交互**：选【倾斜摄影 3DTiles】+ Cesium 方案

---

## 二、两套完整技术方案对比（园区 + WebSocket IoT）

### 方案 A：Three.js + Vue3（推荐，园区简模场景）

**技术栈**

- 前端框架：Vue3 + Vite + Pinia
- 3D 引擎：Three.js
- 模型：Blender 导出 GLB 低面园区简模
- 可视化面板：ECharts（KPI、告警列表、趋势图）
- 实时数据：WebSocket 接收 IoT 平台推送；MQTT.js 备用（如果物联网平台是 MQTT）
- 3D 辅助库：
  - `@tweenjs/tween.js`：相机动画、视角切换
  - `lil-gui`：开发调试，上线删掉
  - 自定义射线拾取：点击楼宇 / 设备，弹窗展示实时数据
- 大屏适配：固定 1920/3840 分辨率，`postcss-px-to-viewport`

**功能清单**

1. 园区 3D 场景加载、缩放旋转漫游
2. 设备点位（2D/3D 标签），WebSocket 实时刷新数值
3. 告警：设备异常时模型闪烁、变色、弹出告警
4. 下钻：点击园区 → 楼栋 → 楼层设备
5. 飞线效果（可选，数据流 / 能源流向）
6. 右侧 / 顶部大屏面板，KPI、告警统计图表

✅优点

1. GLB 模型交互友好，每个设备、楼宇独立对象，物联网点位绑定非常方便；
2. 定制自由度极高，没有平台锁；
3. 浏览器性能可控，普通大屏主机流畅；
4. 开发生态成熟，资料多；
5. WebSocket 数据对接简单，前端收到数据直接更新标签 / 模型状态。

❌缺点

1. 需要建模人员制作园区低面 GLB 模型；
2. 大范围园区（几平方公里）纯 Three 加载海量模型会卡顿，需要自己做 LOD、分块加载；
3. 没有原生 GIS 能力，如果要叠加经纬度、地形比较麻烦。

适合：中小型园区，**重点是设备运维、告警监控，需要点击设备查看实时 IoT 数据**（你的物联网项目大概率是这个）

---

### 方案 B：CesiumJS + Vue3（倾斜摄影实景园区）

**技术栈**

- 前端框架：Vue3
- 3D 引擎：CesiumJS
- 模型：无人机倾斜摄影 → 3DTiles 瓦片
- 可视化：ECharts
- 实时数据：WebSocket
- 附加：GeoJSON 放置设备点位

✅优点

1. 原生 GIS，支持经纬度、地形、卫星底图；超大园区也能流畅加载瓦片；实景照片效果震撼。
   ❌缺点
2. 倾斜摄影模型是合并面片，很难单独选中某栋楼做高亮；
3. 学习成本更高；
4. 点位是贴在模型表面的 billboard 广告牌，3D 交互弱；
5. 倾斜摄影采集 + 处理成本高。

适合：大面积园区，**重点看实景全貌，设备点位只是标记，很少精细下钻交互**

>
> 👉我的建议：优先评估是否需要实景倾斜摄影。物联网园区项目，**90% 选方案 A Three.js + GLB 简模**。

## 三、架构分层（统一两套方案，后端你已有物联网平台）

```
IoT平台（你的现有平台）
    ↓ WebSocket实时推送（设备指标、告警事件）
前端大屏应用
├─ 2D大屏层：ECharts图表、告警列表、顶部标题、控制面板
└─ 3D场景层：Three.js/Cesium渲染园区模型、设备点位、模型高亮动画
```

数据流：
IoT 平台 → WebSocket 推送消息 → 前端接收消息 → Pinia 保存状态 → 自动刷新 3D 标签颜色 / 数值 + 刷新 2D 图表告警面板

>
> WebSocket 消息建议结构化示例：

```
{
  "deviceId": "light_001",
  "deviceName": "园区路灯1",
  "type": "status",
  "value": 1,
  "alarm": false,
  "time": "2026-09-16 15:30:00"
}
```

前端收到后：找到场景里对应的路灯模型，更新标签文字；alarm=true 时模型变红闪烁。

## 四、开发阶段拆分（方便排期）

1. 需求确认：确定园区范围、需要哪些设备点位、交互功能（漫游、下钻、告警）
2. 模型制作：CAD 图纸交给建模师，产出低面 GLB 园区模型；**模型规范非常关键（单独给你一份模型导出规范）**
3. 前端基础工程搭建：Vue3+Vite，集成 Three.js，加载 GLB 模型，基础漫游、点击拾取
4. WebSocket 数据对接：订阅 IoT 平台，数据状态管理
5. 可视化开发：ECharts 面板、告警动画、点位标签
6. 性能优化、大屏适配、压力测试（持续 WebSocket 高频消息）
7. 部署到 Nginx，大屏终端测试

## 五、关键风险点提前提醒

1. **模型面数爆炸**：最常见坑！建模师习惯做高模，浏览器直接卡死。必须强制要求低模。
2. **WebSocket 消息风暴**：设备很多时高频推送，前端要做防抖，不要每次消息都重绘渲染。
3. **浏览器内存泄漏**：切换视角、销毁标签要做好资源释放，长时间大屏 7×24 运行容易卡顿。
4. **大屏显卡性能**：展厅大屏主机显卡弱，需要提前做性能降级方案。

## 六、3D模型导入

1. 用户上传 3D 模型文件（GLB格式，单文件）。
2. 用户上传多个图片，调用腾讯云的3D生成API，自动生成3D模型文件。
